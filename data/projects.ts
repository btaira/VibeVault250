import type { Project } from "@/lib/types";

const SAAS: Omit<Project, "id" | "category">[] = [
  { title: "Churn Whisperer", tagline: "Predicts which subscribers are about to quit.", description: "A B2B dashboard that ingests Stripe + product analytics and flags accounts with rising cancel risk, including an AI-generated rescue email per segment.", prompt: "Build a Next.js SaaS backed by Postgres that connects to Stripe via OAuth and ingests subscription lifecycle webhooks (created, upgraded, canceled, payment_failed) into a normalized customers and events schema. Compute churn-risk features per customer — days since last login synced from your product analytics API, 30-day MRR trend, support ticket count, and consecutive failed payments — stored in a customer_features table refreshed nightly by a Vercel Cron job. Serve a risk-scoring endpoint using a logistic regression model in a Python FastAPI microservice that writes a risk_score and predicted_churn_date back to each customer record. For accounts above a configurable threshold, call Claude via the Vercel AI SDK with each customer's plan, tenure, and usage stats to draft a personalized win-back email queued through Resend with a per-account cooldown. Build a sortable admin table with one-click Send Rescue Email, outcome tracking per intervention, and a cohort chart showing how risk scores shift over time.", stack: ["Next.js", "Stripe", "Postgres", "Vercel AI SDK"], difficulty: "Advanced", vibes: ["B2B", "AI", "Revenue Ops"] },
  { title: "InvoiceNinja AI", tagline: "Drag-and-drop receipts, get categorized invoices.", description: "Upload photos of receipts and the app extracts line items, categorizes spend, and generates client-ready invoices with one click.", prompt: "Build a Next.js SaaS where freelancers upload receipt photos or PDFs via drag-and-drop; process each file through GPT-4o Vision using a structured JSON schema prompt to extract vendor, date, currency, tax, and individual line items. Store data in Postgres organized as clients → projects → expenses → line_items, with an inline-editable table for correcting OCR errors before finalizing. Generate branded PDF invoices server-side using PDFKit with the freelancer's logo, payment terms, itemized rows, and an embedded Stripe payment link so clients can pay directly. Send invoices via Resend with an open-tracking pixel and auto-follow-up after 7 days if the payment link hasn't been clicked, marking paid when Stripe fires the payment_intent.succeeded webhook. Add a receivables dashboard showing outstanding totals, average days-to-pay per client, and a running income summary for the current month.", stack: ["Next.js", "GPT-4o", "Stripe", "Resend"], difficulty: "Intermediate", vibes: ["Freelance", "Finance", "OCR"] },
  { title: "MeetMute", tagline: "Auto-summarize Zoom calls into Notion pages.", description: "Background bot that joins meetings, transcribes with Whisper, and writes a structured action-item recap into your team's Notion workspace.", prompt: "Build a Next.js SaaS that uses the Recall.ai bot SDK to join Zoom and Google Meet links programmatically — users paste a meeting URL and the bot joins within 30 seconds, records audio, and streams transcription via Whisper large-v3 hosted on Replicate. After the meeting ends, run the full transcript through Claude with a structured extraction prompt outputting JSON: meeting title, attendees, key decisions with supporting quotes, action items with assignee and due date, and a 3-sentence executive summary. Write the structured output into a Notion database via the Notion API using OAuth, creating a linked database record with rich text blocks, checkboxes for action items, and a toggle containing the full transcript. Let users configure a default Notion database, custom extraction prompts per meeting type (standup, 1:1, planning), and a Slack webhook to post the summary immediately after processing. Store all transcripts and metadata in Postgres so users can search across past meetings with full-text or semantic search.", stack: ["Next.js", "Recall.ai", "Whisper", "Notion API"], difficulty: "Advanced", vibes: ["Productivity", "Async"] },
  { title: "PromptPilot", tagline: "A/B test your AI prompts with real traffic.", description: "Drop-in SDK that lets product teams version, branch, and stat-sig test LLM prompts in production without a deploy.", prompt: "Build a TypeScript npm SDK that wraps OpenAI and Anthropic API calls, injecting prompt-version metadata and logging each request to a ClickHouse database with fields: prompt_version, model, latency_ms, token counts, a configurable quality_score signal, and a session_id for grouping. Implement a Thompson Sampling bandit allocator that the SDK consults on each call to route live traffic to prompt variants weighted by accumulated reward signals, with a configurable exploration rate. Build a Next.js dashboard where product teams write prompt variants in a syntax-highlighted editor, set traffic splits, and view real-time win-rate charts comparing quality, latency, and cost per variant side by side. Add two-proportion z-test significance calculation with a visible confidence indicator and an auto-promote action that flips all traffic to the winning variant once significance is reached at a user-defined threshold. Support Git-like prompt versioning so teams can branch, diff, and roll back to any prior variant in one click.", stack: ["Next.js", "Node SDK", "ClickHouse"], difficulty: "Advanced", vibes: ["DevTools", "Experimentation"] },
  { title: "FormForge", tagline: "Typeform but the form rewrites itself.", description: "Adaptive forms where the next question is generated by an LLM based on previous answers — perfect for onboarding flows.", prompt: "Build a Next.js form builder where creators describe their goal in plain English and the system generates an initial question sequence via Claude with a structured output schema. At runtime, each submitted response is sent to the LLM API to generate the next contextually appropriate follow-up question, enabling fully adaptive branching without manual logic trees — store session state in Postgres (or Redis for short TTL). Add a form designer where creators set a max question count, required fields like email, branching constraints such as 'always ask budget before ending', and preview the adaptive flow end-to-end before publishing. Store all responses in a structured answers table mapping to the form schema, provide a CSV export, and show a response analytics dashboard with answer distributions and per-question drop-off rates. Integrate Drizzle ORM for type-safe queries and add Stripe for a paid tier that unlocks custom domains and removes branding.", stack: ["Next.js", "OpenAI", "Drizzle"], difficulty: "Intermediate", vibes: ["No-Code", "Onboarding"] },
  { title: "StockSensei", tagline: "Plain-English alerts on your portfolio.", description: "Connect a brokerage and get LLM-written daily briefings that explain why your holdings moved.", prompt: "Build a Next.js SaaS that connects to Plaid Investments via OAuth to read a user's brokerage holdings, then fetches the last 24 hours of relevant market news for each ticker from Alpaca or Polygon.io news endpoints. Every morning at 7am in the user's local timezone via a Vercel Cron job, fetch previous-day price movements, compute percent change and portfolio contribution, and bundle the top 3-5 news articles per significant mover. Pass this structured context to GPT-4o with a prompt persona of 'experienced financial analyst writing a concise morning briefing for a retail investor' and generate a 3-5 paragraph summary explaining the WHY behind each major move in plain English. Send the briefing as a formatted HTML email via Resend with sparkline charts per holding rendered using a server-side charting library, an unsubscribe link, and a link to a web-readable archive. Add a dashboard where users can adjust which holdings to include, set a minimum % move threshold for briefing inclusion, and browse a history of all past daily briefings.", stack: ["Next.js", "Plaid", "Resend", "GPT-4o"], difficulty: "Intermediate", vibes: ["Fintech", "AI"] },
  { title: "DocuLint", tagline: "Grammarly for legal contracts.", description: "Highlights ambiguous clauses, missing definitions, and redlines them inline with suggested rewrites.", prompt: "Build a Next.js SaaS that accepts DOCX and PDF uploads, extracts full text and identifies clause boundaries using mammoth (DOCX) and pdf-parse (PDF), then passes each clause to Claude with a detailed legal-review prompt checking for: ambiguous party references, undefined terms used before definition, missing standard provisions (limitation of liability, governing law, dispute resolution), and one-sided indemnification language. Return a structured JSON array of flagged issues, each with clause text, issue type, severity (critical/warning/info), and a suggested rewrite that preserves the drafter's intent. Render the contract in a split-pane view where flagged clauses are highlighted with color-coded severity indicators; clicking any flag opens a side panel with the issue description and a one-click 'Apply Suggested Rewrite' button. Generate a redlined DOCX output using the docx library that inserts tracked-change annotations for all applied rewrites, downloadable with a single button. Add a dashboard showing contract health scores per upload and a settings page for custom clause checklists per contract type (NDA, MSA, employment agreement).", stack: ["Next.js", "DocX parser", "Claude"], difficulty: "Advanced", vibes: ["Legal", "AI"] },
  { title: "InboxZen", tagline: "Triage email like a chief of staff.", description: "Connect Gmail, get a morning brief that bundles, prioritizes, and drafts replies for everything below the fold.", prompt: "Build a Next.js SaaS that authenticates with Gmail via OAuth, fetches threads from the last 7 days using the Gmail API's threads.list endpoint, and groups them by a combination of sender domain clustering and LLM-assigned topic labels stored in a thread_labels table in Postgres. Each morning, score threads on urgency — time sensitivity, sender VIP status from a user-curated contacts list, and whether a reply is needed — using Claude with structured JSON output for batches of 20 threads at a time. Display the morning brief as a prioritized list with collapsed thread previews, a suggested action per thread (reply, archive, snooze), and pre-drafted reply text generated by an LLM primed with samples of the user's past sent emails to match their writing voice. Implement one-click approval: the user reviews, edits, and sends pre-drafted replies directly from the app using the Gmail API's messages.send endpoint without leaving the interface. Store tone embeddings from past sent emails in pgvector so reply drafts improve in accuracy and voice as more examples are captured over time.", stack: ["Next.js", "Gmail API", "Pinecone"], difficulty: "Advanced", vibes: ["Productivity", "AI"] },
  { title: "PixelPress", tagline: "Headless CMS for indie devs.", description: "Markdown + media management with built-in image optimization and a one-click GraphQL layer.", prompt: "Build a headless CMS as a Next.js app with a TipTap or CodeMirror editor supporting markdown, frontmatter, and content types defined via a JSON schema; back the media library with S3 or Cloudflare R2 with automatic WebP conversion and responsive resizing on upload. Implement a GraphQL API using GraphQL Yoga that auto-generates a type-safe schema from your content type definitions — each type gets list, single, and filtered query resolvers backed by Postgres via Drizzle ORM. Add webhook support so any published content change fires a configurable POST to rebuild a Vercel or Netlify site, with a delivery log showing each attempt, HTTP status, and response body for debugging. Support draft/published states, scheduled publishing via a cron job that checks publish_at timestamps, and content versioning with a simple before/after diff view using jsdiff. Provide an npm CLI tool that lets indie devs pull their content schema locally and generate TypeScript types for their front-end framework of choice.", stack: ["Next.js", "S3", "GraphQL Yoga"], difficulty: "Intermediate", vibes: ["DevTools", "Content"] },
  { title: "ScopeCreep", tagline: "Detects when client requests blow your estimate.", description: "Agency tool that watches Linear/Jira tickets and pings the PM when scope drifts past the original SOW.", prompt: "Build a Next.js SaaS for agencies that ingests a Statement of Work PDF at project kick-off — parse it with pdf-parse and run through Claude to extract a structured list of deliverables, each with a name, description, estimated hours, and acceptance criteria stored in Postgres. Connect to Linear via OAuth to sync all project tickets, and for each new or modified ticket, compute embedding similarity against the SOW deliverables using pgvector to detect tickets that fall outside agreed scope. When drift exceeds a configurable threshold (e.g., 15% of estimated hours consumed by out-of-scope tickets), send a Slack notification to the PM with a breakdown of drifted tickets, their similarity scores to the nearest SOW item, and an estimated dollar impact based on the team's hourly rate. Build an agency dashboard showing scope health per project: a burn chart, scope drift percentage over time, and a one-click change-order PDF generator that lists out-of-scope work and estimated additional cost. Support multi-project views so PMs can monitor all active engagements in a single prioritized list.", stack: ["Next.js", "Linear API", "Claude"], difficulty: "Advanced", vibes: ["Agency", "PM Tools"] },
  { title: "VoiceVault", tagline: "Clone your voice for client demos.", description: "Record 60s of audio, then turn any script into a narrated walkthrough with that voice.", prompt: "Build a Next.js SaaS where users record a 60-second voice sample via the browser's MediaRecorder API, upload the audio to the ElevenLabs voice cloning endpoint, and store the returned voice_id in a per-user profile in Postgres. Present a rich text script editor where users write narration for a product walkthrough, then submit it to ElevenLabs' TTS endpoint using their cloned voice to receive an MP3 audio file. Combine the MP3 narration with a screen recording or slide deck video using FFmpeg on the server via a Vercel background function, producing a final MP4 with audio synced to a configurable offset, delivered via signed S3 URL. Add a WaveSurfer.js waveform preview so users can scrub through the audio and request re-synthesis of individual sentences before combining with the video. Include a usage dashboard showing total characters synthesized this month, a shareable link per generated walkthrough with optional password protection, and a thumbnail editor for the shareable preview card.", stack: ["Next.js", "ElevenLabs", "FFmpeg"], difficulty: "Intermediate", vibes: ["Audio", "AI"] },
  { title: "RetroHQ", tagline: "Async sprint retros with AI clustering.", description: "Team posts wins/blockers all week, AI clusters themes for Friday's retro and surfaces patterns over time.", prompt: "Build a Next.js SaaS where team members post retrospective notes throughout the sprint, each tagged with a sentiment bucket (win, blocker, idea, shoutout) and optionally linked to a Linear ticket, all stored in Postgres. Embed all notes using OpenAI text-embedding-3-small and store in pgvector; on retro day, run k-means clustering in a Python microservice to group semantically similar notes into themes with cluster labels generated by Claude reading the top 5 notes per cluster. Build a real-time retro session view powered by Liveblocks where the facilitator drags themes into four columns (Worked Well / Improve / Action / Kudos), team members upvote items, and the session auto-generates a structured report with action items assigned to GitHub usernames. Email the retro report via Resend after the session and save it to a searchable archive with a trends dashboard visualizing which themes appear most often across the last 10 retros. Add streak-based engagement tracking to encourage consistent participation before retro day.", stack: ["Next.js", "OpenAI Embeddings", "Postgres"], difficulty: "Intermediate", vibes: ["Async", "Team"] },
  { title: "LinkLeaper", tagline: "Linktree but every link is a mini app.", description: "Creators embed live polls, tip jars, and AMAs directly inside their bio link.", prompt: "Build a Next.js creator link-in-bio platform where each user gets a profile page at /username with a curated link list plus embeddable live widgets: a Stripe tip jar with a goal progress bar, a live poll widget with votes persisted in Postgres, and an AMA question box where followers submit questions and creators answer them publicly. Implement a drag-and-drop theme editor with accent color pickers, background style options (gradient, glassmorphism, solid), button shape variants, and profile photo upload stored on S3. Track link clicks per-link with Postgres timestamps and aggregate them into a per-day analytics dashboard showing total clicks, top-performing links, and widget interaction counts with a 30-day trend chart. Monetize with a Stripe subscription for a pro tier that unlocks custom domains via CNAME verification, removes branding, and enables unlimited widgets. Deploy with Next.js static generation per profile page for fast global loads, revalidating on each content update via a revalidation webhook.", stack: ["Next.js", "Stripe", "Tailwind"], difficulty: "Beginner", vibes: ["Creator", "Monetization"] },
  { title: "SeoSage", tagline: "Continuous SEO audits with AI fix-it patches.", description: "Crawls your site nightly, scores pages, and opens GitHub PRs with metadata fixes.", prompt: "Build a Next.js SaaS that accepts a domain and crawls it nightly using Playwright in headless mode, visiting each discovered URL, running a Lighthouse programmatic audit to collect SEO scores, and extracting meta titles, descriptions, heading hierarchy, canonical tags, and Open Graph tags into a Postgres database. Diff the latest crawl against the previous run to identify regressions and newly discovered issues, then rank issues by estimated traffic impact using issue type weight × pages affected × organic traffic estimate from a Google Search Console OAuth integration. For the top 10 issues, generate specific HTML fix suggestions using Claude that receives the current snippet and issue description, then create a GitHub PR against the site's repo using the GitHub REST API with the suggested edits applied as file patches. Send a weekly Slack or email digest summarizing score changes, new issues found, and PR merge status with a historical score chart per domain. Add multi-domain support so agencies can monitor all client sites from a single dashboard with a site health heatmap.", stack: ["Next.js", "Playwright", "GitHub API"], difficulty: "Advanced", vibes: ["Marketing", "DevTools"] },
  { title: "ColdCallCoach", tagline: "Practice sales calls with an AI buyer.", description: "Voice-first roleplay with realistic objections and a per-call scorecard.", prompt: "Build a Next.js SaaS that uses the OpenAI Realtime API with WebRTC-based voice to enable real-time roleplay between a sales rep and an AI buyer persona — define a library of 10+ personas (skeptical enterprise buyer, price-sensitive startup founder, enthusiastic SMB owner) each with a system prompt specifying their objections, priorities, and conversational style. Record each call via MediaRecorder, transcribe with Whisper, and run the transcript through Claude evaluating the rep on a scoring rubric: discovery question quality (0-10), objection handling (0-10), value articulation (0-10), and close attempt (0-10), plus callouts for the 3 best and 3 worst moments with specific quotes. Store all transcripts and scores in Supabase with a per-rep history so managers can track improvement over time and identify which objection types are consistently mishandled across the team. Build a coaching dashboard where reps see their score trends, most-missed objections, and AI-suggested practice scenarios ranked by their personal weak spots. Add a scenario editor where sales managers can create custom buyer personas with their own objections, product context, and scoring rubrics for specialized use cases.", stack: ["Next.js", "OpenAI Realtime", "Supabase"], difficulty: "Advanced", vibes: ["Sales", "Training"] },
  { title: "PantryPilot Biz", tagline: "Inventory + recipe costing for ghost kitchens.", description: "Scan delivery invoices, auto-update ingredient costs, get margin alerts per menu item.", prompt: "Build a Next.js SaaS for ghost kitchens where operators photograph or upload supplier delivery invoices; process them through GPT-4o Vision to extract ingredient name, unit, quantity, and price per unit as structured JSON, saving to an ingredient_costs table in Postgres that maintains a price history per ingredient. Sync with Square or Toast POS via webhooks to ingest all menu item sales in real time, and compute per-recipe COGS by resolving each menu item's recipe (ingredients and quantities from a user-defined recipe table) against current ingredient costs. When any ingredient cost changes more than 5%, automatically recalculate margins for all recipes using that ingredient and push a Slack alert showing which menu items are now below the minimum margin threshold set by the operator. Build a dashboard displaying real-time margin per menu item, a cost-per-dish history chart, and a recipe optimizer that suggests ingredient substitutions ranked by margin recovery potential. Export weekly COGS reports as CSV for accounting and add a low-stock alert when ingredient quantities fall below a configurable par level.", stack: ["Next.js", "Square API", "GPT-4o Vision"], difficulty: "Intermediate", vibes: ["Restaurant", "Ops"] },
  { title: "PaywallPilot", tagline: "Smart paywalls for content sites.", description: "ML-driven decision on when to show a paywall based on session signals.", prompt: "Build a Next.js SaaS with an embeddable JavaScript snippet served from a CDN edge function that collects anonymous visitor signals per page load: articles viewed in session, scroll depth, referrer domain, time-of-day, device type, and return visitor cookie — hashed into a feature vector and POSTed to a scoring Edge Function. Train a gradient-boosted classifier using LightGBM on historical conversion data (visitors who subscribed vs. bounced), serialize it to ONNX, and serve predictions with sub-50ms latency from Cloudflare Workers KV. Based on predicted conversion probability, route the visitor to one of three paywall variants (soft, metered, hard) and log the variant shown plus the eventual outcome in ClickHouse for model retraining. Build an admin dashboard with a lift chart showing revenue uplift vs. a random baseline, per-variant conversion rates, A/B test configuration for new paywall copy, and a model retraining trigger after each month of new data. Support multi-site with separate model instances and paywall configurations per publication.", stack: ["Next.js", "Edge Functions", "ClickHouse"], difficulty: "Advanced", vibes: ["Media", "Conversion"] },
  { title: "BugBounce", tagline: "Auto-triage GitHub issues with AI labels.", description: "Reads new issues, applies labels, suggests duplicates, and pings the right code-owner.", prompt: "Build a GitHub App using Probot or the Octokit REST SDK that listens to issues.opened and issues.edited webhooks, then embeds each new issue's title and body using OpenAI text-embedding-3-small and queries a pgvector table of all existing issues for the top 5 nearest neighbors. Use Claude to analyze the nearest neighbors and determine: is this a duplicate (with confidence score), which labels apply (bug/feature/question/docs), which code area is affected (parsed from CODEOWNERS + git blame on files mentioned in the issue), and who should be pinged. Post a formatted comment on the issue with suggested labels applied via the GitHub labels API, possible duplicates linked with similarity scores, and a code-owner mention — only auto-apply labels when confidence exceeds a configurable threshold to avoid false positives. Store all issue embeddings, triage decisions, and feedback (when a human overrides the bot's label) in Postgres to retrain and improve accuracy over time. Add a web dashboard for maintainers to review triage accuracy metrics, configure confidence thresholds per repo, and see a weekly digest of issue volume and bot accuracy.", stack: ["Next.js", "GitHub Probot", "pgvector"], difficulty: "Advanced", vibes: ["DevTools", "Open Source"] },
  { title: "RentRadar", tagline: "Track listings, get notified the moment one drops.", description: "Niche rental aggregator with email + push alerts when a unit matches your filter set.", prompt: "Build a Next.js rental aggregator that scrapes Zillow, Apartments.com, and Craigslist rental listings on a 15-minute cycle using Playwright with rotating user agents, parsing listing details (address, price, beds, baths, sqft, availability date) into a normalized Postgres database and deduplicating by canonical address using fuzzy matching. Let users define saved search filters (city, max rent, min beds, pet-friendly, max commute time to a target address via Google Distance Matrix API) stored per account in a user_filters table, with a map-based filter UI using Mapbox. When a new listing matches a saved filter, send a Web Push notification and a Resend email alert within 60 seconds of discovery, including a Mapbox static map embed showing the unit's location relative to the commute target. Add a listing comparison view where users pin up to 5 units and compare side-by-side with commute times, price-per-sqft, neighborhood walk scores from the Walk Score API, and a notes field per listing. Implement polite scraping with configurable rate limits, request delays, and a proxy rotation strategy to stay within fair-use norms.", stack: ["Next.js", "Playwright", "Postgres"], difficulty: "Intermediate", vibes: ["Real Estate", "Alerts"] },
  { title: "QuoteForge", tagline: "Quote generator for service businesses.", description: "Plumbers, painters, movers — describe the job, get a branded quote PDF in 30s.", prompt: "Build a Next.js SaaS for tradespeople where a service pro describes a job in natural language — 'replace kitchen faucet and fix slow drain in the bathroom' — and an LLM generates a structured quote with itemized line items, quantities, unit prices, and subtotals based on the user's stored price book in Postgres. Render the quote in an editable table so the pro can adjust any line item before finalizing, then export a professional branded PDF using PDFKit with the company logo, payment terms, expiration date, and a drawn-signature canvas block for electronic signing. Let clients sign the quote via a shareable link (storing signed_at timestamp in Postgres) and trigger a Stripe payment request for a configurable deposit percentage upon signing. Add a quote history dashboard with status tracking (draft, sent, signed, paid, declined) and follow-up reminders sent via Resend if a quote hasn't been viewed in 48 hours. Allow pros to build and refine their price book over time, with Claude suggesting new line items based on job descriptions that don't match any existing entry.", stack: ["Next.js", "OpenAI", "PDFKit"], difficulty: "Beginner", vibes: ["SMB", "Trades"] },
  { title: "StackSage", tagline: "Onboarding tour generator for new hires.", description: "Point at your repo and the app produces a guided walkthrough of the codebase.", prompt: "Build a Next.js SaaS that accepts a GitHub repo URL, fetches the repository via the GitHub API (zip download or tree endpoint), and parses function and class definitions across all supported languages using Tree-sitter, storing them in a Postgres table alongside their file paths and docstrings. Pass the full file tree and git log (most-changed files over last 90 days) to Claude with a prompt that identifies architecturally significant files — entry points, core abstractions, high-churn files, and files with the most imports — then generates a 6-12 step guided tour where each step explains what a file does, why it matters, and how it connects to the previous step. Render the tour as an interactive step-by-step walkthrough with syntax-highlighted code snippets from the actual files, an architecture diagram auto-generated as Mermaid.js markup, and a sidebar table of contents with estimated reading time per step. Let teams add custom annotations, mark steps as role-specific (frontend vs. backend vs. infra engineer), and embed runbook links alongside any step. Export finished tours as a static HTML file that can be committed to the repo's docs/ directory and linked from the README.", stack: ["Next.js", "Tree-sitter", "Claude"], difficulty: "Advanced", vibes: ["DX", "Onboarding"] },
  { title: "AdCopyArena", tagline: "Battle-test ad copy in a single click.", description: "Generates 12 ad variants and instantly scores them against your historical CTR.", prompt: "Build a Next.js SaaS where marketers connect their Meta or Google Ads account via OAuth and import historical campaign data (ad copy, targeting, impressions, clicks, conversions) into Postgres, then train a LightGBM CTR predictor in a Python microservice on their own campaign history so predictions are calibrated to their specific audience and vertical. Implement an ad copy generator that takes a product description, target audience, and campaign objective and produces 12 distinct variants using Claude, varying headline style, emotional tone, proof type (social proof, scarcity, feature-led), and call-to-action phrasing across the variants. Score each generated variant through the trained CTR predictor and rank them with confidence intervals displayed as a horizontal bar chart, color-coded by predicted performance tier (top / neutral / avoid). Let marketers save top variants directly to their Meta or Google Ads account as draft ads via the respective APIs, and track actual post-launch performance against predictions to continuously improve the model. Add a variant history view with a searchable archive of all generated and tested copy organized by campaign, and a week-over-week accuracy report showing how well the CTR predictor has performed.", stack: ["Next.js", "Meta API", "OpenAI"], difficulty: "Advanced", vibes: ["Marketing", "AI"] },
  { title: "DealDeck", tagline: "Pitch deck reviewer that thinks like a VC.", description: "Drop a PDF deck, get partner-grade feedback in seconds.", prompt: "Build a Next.js SaaS that accepts PDF pitch deck uploads, parses them page-by-page using pdf-parse to extract text and page structure, then runs each slide's content through Claude with a VC evaluation prompt checking for: market size framing (TAM/SAM/SOM clarity), competitive moat articulation, team slide strength (relevant experience and founder-market fit), traction evidence (metrics, growth rate, customer logos), and financial projection realism. Generate a structured JSON review with a per-slide score (0-10), the top strength, the top weakness, and a copy-paste-ready rewrite suggestion for the weakest element on each slide. Render the review as a split-screen interface with the deck on the left (using PDF.js for rendering) and critique panels on the right color-coded by score, with the suggested rewrites appearing inline. Produce a downloadable annotated PDF using PDFKit that inserts comment bubbles on each slide with the critique text, suitable for sharing with co-founders or advisors. Add a benchmarking section that compares each slide's score against anonymized averages from a curated database of successful seed-stage pitch decks.", stack: ["Next.js", "PDF.js", "Claude"], difficulty: "Intermediate", vibes: ["Startups", "Pitch"] },
  { title: "ChurnScribe", tagline: "Auto-write release notes from PRs.", description: "Pulls merged PRs each week and outputs polished customer-facing release notes.", prompt: "Build a Next.js SaaS that connects to a GitHub organization via OAuth, listens to pull_request.closed webhooks (merged only), and groups merged PRs by milestone or release tag into a release_events table in Postgres, storing each PR's title, body, labels, and any embedded images from the PR description. For each release event, pass all PR titles, bodies, and labels to Claude with a prompt that transforms technical PR summaries into user-facing changelog entries grouped by category (New Features, Improvements, Bug Fixes, Deprecations), written in a warm marketing voice appropriate for end users. Let teams configure their changelog voice (technical, friendly, formal) and define label-to-category mappings in a settings UI so the AI routing is consistent and controllable across releases. Publish changelogs to a public changelog page with a clean timeline UI, send a Resend email digest to subscribers who opted in, and post to a Slack channel via webhook immediately after publish. If a PR body contains image links (common in UI-facing PRs), automatically embed those screenshots in the changelog entry for visual context that showcases the actual UI improvement.", stack: ["Next.js", "GitHub API", "OpenAI"], difficulty: "Beginner", vibes: ["DevRel", "Marketing"] },
  { title: "BackupBuddy Pro", tagline: "Encrypted backups for indie devs' Postgres.", description: "Hourly snapshots, point-in-time restore, and Slack alerts on anomalies.", prompt: "Build a Next.js SaaS that connects to users' Postgres databases via a secure connection string encrypted at rest with libsodium, uses pg_dump with --format=custom to capture hourly snapshots, and uploads them AES-256 encrypted to an S3 bucket or Cloudflare R2 with a configurable retention policy (7 days, 30 days, 90 days). Implement a point-in-time restore UI where users browse available snapshots on a timeline, select a target, and initiate a restore to a new database spun up on Neon or Supabase via their provisioning API, with a progress bar and row count verification after completion. Continuously analyze backup deltas between snapshots — if the delta size is unusually large or small relative to a rolling 7-day average (a sign of bulk deletes or data corruption), trigger a Slack alert with the anomaly details and a link to inspect the last-known-good snapshot. Build a dashboard showing backup health: last successful backup timestamp, storage used, estimated monthly cost, and the result of an automated weekly restore-test that verifies row count integrity against the live database. Add Stripe usage-based billing tied to database size in GB with a free tier for databases under 1GB.", stack: ["Next.js", "Postgres WAL", "S3"], difficulty: "Advanced", vibes: ["DevOps", "Reliability"] },
  { title: "DemoMagnet", tagline: "Convert sales demos into self-serve replays.", description: "Records the demo session and turns it into a clickable interactive replay for the prospect.", prompt: "Build a Next.js SaaS that records browser-based sales demos using the Screen Capture API (getDisplayMedia) and MediaRecorder to capture video and audio simultaneously, uploading the recording to S3 then processing it through Whisper via Replicate for a timestamped transcript. Use Claude to segment the transcript into 5-10 logical topics (e.g., 'Problem Overview', 'Reporting Feature', 'Pricing') each with a start and end timestamp, storing segments in Postgres and generating AI thumbnail descriptions for each chapter. Build an interactive replay player using a custom HTML5 video player where chapter navigation appears in a sidebar with thumbnail previews, and interactive hotspots generated from the transcript's feature mentions appear as clickable overlays at the correct timestamps — clicking opens a modal with deeper detail, a docs link, or a case study. Generate a unique shareable link per replay with an optional email-capture gate (storing lead info in Postgres), and track which chapters and hotspots each prospect engaged with, reporting engagement analytics back to the sales rep. Add a CRM integration via webhooks that fires a prospect_engaged event when a prospect watches more than 60% of a replay.", stack: ["Next.js", "RecordRTC", "Whisper"], difficulty: "Advanced", vibes: ["Sales", "Video"] },
  { title: "GuildLedger", tagline: "Bookkeeping designed for D&D groups & raid teams.", description: "Track shared expenses, loot drops, and reimbursements with a fantasy-themed UI.", prompt: "Build a Next.js expense-splitting app with a tabletop RPG aesthetic — groups are 'guilds', members are 'adventurers', expenses are 'loot drops', and settlements are 'tribute payments'. Implement the Splitwise debt-simplification algorithm in Postgres: track who paid what, compute per-member balances, and simplify debts to the minimum number of transfers using a greedy graph-reduction approach. Support custom guild currencies (gold pieces, credits, doubloons) defined by the guild alongside real currency for actual settlement via Stripe Connect peer-to-peer payouts between members. Let guilds log loot drops with item name, gold value, optional photos stored on S3, and expense tags (tavern, travel, quest supplies), with a running adventure ledger rendered in a fantasy-styled UI using custom Tailwind components and illustrated card elements. Generate a quarterly 'Campaign Report' PDF using PDFKit — styled like a D&D adventure module — summarizing total guild spending, biggest spenders, most common expense categories, and a pie chart of spend by tag, perfect for tax-season reference.", stack: ["Next.js", "Tailwind", "Stripe"], difficulty: "Beginner", vibes: ["Niche", "Gaming"] },
  { title: "OpsParrot", tagline: "Slack bot that explains your dashboards.", description: "Ask in plain English, get an answer pulled live from Looker/Mode/Metabase.", prompt: "Build a Slack bot using Bolt for JavaScript that listens for slash commands and @mentions, accepts natural language questions like 'what was revenue last Tuesday by region?', and resolves the question against the user's connected BI tool (Metabase, Looker, or Mode) by authenticating with each tool's REST API via stored OAuth tokens. Convert the natural language question to SQL using Claude with schema context — table names, column names, and sample values loaded from the BI tool's metadata API and cached in Redis with a 1-hour TTL — then execute the query against Metabase's /api/dataset endpoint or Looker's run_inline_query endpoint with a 30-second timeout. Post the result back to Slack as a formatted message: a table for results under 20 rows, a chart image rendered server-side using Chart.js and captured via a headless Playwright screenshot for larger results, and a brief LLM-generated plain-English interpretation with data freshness caveats. Log all queries, execution times, and row counts to a Postgres audit table so teams can review what data is being accessed and identify expensive or frequently-run queries for optimization. Add an admin panel to configure which Slack channels and user groups have access to which BI tool connections and which database schemas.", stack: ["Slack Bolt", "Metabase", "OpenAI"], difficulty: "Advanced", vibes: ["Analytics", "ChatOps"] },
  { title: "NicheNewsletter", tagline: "AI editor that runs your niche newsletter.", description: "Daily roundup curated, summarized, and sent — you just approve.", prompt: "Build a Next.js SaaS where users define a newsletter topic and audience persona (e.g., 'Biotech for non-scientists') and configure 5-15 content sources: RSS feeds, subreddits via the Reddit API, and Hacker News search queries via the Algolia HN Search API. Run a cron job every 24 hours that fetches new items from all sources, ranks them by relevance to the defined topic using embedding cosine similarity via pgvector, deduplicates URLs, and selects the top 8-12 items. Pass the ranked items to Claude, which writes a 2-3 sentence summary per item, selects a witty intro paragraph in the user's defined voice (formal, casual, snarky), and groups items into named sections with transition sentences between them. Render a preview of the newsletter in a Substack-compatible HTML format that users can approve with inline editing — auto-publish on a schedule or hold for manual review — then deliver via Resend with a web-accessible archive page, open and click tracking, and an unsubscribe link. Add a growth dashboard showing subscriber count, open rate, click rate per issue, most-clicked items, and a recommendation engine that suggests adding new content sources based on reader engagement patterns.", stack: ["Next.js", "Cron", "OpenAI"], difficulty: "Intermediate", vibes: ["Creator", "Curation"] },
  { title: "TicketTriageAI", tagline: "Auto-route Zendesk tickets with confidence scores.", description: "Saves CS managers hours by classifying and prioritizing inbound tickets with explainability.", prompt: "Build a Next.js Zendesk integration (packaged as a Zendesk App Framework app) that hooks into ticket.created and ticket.updated events, embeds each new ticket's subject and body using OpenAI text-embedding-3-small, and queries a pgvector table of resolved tickets for the 5 most similar past tickets. Pass those neighbors plus the new ticket to Claude with a structured output prompt producing: urgency score (1-5), category label (billing, technical, feature-request, account), confidence score (0-1), recommended assignee group, and a one-sentence reasoning chain in plain English. Auto-apply the suggested labels via the Zendesk API if confidence exceeds 85%, add a private internal note with the routing rationale and similar tickets linked, and escalate to a senior queue if urgency ≥ 4. Build a CS manager dashboard outside Zendesk showing triage accuracy (labels accepted vs. overridden), average handling time by category, SLA breach risk heatmap by ticket age, and category volume trends by hour and day of week. Retrain the category classifier monthly on labeled closed tickets using the feedback loop of human overrides to continuously improve routing precision.", stack: ["Next.js", "Zendesk API", "Claude"], difficulty: "Intermediate", vibes: ["CS", "AI"] },
  { title: "GymGenie SaaS", tagline: "White-label coach app for personal trainers.", description: "Coaches design templated programs; clients get a polished branded mobile-web experience.", prompt: "Build a multi-tenant Next.js SaaS where fitness coaches create an account, customize their branded client portal (logo, colors, welcome message stored in a tenant_config table), and design reusable workout program templates with named phases, weeks, and daily sessions specifying exercises, sets, reps, rest time, and coaching notes. Implement tenant isolation using tenant_id foreign keys on every Postgres table scoped by Supabase row-level security policies, so each coach's data is completely isolated and all API queries automatically filter by the authenticated tenant. Clients receive invitation emails via Resend and access their branded portal at coach-slug.gymgenie.app (wildcard DNS + Next.js middleware routing) where they log completed workouts, track personal records, and message their coach via a simple inbox. Automate a Monday check-in flow: clients fill a short survey (sleep, soreness, energy, notes) stored in Postgres; coaches see a weekly dashboard of client check-ins with color-coded flags when a client missed more than 2 consecutive sessions. Add Stripe billing for coaches (monthly subscription) and optional client payment collection for one-time program purchases or recurring coaching fees with revenue share tracking.", stack: ["Next.js", "Multi-tenant", "Supabase"], difficulty: "Advanced", vibes: ["Fitness", "White-label"] },
  { title: "WaitlistWizard", tagline: "Beautiful waitlists with referral mechanics.", description: "Drop a snippet, get a Robinhood-style waitlist with ranks and shareable referral codes.", prompt: "Build a Next.js embeddable waitlist widget (a single <script> tag with a data-product-id attribute) that renders a styled signup form, captures email and optional name, stores entries in Postgres, and immediately assigns a queue position based on signup order plus a referral bonus. Generate a unique referral code per signup and display it with pre-populated shareable links (tweet text, WhatsApp message) on a personalized confirmation page showing 'Your spot: #247' that updates in real-time via a Postgres LISTEN/NOTIFY and SSE endpoint. Implement the Robinhood-style 'move up the line' mechanic: every confirmed referral signup (tracked via referral_code query parameter) moves the referrer up N positions (configurable per product); show a live leaderboard of top referrers with their current rank and number of successful referrals. Build an admin dashboard behind auth showing total signups, referral chain trees rendered as a D3 force graph, viral coefficient (average referrals per converted user), conversion funnel from link click to signup, and a CSV export of all waitlist entries sorted by position. Add a webhook event fired to the admin endpoint when the waitlist crosses configured milestones (100, 500, 1000 signups).", stack: ["Next.js", "Edge Functions", "Postgres"], difficulty: "Beginner", vibes: ["Growth", "Launch"] },
  { title: "TermAlert", tagline: "Monitor competitor's terms-of-service for changes.", description: "Watches ToS pages and notifies you with a redline diff when anything material updates.", prompt: "Build a Next.js SaaS where users input a list of Terms of Service, Privacy Policy, or other legal document URLs to monitor — store them in a pages table in Postgres with a SHA-256 hash of the last-known content. Run a Playwright-based cron job every 24 hours to visit each URL (handling JavaScript-rendered pages by waiting for network idle), extract all visible text content, compute a new hash, and compare against the stored version; if different, create a change_event record with before/after text snapshots. For each detected change, pass the before and after text to GPT-4o with a prompt instructing it to identify which sections changed, classify each change as Material (affects user rights, data collection, payment terms, liability) or Minor (formatting, grammar fixes), and write a plain-English summary of the practical impact for a non-lawyer. Render a side-by-side diff view using jsdiff with Material changes highlighted in red, Minor changes in gray, and the AI summary displayed prominently at the top; send a Resend email alert to all subscribed users for any Material change. Add a compliance dashboard showing all monitored pages, last-checked timestamp, change frequency history, and a searchable change log filterable by Material vs. Minor classification.", stack: ["Next.js", "Diff", "GPT-4o"], difficulty: "Intermediate", vibes: ["Compliance", "Monitoring"] },
  { title: "GrantGuru", tagline: "Match nonprofits to grants automatically.", description: "Profile your org, get a daily list of grants you actually qualify for.", prompt: "Build a Next.js SaaS where nonprofits create a detailed organization profile — mission statement, focus areas, annual budget range, geographic scope, population served, 501c3 status, and past grant history — stored in Postgres, with the profile text embedded using OpenAI text-embedding-3-small and stored in pgvector. Scrape or sync grant listings from Grants.gov (RSS/API), regional community foundations (Playwright scrapers), and user-submitted foundation portals on a weekly cadence, embedding each grant's eligibility requirements and focus areas for similarity matching. On a weekly cadence per organization, compute cosine similarity between the org's embedding and all active grant listings, filter out already-applied grants (tracked in an applications table), and rank the top 15 matches. For each top match, use Claude to generate a 2-3 paragraph eligibility assessment explaining why the org is a strong fit, what the grant covers, the application deadline, and one concrete suggestion to strengthen their application for that specific funder. Deliver weekly match digests by email via Resend and surface a web dashboard where grant managers can mark opportunities as pursuing, submitted, awarded, or declined — building a full grant pipeline CRM with a funding pipeline value total.", stack: ["Next.js", "pgvector", "OpenAI"], difficulty: "Intermediate", vibes: ["Nonprofit", "Matching"] },
  { title: "SaaSWatch", tagline: "Catch all your unused subscriptions automatically.", description: "Reads receipts from Gmail, flags duplicates and unused tools across the org.", prompt: "Build a Next.js SaaS that connects to Google Workspace Gmail via OAuth and scans all receipt emails from the last 12 months using the Gmail API's search query 'receipt OR invoice OR billing', then passes each email through Claude to extract: vendor name, amount, billing frequency, billing email, and service category — building a comprehensive subscription inventory stored in Postgres. Cross-reference detected vendors against an Okta or Google Workspace directory synced via OAuth to identify which employees use each tool (matched by email domain), flag subscriptions where the purchasing employee has left the company, and detect overlapping tools serving the same function using category embeddings and pgvector similarity (e.g., two project management tools). For each redundant or unused subscription, generate an AI-drafted action plan: who to contact for cancellation, estimated annual savings, and the recommended alternative tool from the company's existing approved stack. Build an admin dashboard showing total SaaS spend by department and category, month-over-month spend trend, top 10 most expensive tools with per-seat cost breakdown, and a savings opportunities total with one-click Linear or Jira ticket creation for each recommended action. Schedule a monthly spend report sent to the CFO's email via Resend formatted as an executive summary with trend charts.", stack: ["Next.js", "Gmail API", "Okta"], difficulty: "Advanced", vibes: ["Finance Ops", "AI"] },
  { title: "Rephrase.ai Pro", tagline: "Tone-shift entire knowledge bases.", description: "Migrate your help docs from formal to friendly with one click — preserving structure.", prompt: "Build a Next.js SaaS that connects to Notion, Intercom Articles, or Zendesk Guide via OAuth and ingests the full knowledge base as a tree of documents, preserving heading hierarchy, body content, embedded images, and code blocks with their exact formatting. Present a tone palette selector with presets (Formal → Casual, Passive → Active, Dense → Scannable, Jargon-Heavy → Plain English) plus a free-form 'custom tone guidelines' text box where users describe their brand voice. Process each document by sending its body text to Claude with the tone-shift prompt — marking code blocks and headings as [PRESERVE] tokens so they pass through untouched — and producing a rewritten version that hits the target tone without altering technical accuracy. Display a side-by-side before/after diff for each document using jsdiff, with a Flesch-Kincaid readability score shown for both versions so users can verify the accessibility improvement quantitatively. Let users approve individual documents, bulk-approve an entire folder, or push all approved rewrites back to the source system via the respective API, with a CSV audit trail of every changed document including old and new readability scores for governance review.", stack: ["Next.js", "OpenAI", "Notion API"], difficulty: "Intermediate", vibes: ["Content", "Migration"] },
  { title: "VetVerify", tagline: "Background checks for marketplace sellers.", description: "Marketplace operators run on-demand verification flows with global ID providers.", prompt: "Build a Next.js verification orchestration platform where marketplace operators use a visual drag-and-drop workflow builder to sequence identity verification steps — government ID check, selfie liveness test, address verification, business registration check — pulling from provider APIs like Persona, Veriff, or Onfido, with each step configurable via pass/fail thresholds and retry limits. Store workflow definitions as JSON in Postgres and expose a Verification Sessions API (with API key auth) that marketplace clients call to initiate a verification for an end user; the platform executes the workflow steps in order, calling the relevant provider SDKs server-side, collecting results, and writing step-level outcomes to a sessions table. Build a verification dashboard showing session status, step-by-step results with risk flags (document expired, face match below threshold, address mismatch), and the final pass/fail outcome with a complete audit log of every API call, response, and timestamp. Fire HMAC-signed webhook events (verification.started, verification.passed, verification.failed) to the marketplace's configured endpoint with a retry mechanism for failed deliveries, and provide a webhook delivery log in the admin UI. Store all PII in encrypted Postgres columns using pgcrypto with a configurable data retention policy and automatic purge after a compliance-defined window.", stack: ["Next.js", "Persona", "Webhooks"], difficulty: "Advanced", vibes: ["Trust", "KYC"] },
  { title: "ShipShape", tagline: "AI-generated changelog landing pages.", description: "Each release auto-publishes a beautiful landing page with screenshots and a tweet thread.", prompt: "Build a Next.js SaaS that watches a GitHub repo for new release tag pushes via a GitHub webhook, fetches all PRs merged since the previous release tag using the GitHub API's compare endpoint, and passes them to Claude to produce: a marketing headline for the release, a 3-5 bullet feature summary for a landing page, and a Twitter/X thread of 7-10 tweets announcing the release in a conversational, excitement-building style. Auto-generate a full release landing page at a custom subdomain (releases.yourapp.com/v1.2.3) using a Next.js dynamic route with a hero section, features list with emoji bullets, changelog grouped by PR label, and an embedded GitHub diff summary showing files changed. Extract the first screenshot found in any merged PR description and use it as the hero image — if none found, generate a stylized version badge SVG using a server-side SVG template. Queue the tweet thread to the Twitter API v2 using threads.write and schedule it for a configurable day and time (e.g., Monday 9am in the user's timezone), tracking engagement metrics (likes, retweets, link clicks) in a Postgres analytics table. Notify the team in a Slack channel when a release page goes live and again when the tweet thread has been posted successfully.", stack: ["Next.js", "GitHub API", "Tweet API"], difficulty: "Intermediate", vibes: ["DevRel", "Launch"] },
  { title: "TimeTeller", tagline: "AI timekeeper for law firms.", description: "Reads your calendar + emails to suggest billable time entries you forgot to log.", prompt: "Build a Next.js legal-tech SaaS that connects to Microsoft Outlook and Google Calendar via OAuth (Microsoft Graph API and Google Calendar API) and Gmail for email threading, continuously syncing calendar events and email metadata — subject, sender, recipients, duration — into a Postgres activity log without storing email body content to protect privilege. Run a nightly LLM classification pass over the day's activities, matching each to a client matter using a user-defined matter list (matter name, number, client name, practice area) stored in Postgres — Claude determines the most likely matter based on subject keywords, recipient email domain, and calendar event title patterns. Pre-generate draft time entries for each activity: matter number, timekeeper ID, hours from duration rounded to the nearest 0.1 hour, a billing code based on activity type (call, research, drafting, review), and a 6-8 word description written in proper legal billing style. Push draft entries to Clio Manage via the Clio API (using OAuth) into the attorney's pending time entries queue for one-click approval, and track approval rate and modification rate per attorney to measure AI accuracy over time. Build a billing realization dashboard showing unbilled hours by matter, revenue at risk calculated as unbilled hours × billing rate, and capture rate trending upward as the model improves from attorney corrections.", stack: ["Next.js", "Clio API", "Microsoft Graph"], difficulty: "Advanced", vibes: ["Legal", "Time Tracking"] },
  { title: "PluginPilot", tagline: "Self-serve Figma plugin marketplace for orgs.", description: "Internal app store where design ops teams approve and distribute custom Figma plugins.", prompt: "Build a Next.js internal Figma plugin distribution platform where design ops admins upload plugin bundles (manifest.json + code.js) to S3, set a version number, write release notes, and choose which team groups can access each plugin using Okta or Google Workspace group membership synced via OAuth and cached in Postgres. Implement an approval workflow where plugin submissions enter a review queue visible to design ops admins who can test the plugin via a sandboxed Figma preview, leave inline comments on specific code lines, approve with a required review note, or reject with a reason — all workflow state stored in Postgres with a full audit trail and email notifications via Resend at each state transition. Approved plugins appear in an internal app store at plugins.yourcompany.com where designers browse by category (UI kits, accessibility tools, prototyping helpers), install with one click using a Figma install link generated via the Figma REST API's private plugins endpoint, and leave 5-star ratings with short reviews. Track install counts, active users per plugin via usage events from a lightweight analytics ping in each plugin's code, and version adoption rate — auto-notify plugin authors in Slack when adoption of a new version stalls below 50% after two weeks, suggesting they send a migration guide. Send a weekly digest to the design organization channel with newly released plugins, top-rated tools this week, and plugins approaching deprecation.", stack: ["Next.js", "Figma REST", "Okta"], difficulty: "Advanced", vibes: ["Design Ops", "Enterprise"] },
  { title: "AffiliateAtlas", tagline: "Map every affiliate dollar across creators.", description: "Brands see attribution per creator across UTMs, codes, and link aliases in one chart.", prompt: "Build a Next.js affiliate analytics platform that authenticates with Impact Radius, ShareASale, and CJ Affiliate networks via their REST APIs, fetching all active affiliate links, click data, conversion events, and commission payouts into a unified Postgres schema with an affiliate_events table keyed by publisher_id, click_id, and conversion_id to enable cross-network deduplication. For direct affiliate codes not tracked through a network, ingest UTM-tagged conversion events from the storefront via a lightweight server-side tracking pixel that fires a POST to an API route and logs to the same events table. Compute per-creator LTV: sum commissions over a configurable lookback window, segment creators by performance tier (micro, mid, top), and identify seasonal uplift by comparing promo-period conversion rates stored in ClickHouse against a 90-day rolling baseline. Build a visual attribution dashboard using Recharts showing revenue by creator with sparklines, commission rate vs. revenue efficiency scatter plot, promotional cycle analysis with weekly cohort comparison, and a creator leaderboard with 90-day trend indicators. Export per-creator performance reports as styled PDFs for quarterly business reviews and send automated Slack alerts when a top-tier creator's conversion rate drops more than 20% week-over-week.", stack: ["Next.js", "Impact API", "ClickHouse"], difficulty: "Advanced", vibes: ["Marketing", "Attribution"] },
  { title: "RoadmapRoulette", tagline: "Public roadmaps that customers can vote on.", description: "Beautiful roadmap pages with weighted voting and changelog auto-publishing.", prompt: "Build a Next.js public roadmap SaaS where each item has a status (idea, planned, in-progress, shipped), a description, and a category, all stored in Postgres with a full revision history. Implement weighted voting where paid customers' votes carry a configurable multiplier (3x enterprise, 2x pro, 1x free) resolved by looking up each voter's email against the company's Stripe customer records via the Stripe API — users authenticate via magic link and their tier is resolved automatically on each vote. Build a beautiful public roadmap page at roadmap.yourproduct.com with kanban-style status columns, sortable by weighted vote count or date added, with threaded comment sections per item where customers can share context and upvote comments. When an item status changes to 'shipped', trigger an auto-publish flow: generate a changelog entry (optionally AI-written by Claude from the item's description and top customer comments), and send a Resend email to all voters who upvoted that specific item, linking directly to the changelog entry. Provide a PM admin dashboard with a weighted prioritization matrix plotting customer demand score against estimated engineering effort, the full voter list per item (useful for customer discovery calls), and a changelog management UI for curating shipped item descriptions before publishing.", stack: ["Next.js", "Stripe", "Postgres"], difficulty: "Intermediate", vibes: ["PM", "Customer"] },
  { title: "SignalScout", tagline: "Find buying intent on Reddit and HN.", description: "Monitors threads for prospects who match your ICP and writes draft replies.", prompt: "Build a Next.js sales-intelligence tool where SDRs define an Ideal Customer Profile in plain English — 'B2B SaaS companies with 50-500 employees struggling with manual data pipelines' — and configure 10-20 subreddits and Hacker News search queries (via the Reddit API's r/subreddit/new endpoint and the Algolia HN Search API) to monitor continuously. Every 4 hours, fetch new posts and comments from all configured sources, embed each using OpenAI text-embedding-3-small, and compute cosine similarity against the ICP embedding stored in pgvector to score each post's relevance on a 0-1 scale. For posts above a 0.75 relevance threshold, pass the full post content and ICP definition to Claude to determine: is the poster likely the target buyer persona, what is their specific pain point, and is the timing right for outreach — then generate a value-first reply draft that leads with a helpful insight and mentions the product naturally as a potential solution without being salesy. Present an SDR inbox UI showing top intent signals ranked by recency and relevance score, with the post summary, buyer analysis, pain point diagnosis, and pre-drafted reply all in a single panel — the SDR can edit and submit the reply directly via the Reddit OAuth API or export to their CRM. Track outreach outcomes (replied, ignored, converted) in Postgres and use the signal as a feedback loop to refine ICP definition accuracy over time.", stack: ["Next.js", "Reddit API", "OpenAI"], difficulty: "Advanced", vibes: ["Sales", "Intent"] },
  { title: "FormFire", tagline: "Public sector forms that don't suck.", description: "City clerks build accessible, multi-language forms via drag-and-drop.", prompt: "Build a Next.js form builder specialized for government and public sector use cases with a drag-and-drop component library of WCAG 2.1 AA-compliant form elements — text inputs with correct contrast ratios, properly labeled checkboxes and radio groups, accessible date pickers, and screen-reader-compatible error messages with aria-describedby wiring. Support automatic translation of all form labels, help text, error messages, and option labels into 50+ languages via the DeepL API — translations are stored per-language in a translations table in Postgres, reviewed by a human before publishing, and served based on the browser's Accept-Language header with a manual language switcher. Implement multi-step form flows with conditional logic (show field B if field A equals 'yes'), a visual step progress indicator, and save-and-resume via a secure token emailed to the respondent using Resend so they can complete the form across multiple sessions. On submission, generate a confirmation PDF using PDFKit formatted as an official-looking document with the respondent's answers, submission timestamp, form version, and a unique submission reference number, then export all responses as a CSV for the city clerk's records. Add a form analytics dashboard showing completion rates, average time-to-complete, drop-off rates per step, and the top submitted values per question to help clerks identify confusing field labels.", stack: ["Next.js", "DeepL", "PDFKit"], difficulty: "Intermediate", vibes: ["GovTech", "Accessibility"] },
  { title: "VendorVerse", tagline: "Procurement workflows for early-stage CFOs.", description: "Centralizes vendor approval, MSA storage, and renewal alerts.", prompt: "Build a Next.js procurement SaaS for startup CFOs where each vendor has a profile — name, category, contract value, renewal date, primary contact, payment terms — stored in Postgres, and teams can upload MSA PDFs that are processed by Claude to extract and surface key clauses: auto-renewal terms, data processing addendum presence, limitation of liability caps, termination provisions, and payment schedule. Implement a multi-stage approval workflow where purchase requests above configurable thresholds route through a chain of approvers (manager → CFO → board above $100K), with email notifications via Resend at each step, a Slack bot command for quick approvals, and a full decision audit trail in Postgres. Build a renewal calendar view showing all upcoming contract renewals color-coded by lead time (red = under 30 days, amber = under 90 days, green = over 90 days), with automatic reminder emails sent 90, 30, and 7 days before each renewal date. Add a vendor risk dashboard showing which vendors have access to sensitive data, which are missing signed DPAs, and which contracts have been flagged by Claude for missing standard legal clauses during MSA ingestion. Export a vendor master sheet as a formatted CSV for quarterly CFO review and board audit preparation.", stack: ["Next.js", "Postgres", "Claude"], difficulty: "Intermediate", vibes: ["Finance Ops", "Compliance"] },
  { title: "Persona Pulse", tagline: "User-research repo with semantic search.", description: "All your interview transcripts queryable like a search engine.", prompt: "Build a Next.js UX research repository where teams upload interview transcripts (TXT, DOCX, or audio/video processed via Whisper on Replicate), split into individual speaker turns, and embed each turn using OpenAI text-embedding-3-small stored in pgvector with metadata: participant ID, study name, date, researcher, and interview guide question that prompted the response. Build a semantic search interface where researchers type queries like 'frustrations with the onboarding flow' and retrieve the 10-15 most relevant quotes ranked by cosine similarity, displayed with full participant context, a link to the source transcript, and a timestamp deep-link if the source was audio/video. Support synthesis queries — 'What are the top 3 themes around pricing objections?' — that retrieve the top 30 relevant quotes and pass them to Claude, which returns a structured synthesis with key themes, supporting quotes (at least 3 per theme), and participant count per theme as a ready-to-paste research artifact. Let researchers tag and organize findings into insight cards with quotes, implications, and design recommendations, then auto-generate a research report outline from a collection of cards using a Claude-powered template. Integrate with Figma via the Figma plugin API so researchers can drag insight cards directly into design files as annotated sticky notes.", stack: ["Next.js", "pgvector", "Claude"], difficulty: "Advanced", vibes: ["UX Research", "AI"] },
  { title: "ChartChaser", tagline: "Embed dashboards in Notion with live data.", description: "Paste a SQL query, get a live chart block in any Notion page.", prompt: "Build a Next.js integration where users connect their Postgres, MySQL, or BigQuery database (connection strings stored encrypted with libsodium in Postgres), write a SQL query in a Monaco editor, choose a chart type (bar, line, pie, scatter, table), and configure axis labels, colors, and a refresh interval. Store each chart configuration as a record in Postgres with a chart_id and expose a public chart proxy endpoint at /c/{chart_id} that runs the stored SQL against the configured database, transforms the result into a Chart.js config, renders the chart server-side as a PNG using a headless rendering approach, and returns the image with Cache-Control headers for the configured refresh interval. Provide a Notion integration via a simple Notion database where users paste the proxy URL as an image block — the live-updating PNG renders natively inside Notion without any custom integration needed. Add a Recharts-based interactive chart preview in the configuration UI with a live query runner that shows row count, execution time, and a data table below the chart so users can validate the query before publishing. Include a query history sidebar, saved chart templates, and a shareable chart link with an optional password for external sharing with clients.", stack: ["Next.js", "Notion API", "Recharts"], difficulty: "Intermediate", vibes: ["Analytics", "Integrations"] },
  { title: "PolicyPilot", tagline: "Generate SOC 2-ready policies from a few prompts.", description: "Stay audit-ready without copy-pasting templates.", prompt: "Build a Next.js SaaS that walks startup founders through a 10-minute structured interview via a conversational UI, asking about their tech stack and cloud providers, types of data processed (PII, PHI, financial), number of employees and contractors, existing security controls (MFA enforcement, SSO, endpoint management), and compliance goals (SOC 2 Type I, SOC 2 Type II, ISO 27001, HIPAA). Store all interview responses in a Postgres session table and pass the complete profile to Claude with a detailed prompt that generates a full set of tailored information security policies — Access Control Policy, Incident Response Plan, Business Continuity Plan, Vendor Risk Management Policy, and Asset Inventory Policy — each customized to the company's specific stack and controls rather than being generic boilerplate. Render each generated policy in a TipTap markdown editor where founders can refine specific sections, then export as a professionally formatted PDF using PDFKit with the company name, date, version number, and a table of contents. For each policy, surface 3-5 specific technical controls the company should implement (e.g., 'Enable MFA on all AWS IAM users', 'Configure CloudTrail logging to S3 with 1-year retention') as a prioritized checklist linked to relevant AWS/GCP/Azure documentation. Add a compliance readiness score dashboard showing which policies are drafted, which controls are verified, estimated time to audit readiness, and a shareable compliance report link for auditors.", stack: ["Next.js", "Claude", "PDFKit"], difficulty: "Intermediate", vibes: ["Compliance", "Startups"] },
  { title: "FundFlow", tagline: "LP reports that write themselves.", description: "Emerging fund managers generate polished quarterly LP reports from portfolio data and meeting notes in minutes, not days.", prompt: "Build a Next.js SaaS for emerging VC and angel fund managers where managers maintain a portfolio database in Postgres — each company record stores name, investment date, round, check size, ownership percentage, post-money valuation, sector, stage, and lead investor. Let managers import or manually enter quarterly KPIs per portfolio company (ARR, MRR, headcount, cash runway, last financing date, notable milestones) through a structured data-entry form with inline validation, storing a time-series of KPI snapshots so managers can track progress across quarters. Each quarter, prompt managers to add brief deal notes per company — highlights since last quarter, key risks, valuation mark update rationale, and follow-on recommendation — through a guided form that ensures consistent coverage across the portfolio. Pass the complete portfolio snapshot, KPI time-series, and deal notes to Claude with a structured LP report generation prompt producing: a one-page executive summary covering fund performance metrics, a per-company narrative section with standardized formatting (3-5 sentences each), a portfolio performance table computing total invested capital, current NAV, gross MOIC, and net IRR using a TVPI/DPI/RVPI breakdown, and a two-paragraph market commentary section on the fund's thesis sectors. Render the assembled report as a professionally formatted PDF using PDFKit with the fund's uploaded logo, configurable brand colors, page headers, a table of contents with page numbers, and footnotes for valuation methodology — ready to send to LPs without any manual editing. Let managers maintain an LP contacts list in Postgres with name, entity, commitment amount, and email; send the quarterly PDF report via Resend with a signed S3 URL link for download, tracking who opened the email and clicked the download link per distribution. Add a fund analytics dashboard showing total portfolio NAV over time as a line chart, a sector allocation donut chart, a stage distribution breakdown, top performers by MOIC, and a fund-level cash flow waterfall visualizing contributed capital versus distributions using Recharts. Support multiple funds per account so managers running a Fund I and Fund II can maintain separate portfolios, report templates, and LP lists under one login.", stack: ["Next.js", "Postgres", "Claude", "PDFKit"], difficulty: "Intermediate", vibes: ["VC", "Fintech", "Finance Ops"] },
];

const CREATIVE: Omit<Project, "id" | "category">[] = [
  { title: "Kaleidofy", tagline: "Drag any photo into a living kaleidoscope.", description: "WebGL kaleidoscope that pulses with the user's mic input — perfect for music visuals.", prompt: "Build a Three.js WebGL app where users drag and drop any image file onto the canvas — load it as a texture and render it into a kaleidoscope shader that mirrors and rotates N segments symmetrically (N configurable via a slider from 4 to 32). Connect a WebAudio AnalyserNode to the microphone via getUserMedia and compute FFT bins every animation frame, mapping bass frequencies (0-300Hz) to rotation speed, mids (300-3kHz) to symmetry count, and highs (3kHz+) to a chroma-shift post-processing pass using a custom GLSL fragment shader. Let users export a 10-second WebM recording of the live view using the MediaRecorder API capturing the canvas stream. Add a fullscreen mode, a screenshot button that downloads the current frame as PNG, and a shareable link that encodes the image (as a base64 URL param or via a short-lived S3 upload) and slider state in the URL for sharing specific looks. Optimize with requestAnimationFrame and offscreen canvas where available for smooth 60fps performance even on mobile.", stack: ["Three.js", "WebGL", "WebAudio"], difficulty: "Advanced", vibes: ["Visualizer", "Audio Reactive"] },
  { title: "Lyric Lava Lamp", tagline: "Spotify-synced ambient typography.", description: "Lyrics drift across a flowing fluid sim that responds to song tempo and mood.", prompt: "Build a React app that authenticates with Spotify via PKCE OAuth, polls the currently-playing endpoint every 2 seconds to get track ID and playback position, and fetches synchronized lyrics from a lyrics API (Musixmatch or lrclib.net) as a timestamped list. Write a 2D fluid simulation in a GLSL fragment shader using Jos Stam's stable fluids method, using the song's energy and valence values from the Spotify Audio Features endpoint to drive viscosity and turbulence — high energy = fast swirling, low valence = cool blue hues. On each lyric-line transition, spawn the new lyric string as canvas-drawn text that gets advected through the fluid field, fading out as the next line arrives, giving the effect of words drifting and dissolving in ink. Implement a color theme system that maps Spotify's audio feature axes (energy vs. valence) to a 2D color palette so every song has a unique ambient mood. Add a fullscreen mode, a screensaver toggle that dims after 30 seconds of no interaction, and a shareable card generator that captures the current frame as a PNG with the song title and artist.", stack: ["Spotify API", "GLSL", "React"], difficulty: "Advanced", vibes: ["Music", "Shaders"] },
  { title: "Doodlefy", tagline: "Hand-drawn sketches become 3D toys.", description: "Sketch on canvas, the AI lifts it into a textured 3D model you can spin.", prompt: "Build a Next.js app with a pressure-sensitive drawing canvas (using the Pointer Events API with pressure support via a canvas fallback) where users sketch a simple object with a felt-tip brush style, then click 'Make 3D' to submit the canvas PNG to the InstantMesh API on Replicate, which returns a textured GLB model. Load the GLB into a Three.js scene with an OrbitControls camera so users can spin, zoom, and inspect the model in 3D, and add a point light that follows the mouse for dramatic real-time shading. Let users apply a 'toy style' post-processing pass using Three.js OutlinePass and cel-shading shader to make the model look like a plastic toy fresh from the mold. Add a GLTF export button that downloads the model for use in Blender or a game engine, and a gallery page (persisted in localStorage or a simple Postgres table) showing the user's recent Doodlefy creations as rotating thumbnails. Show a fun loading animation with a spinning voxel while the Replicate job runs, polling the prediction status endpoint every 1.5 seconds.", stack: ["Canvas", "InstantMesh", "Three.js"], difficulty: "Advanced", vibes: ["AI", "3D"] },
  { title: "MoodMixer", tagline: "Spotify playlist generator from a vibe slider.", description: "Drag sliders for energy, melancholy, and whimsy — get a playlist back.", prompt: "Build a Next.js app that authenticates with Spotify via PKCE OAuth and presents a 2D mood grid — X axis is energy (calm to electric), Y axis is valence (melancholy to euphoric) — where users click or drag to set their desired vibe point. Map the X/Y coordinates to the corresponding Spotify Audio Features values (energy: 0.0-1.0, valence: 0.0-1.0) and call the Spotify Recommendations endpoint with 5 seed genres (derived from a genre dropdown) plus the target_energy and target_valence parameters to generate 20 tracks. Render the recommended tracks as a beautiful playlist UI with album art, track name, and a play preview button using the Spotify 30-second preview URL, with a BPM display showing the track's tempo. Let users one-click save the generated playlist to their Spotify library via the Create Playlist and Add Tracks endpoints, naming it automatically based on the mood quadrant (e.g., 'Calm & Euphoric Mix — Saturday vibes'). Add a history of past generated playlists stored in localStorage and a 'shuffle mood' button that randomly lands somewhere new on the grid.", stack: ["Spotify API", "Next.js"], difficulty: "Beginner", vibes: ["Music", "Mood"] },
  { title: "PixelPet", tagline: "Tamagotchi powered by your daily steps.", description: "Connects to Apple Health — pet thrives when you walk, sulks when you don't.", prompt: "Build a Next.js PWA that on iOS uses a HealthKit Web Bridge (or prompts users to use the iOS Shortcuts app to POST step data to a webhook) and on Android uses the Google Fit REST API via OAuth to sync the user's daily step count, storing it in a Postgres table with a date key. Compute a daily 'thrive score' on a 0-100 scale (10,000 steps = 100, fewer steps linearly scaled) and map that score to one of 8 pet mood states: sleeping, hungry, sad, bored, content, happy, excited, and dancing. Animate the pixel-art pet using Pixi.js with a sprite sheet containing animations for each mood state, transitioning smoothly with a Pixi AnimatedSprite, and add idle micro-animations (breathing, ear twitching) within each state. Implement pet hunger (decreases by 10 each day) that the user must feed by tapping a food button, and pet loneliness (increases if the user doesn't open the app for 2 days) that's cured by petting — a tap gesture on the sprite. Persist all pet state (mood, hunger, loneliness, name, cosmetic items) in Postgres so it survives reinstalls, and show a 30-day mood chart reflecting the user's step history alongside their pet's mood timeline.", stack: ["Next.js", "HealthKit", "Pixi.js"], difficulty: "Intermediate", vibes: ["Wellness", "Game"] },
  { title: "ColorWander", tagline: "Generative color-palette explorer.", description: "Palettes evolve via genetic algorithm based on your taste votes.", prompt: "Build a React app that generates an initial population of 20 color palettes (each with 5 HSL colors sampled from a perceptually uniform distribution) and presents them in a Pinterest-style grid where users vote thumbs-up or thumbs-down on each palette, with votes recorded as fitness scores. Implement a genetic algorithm that runs after every 20 votes: select the top 8 palettes by fitness, apply crossover (swap color slots between two parent palettes) and mutation (randomly shift H, S, or L of one color by ±15%) to produce 12 offspring, then merge offspring with the top 8 survivors to form the next generation of 20 palettes. Animate palette transitions using Framer Motion with a satisfying card-flip effect, and add a 'lock' button on any palette to preserve it across generations as a permanent anchor. Let users export any palette as CSS custom properties (--color-primary, etc.), a Tailwind config snippet, a Figma plugin import JSON, or a PNG swatch sheet. Add a shareable palette URL that encodes the 5 HSL values as a compact query string so users can send specific palettes to collaborators.", stack: ["React", "GA", "Framer Motion"], difficulty: "Intermediate", vibes: ["Design", "Generative"] },
  { title: "TypeTrip", tagline: "Type a sentence, watch it become a music video.", description: "Each word triggers an AI-generated visual scene cut to the rhythm of typing.", prompt: "Build a Next.js app where users type a sentence into a large, centered text input and each spacebar press (word completion) triggers an image generation request to Stable Diffusion XL on Replicate using the word plus surrounding context as the prompt, with a consistent style guide suffix (e.g., 'cinematic still, film grain, shallow DOF') applied to every image for visual coherence. As images complete (poll the Replicate prediction endpoint), display them in a storyboard strip below the input with a crossfade animation timed to the user's average typing cadence — fast typist = quick cuts, slow typist = long holds. Once the sentence is complete, stitch the images into a 10-second MP4 using FFmpeg.wasm on the client, with each image held for (10 / word_count) seconds and a dissolve transition between frames, plus the original sentence as subtitle text overlaid at the bottom in a cinematic font. Allow users to re-roll any individual frame by clicking it and typing a revised prompt for that word slot, then regenerate just that frame and re-export. Add a download button for the MP4 and a shareable link that stores the image URLs and timing in a short URL backed by a Postgres record.", stack: ["Next.js", "Replicate", "FFmpeg"], difficulty: "Advanced", vibes: ["AI Video", "Generative"] },
  { title: "EchoCanvas", tagline: "Voice notes painted onto an infinite canvas.", description: "Speak to leave colorful waveforms drifting across a shared whiteboard.", prompt: "Build a Next.js collaborative infinite canvas using Liveblocks for real-time state sync where each connected user is assigned a unique color; when a user holds a record button, the browser's MediaRecorder API captures mic audio in 5-second chunks and uploads each chunk to a server route that converts it to a waveform data array using the WebAudio API's OfflineAudioContext. Render each recorded voice note as a colored waveform stroke on the shared canvas — an SVG polyline drawn from the amplitude data — pinned to the canvas coordinates where the user was when they recorded it, with the user's name and timestamp shown on hover. Users can pan the infinite canvas by dragging (using a transform matrix applied to an SVG viewport), zoom with the scroll wheel, and click any waveform to play back the audio clip via an HTML5 Audio element loaded from the stored URL. Add a presence layer showing each connected user as a colored cursor dot that moves in real time via Liveblocks presence, with a sidebar showing all active users and a room code for joining. Store all waveform objects and their canvas coordinates in Liveblocks storage so new joiners see the full shared canvas immediately.", stack: ["Next.js", "WebRTC", "Liveblocks"], difficulty: "Advanced", vibes: ["Collab", "Audio"] },
  { title: "InfiniteZine", tagline: "Endless scrollable AI-generated zine.", description: "Pick a theme, scroll forever — every page is a freshly generated spread.", prompt: "Build a Next.js app with an infinite-scroll layout where each 'spread' is a viewport-height section consisting of a full-bleed AI-generated image on the left half and an AI-generated poem on the right half, both themed around the user's chosen topic (e.g., 'melancholy robots', 'underwater tea ceremonies'). Implement lazy generation: when the user is within 2 spreads of the bottom, trigger a parallel fetch — Stable Diffusion XL on Replicate for the image (using a style-consistent prompt suffix for visual coherence across spreads) and Claude for a 12-16 line poem in a specified form (haiku, free verse, villanelle, configurable). Cache already-generated spreads in a React ref so scrolling back up is instant, and persist the full session as a JSON array in localStorage so users can continue their zine after refreshing. Add a 'freeze' button that saves the current zine as a PDF using jsPDF with each spread as a page, suitable for digital publication or printing as an actual zine. Support 6 base themes selectable at the start, with an 'add your own' text input that seeds the generation prompts.", stack: ["Next.js", "Replicate", "OpenAI"], difficulty: "Intermediate", vibes: ["AI", "Editorial"] },
  { title: "GestureGarden", tagline: "Grow plants with your webcam dance moves.", description: "MediaPipe pose detection turns body movement into plant growth in a 3D garden.", prompt: "Build a Three.js app that loads MediaPipe Pose Landmarker (WASM model) in the browser and runs it on a live webcam feed at 30fps using requestAnimationFrame, extracting the 33 pose keypoints and computing joint angles for wrists, elbows, shoulders, and the overall body bounding box size. Map arm extension (angle between wrist-elbow-shoulder) to plant height in a 3D garden scene rendered in Three.js — fully extended arms = maximum height, arms at sides = minimum height — and map motion velocity (difference between consecutive keypoint positions) to flower color cycling through a palette using HSL interpolation. Represent plants as procedurally generated meshes using Three.js TubeGeometry for stems and ConeGeometry for flower heads, with new plants spawning when the user holds a specific pose (both arms raised) for 2 seconds, up to a maximum garden of 20 plants. Add a particle system for falling petals triggered when the user jumps (vertical torso displacement above threshold), and a day/night cycle ambient lighting that slowly shifts over the session. Export a timelapse GIF of the full session using a canvas capture and gif.js, saved to the user's device as a memory of their garden.", stack: ["MediaPipe", "Three.js"], difficulty: "Advanced", vibes: ["Movement", "AR"] },
  { title: "SoundDoodle", tagline: "Draw a line, hear it as melody.", description: "Each squiggle becomes a musical phrase via a custom mapping engine.", prompt: "Build a React canvas app where users draw freehand strokes with their mouse or finger — capture each stroke as an array of {x, y, pressure} points sampled at 60fps using the Pointer Events API, then immediately convert the stroke to a melody: X position maps to time within the phrase (left = beat 1, right = beat 4), Y position maps to pitch (top = high notes, bottom = low notes) quantized to the user's chosen musical scale (pentatonic, blues, major, minor), and stylus pressure (or simulated via stroke velocity) maps to MIDI velocity. Play the resulting melody back in real time using Tone.js Synth with the user's chosen instrument timbre (piano, marimba, bell, pad), with the stroke highlighted on the canvas in sync with the note being played. Let users layer up to 8 strokes on the same canvas — each in a different color assigned from a palette — creating a chord progression or counter-melody, with playback looping all layers simultaneously. Export the composition as a MIDI file using a MIDI generation library and as a canvas PNG showing the color-coded strokes, and add a 'randomize scale' button that reharmonizes all existing strokes into a new key without redrawing.", stack: ["Tone.js", "Canvas"], difficulty: "Intermediate", vibes: ["Music", "Sketch"] },
  { title: "ChromaCipher", tagline: "Encrypt messages as abstract art.", description: "Each letter becomes a colored geometric shape — only your friend with the key can decode.", prompt: "Build a client-side-only React app that encodes text messages into abstract visual art using a deterministic shape grammar: each ASCII character maps to a unique combination of shape (circle, triangle, square, hexagon, star), hue (based on character code mod 360), size (based on character frequency in common English), and rotation angle (character code × 7 degrees) — all determined by a user-supplied seed key that offsets every mapping. Render the encoded art on an HTML5 canvas using the 2D context API, arranging shapes in a grid layout with configurable padding and background color, creating output that looks like intentional abstract art rather than obviously encoded data. Implement the decoder: the receiver opens the app, enters the same seed key, uploads or pastes the encoded image, and a client-side pixel-reading algorithm extracts the shape parameters from the image to reconstruct the original text. Add a 'generate key' button that creates a memorable 4-word passphrase using a local word list, a copy-to-clipboard button for both the image and the key, and an export as PNG with configurable resolution (800px, 2000px, 4000px for print). Include an animation mode where shapes float onto the canvas one by one as a reveal, shareable as a GIF via gif.js.", stack: ["Canvas", "React"], difficulty: "Beginner", vibes: ["Privacy", "Art"] },
  { title: "DreamLoom", tagline: "Knit your dream journal into image collages.", description: "Each morning's entry becomes a generative tapestry you can share.", prompt: "Build a Next.js dream journal PWA where users write a morning entry in a distraction-free text editor, then click 'Weave Dream' to trigger a two-step AI pipeline: first, Claude extracts all nouns, adjectives, and emotional tones from the entry as a structured JSON list; second, those extracted words are used as seeds to generate 4-6 Stable Diffusion XL images on Replicate using varied style prompts (watercolor, oil painting, pencil sketch, surrealist collage) to create visual diversity within a single dream. Arrange the generated images into a collage layout using a CSS masonry grid algorithm that randomly varies image sizes and overlaps them slightly with soft drop shadows, creating a tapestry-like patchwork effect. Persist all dream entries and their associated image URLs in a Postgres database (or SQLite via Turso) with a calendar view showing which days have entries, and let users browse their dream history as a scrollable gallery. Add a 'Full Moon Report' feature that runs on the last day of each month: Claude synthesizes all the month's extracted nouns and emotions into a thematic reading of the user's dream patterns, delivered as a beautifully formatted HTML email via Resend.", stack: ["Next.js", "Replicate", "OpenAI"], difficulty: "Intermediate", vibes: ["Wellness", "Generative"] },
  { title: "MotionMosaic", tagline: "Turn videos into living mosaics.", description: "Each tile is a tiny video looping in sync with the bigger picture.", prompt: "Build a Next.js app where users upload a target image and a folder of short video clips (5-30 seconds each); the server processes the target image by dividing it into a grid of N×M tiles (grid size configurable) and computes the average color of each tile. For each tile position, find the video clip from the corpus whose average frame color best matches the tile's target color using a k-d tree for fast nearest-neighbor lookup in RGB color space, then assign that clip to the tile. Render the animated mosaic in a WebGL canvas using Three.js where each tile is a plane mesh with a VideoTexture sourced from an HTML5 video element playing the assigned clip in a loop, with all clips synchronized to start simultaneously. Implement a 'zoom out' reveal animation that starts with a close-up of individual clips and slowly zooms out to reveal the full mosaic image emerging from the pattern of videos, triggerable with a button or on a timer. Export a static PNG screenshot of the mosaic's best frame using the canvas.toDataURL method, and let users adjust grid density (coarser or finer tiles) and re-run the tile-matching with a new grid interactively.", stack: ["WebGL", "FFmpeg.wasm"], difficulty: "Advanced", vibes: ["Video", "Generative"] },
  { title: "Glitchify", tagline: "One-click datamosh for your TikToks.", description: "Upload a clip, get five glitch presets ranging from VHS to broken JPEG.", prompt: "Build a Next.js app that accepts video uploads up to 100MB and processes them entirely in the browser using FFmpeg.wasm, applying 5 distinct glitch effect presets: VHS (horizontal noise lines, color bleeding, tape wobble via chromatic aberration), datamosh (remove I-frames to create smearing motion between cuts), broken JPEG (random DCT block corruption via bit manipulation), scanlines (dark horizontal lines overlaid with configurable spacing), and RGB split (chroma key offset by N pixels on each channel). Process each preset in a Web Worker using FFmpeg.wasm so the main thread stays responsive, showing a progress bar for each preset as it renders. Display all 5 glitched previews in a comparison grid where users click any variant to see it full-size in a lightbox, and provide a download button for each variant as an MP4. Add a custom preset editor with sliders for each parameter (noise intensity, I-frame skip rate, JPEG quality corruption level, scanline density, RGB offset amount) so users can dial in their exact aesthetic. Support both drag-and-drop upload and a mobile camera capture button for filming directly from the phone.", stack: ["FFmpeg.wasm", "Next.js"], difficulty: "Intermediate", vibes: ["Video FX"] },
  { title: "FontForage", tagline: "Discover fonts that feel like a vibe word.", description: "Type 'cozy bakery' and get matched fonts via embedding similarity.", prompt: "Build a Next.js font discovery tool that pre-processes the entire Google Fonts catalog (1,500+ fonts): for each font, generate a sample render of the alphabet and common words at various sizes using a headless browser screenshot or canvas rendering, then embed the font's metadata (name, category, tags, designer notes, sample text render as a description) using OpenAI text-embedding-3-small, storing all embeddings in pgvector. When a user types a vibe phrase like 'vintage travel poster' or 'tech startup friendly', embed the query and retrieve the 12 most similar fonts from pgvector, then render each match in a live preview panel where users can type their own custom phrase and see it in each font instantly via Google Fonts API CSS loading. Let users filter by category (serif, sans-serif, display, handwriting, monospace), weight availability (does it have a bold?), and license (OFL only), and add a side-by-side comparison mode for up to 4 fonts simultaneously. Add a 'pair me' feature where selecting one font suggests the top 3 complementary pairing fonts (body + heading combos) using a second embedding query biased toward contrast. Export any discovered font pairing as a ready-to-use CSS @import snippet, a Figma Styles JSON, and a Tailwind fontFamily config block.", stack: ["Next.js", "Embeddings"], difficulty: "Intermediate", vibes: ["Design", "Search"] },
  { title: "AuraPalette", tagline: "Get your aura color from a selfie.", description: "Webcam scan, photogenic loading bar, and a personalized aura palette to share.", prompt: "Build a Next.js PWA that accesses the user's webcam via getUserMedia, captures a selfie on button press, and runs it through TensorFlow.js face-detection model to locate the face bounding box — then samples 50 random pixels from the skin and eye regions to extract dominant colors using a k-means clustering algorithm (k=3) running in a Web Worker. Pass the 3 dominant colors as hex values plus a random 'cosmic influence' seed to Claude with a playful astrology-adjacent prompt that generates: a unique aura name ('Celestial Amber Dreamer'), a 3-sentence personality interpretation, and 3 complementary accent colors to complete the palette. Render the result as a beautiful shareable card: a gradient background built from the extracted colors, the aura name in a display font, the personality text, and a 5-swatch color palette strip with hex codes, all laid out on a canvas element. Allow one-tap export as a PNG card optimized for Instagram Stories (1080×1920) and as a square post (1080×1080), and add a 'try a different expression' button that re-captures and re-analyzes for a slightly different result. Persist the generated aura in localStorage so returning visitors see their last reading and can compare it to a new one.", stack: ["TensorFlow.js", "OpenAI"], difficulty: "Beginner", vibes: ["Toy", "Selfie"] },
  { title: "Rhythmville", tagline: "Build a city where every building is a sequencer.", description: "Drag buildings around — each emits a synth pattern; the city grooves.", prompt: "Build a Three.js isometric city builder where users place building tiles on a grid — each building type (skyscraper, shop, park, factory, stadium) has a unique step-sequencer pattern and synthesizer timbre defined in Tone.js, with the sequencer grid configurable in a sidebar panel that appears when you click any building. Implement a global city BPM (set via a tempo slider from 60-180 BPM) that drives a master Tone.Transport clock, with all buildings' sequencers synchronized to the same clock so the city always grooves together. As users place buildings closer together, blend their audio levels using a distance-based spatial panner (Tone.Panner3D) so the camera position in the 3D scene acts as the listener's ears — walking the camera left increases the mix of buildings on the left. Add 4 unlockable district themes (Downtown Jazz, Industrial Techno, Suburban Lofi, Festival EDM) that restyle all buildings' synthesizer presets simultaneously for a dramatic mood shift. Export the current city's sequencer state as a MIDI file with one track per building type, and as a 30-second MP3 mix recorded via Tone.Offline rendering.", stack: ["Three.js", "Tone.js"], difficulty: "Advanced", vibes: ["Music", "City"] },
  { title: "SketchScript", tagline: "Storyboard like a pro using stick figures + AI.", description: "Hand-drawn frames upgrade into rendered scenes via image-to-image.", prompt: "Build a Next.js storyboard app with a multi-panel canvas interface (3×2 grid of panels, each panel a 16:9 drawing canvas) where users sketch rough stick-figure compositions using a minimal brush tool with adjustable weight — drawing with a mouse or Apple Pencil via Pointer Events pressure API. When the user clicks 'Render Scene' on any panel, upload the canvas PNG to a ControlNet pipeline on Replicate using the scribble ControlNet model with the user's chosen art style (photorealistic, anime, oil painting, concept art) as the prompt suffix — the ControlNet preserves the composition while upgrading the rendering quality dramatically. Show a before/after toggle on each panel that flips between the user's sketch and the AI-rendered version with a smooth CSS transition, and let users iterate by painting over the rendered result to refine composition and re-rendering. Add a panel-level prompt input where users describe the scene ('rainy alley, neon lights, 1950s Tokyo') to guide the render without overriding the compositional sketch. Export the full 6-panel storyboard as a single PDF with a film-style layout, panel numbers, and a notes field below each panel.", stack: ["Replicate", "Controlnet"], difficulty: "Advanced", vibes: ["Film", "AI"] },
  { title: "PoemPress", tagline: "Press a key, write a poem in your favorite poet's style.", description: "Choose a poet, get a stanza generated in their cadence with explainable scansion.", prompt: "Build a Next.js poetry generator with a dropdown of 20 famous poets (Shakespeare, Dickinson, Neruda, Rumi, Plath, Whitman, etc.), each with a carefully crafted system prompt that captures their characteristic forms, vocabulary, imagery, and emotional register — stored as a poets.ts config file with name, bio snippet, and system prompt. When the user selects a poet and optional theme (love, mortality, nature, revolution) and clicks Generate, call Claude with the poet-specific system prompt and produce a 3-4 stanza poem, then run a separate scansion analysis call that identifies the meter pattern (iambic pentameter, trochaic tetrameter, free verse) and marks each syllable as stressed (/) or unstressed (u) in a visual overlay beneath the poem text. Display the poem in a beautifully typeset layout using a Google Font appropriate for the poet's era, with the scansion marks shown as a toggleable overlay, a rhyme scheme indicator (ABAB, ABBA, free) on the right margin, and an inline 'explain this line' button that opens a tooltip from Claude explaining a specific imagery choice. Add a 'continue the poem' button that feeds the generated stanza back as context for Claude to write the next stanza in the same poet's style, and an export button that saves the full poem as a printable PDF poetry broadside.", stack: ["Next.js", "OpenAI"], difficulty: "Beginner", vibes: ["Writing", "AI"] },
  { title: "VinylVisuals", tagline: "Spotify-driven album-cover generator.", description: "Listening party? The screen displays evolving cover-art for the song playing.", prompt: "Build a React ambient visualizer that authenticates with Spotify via PKCE OAuth and polls the currently-playing endpoint every 3 seconds — when a new track starts, extract the song title, artist, genre tags, and the 5 audio features (energy, valence, danceability, acousticness, instrumentalness) from the Spotify Audio Features endpoint. Construct a rich image prompt from this metadata — 'high energy electronic, dark valence: glitching chrome city at midnight, neon reflections, abstract data streams' — and submit it to Stable Diffusion XL on Replicate with a fixed seed derived from the track ID so the same song always produces the same art. While the image generates (polled via Replicate's prediction status endpoint), display a generative CSS animation placeholder with hue and motion characteristics driven by the audio features, then crossfade the generated image in once it's ready using a CSS opacity transition. Overlay the current track title and artist in the bottom-left corner in a minimal sans-serif, and add a click-to-fullscreen mode that hides all UI for a pure ambient experience. Cache generated images by track_id in localStorage so repeat plays skip the Replicate call and display instantly.", stack: ["Spotify API", "Replicate"], difficulty: "Intermediate", vibes: ["Music", "AI"] },
  { title: "InkInked", tagline: "Tattoo design playground.", description: "Iterate on a tattoo concept with reference images, mirroring, and skin previews.", prompt: "Build a Next.js tattoo design studio where users type a concept prompt ('geometric wolf, fine line, stippling style, minimalist') and generate tattoo design concepts via Stable Diffusion on Replicate with a negative prompt that removes color and background, producing clean black-and-white linework designs. Display generated designs in a light table UI where users can flip horizontally (tattoo artists mirror designs to trace on skin), rotate, scale, and place the design on a body diagram showing common tattoo placement locations (inner arm, shoulder, back, ankle). Implement inpainting for skin preview: users upload a photo of their skin area, the app overlays the tattoo design at the placed position using canvas compositing with a multiply blend mode to simulate ink on skin, adjustable for opacity and slight distortion to follow skin curvature. Add iterative refinement: users can sketch corrections directly on the generated design (using a canvas overlay with an eraser and pen) and re-submit with 'refine this design' triggering an img2img ControlNet pass that preserves the edited sketch while improving linework quality. Save all generated designs to a personal design vault in localStorage or Postgres, organized by body placement with a comparison grid of all iterations per concept.", stack: ["Replicate", "Canvas"], difficulty: "Intermediate", vibes: ["AI", "Body Art"] },
  { title: "EchoRoom", tagline: "Spatial-audio voice chat with friends.", description: "Walk around an isometric room — voices fade with distance.", prompt: "Build a Next.js real-time spatial audio chat app where users enter a named room and see a top-down 2D canvas room rendered with Pixi.js — each user is an avatar (small circle with username label) that can be dragged to any position in the room. Establish WebRTC peer connections using a lightweight SFU approach (or PeerJS for simplicity) to stream mic audio between all participants, and create a WebAudio PannerNode for each remote peer's audio stream, updating the panning and gain in real time based on the pixel distance and angle between the local user's avatar position and each remote peer's position. Sync all avatar positions in real time via Liveblocks presence so all participants see each other moving live; when a user drags their avatar, emit the new x/y coordinates via the Liveblocks presence channel which all clients consume to update the PannerNode values for that peer. Add persistent room objects: a fireplace (everyone within 50px hears crackling ambient audio), a music corner (plays a shared lo-fi YouTube embed for anyone nearby), and a 'quiet zone' that automatically reduces all incoming audio volumes to a whisper. Implement presence indicators showing which users are speaking (via a WebAudio AnalyserNode monitoring microphone level) as a pulsing ring around their avatar.", stack: ["WebRTC", "Liveblocks"], difficulty: "Advanced", vibes: ["Voice", "Spatial"] },
  { title: "BrushBot", tagline: "Co-painting with a generative AI brush.", description: "Every stroke is completed in real time by a model trained on classical painters.", prompt: "Build a Next.js collaborative painting canvas where each user stroke is rendered locally using Canvas 2D API with a bristle brush simulation (multiple parallel lines with slight random offset per point to simulate brush hairs), then the stroke's bounding-box region is immediately uploaded to a streaming image-completion model on Replicate (e.g., a fine-tuned inpainting model). Stream the completion result back via Replicate's streaming API and composite it onto the canvas beside the user's stroke using a CSS mix-blend-mode of 'multiply' so the AI continuation blends naturally with existing paint. Let users select a 'painter style' from a dropdown (Rembrandt, Monet, Van Gogh, Basquiat, Klee) which modifies the inpainting prompt suffix, and show the style name as a floating label while painting. Connect multiple users to the same canvas via Liveblocks storage: each completed stroke (both the user drawing and the AI continuation) is stored as a serialized path in Liveblocks storage so new joiners see the full painting history. Export the finished painting at 4000×3000px via canvas.toBlob with maximum quality JPEG encoding, and add a timelapse replay mode that re-draws all strokes in chronological order at 10× speed.", stack: ["Canvas", "Replicate"], difficulty: "Advanced", vibes: ["Art", "AI"] },
  { title: "MemeMint", tagline: "Generate templated memes from a tweet.", description: "Paste a tweet URL, get five meme adaptations on classic templates.", prompt: "Build a Next.js meme generator where users paste a tweet URL; the server fetches the tweet text via the Twitter API v2 or a scraping fallback, then embeds the tweet text using OpenAI text-embedding-3-small and queries a vector database of 50 classic meme templates (Drake, Distracted Boyfriend, This Is Fine, Change My Mind, etc.) each with a description and expected caption positions — returning the 5 most semantically appropriate templates for that tweet's content. For each matched template, call Claude with the tweet text and the template's expected caption format to generate fitting caption text that adapts the tweet's punchline to the meme format, then render the captioned meme server-side using Sharp to composite the caption text (Impact font, white with black stroke) onto the template image at the correct position. Display all 5 generated memes in a grid where users click to select their favorite, then copy to clipboard or download as PNG with one button. Add a 'randomize template' button for any panel, and a custom template upload flow where users provide their own image and define caption position boxes by clicking and dragging on the template preview. Include a shareable link for each generated meme set that encodes the tweet ID and chosen template IDs in a short URL backed by a Postgres record.", stack: ["Next.js", "Sharp", "OpenAI"], difficulty: "Beginner", vibes: ["Memes", "Social"] },
  { title: "AsciiArcade", tagline: "Webcam to ASCII video filter, retro-style.", description: "Real-time ASCII rendering with toggleable color, density, and CRT effect.", prompt: "Build a Next.js app that accesses the webcam via getUserMedia, draws each video frame onto a hidden canvas at reduced resolution (160×120), reads pixel data using getImageData, and maps each pixel's luminance value to a character from a density-ordered ASCII charset (from ' ' for light pixels to '@' for dark pixels) using a lookup table for performance. Render the ASCII output onto a visible canvas using the fillText method in a monospace font with adjustable font size (smaller = more detail, larger = chunkier retro look), painting each character in either grayscale (luminance-matched) or the original pixel color for a colorized ASCII mode. Add a CRT post-processing pass as a second canvas layer using a WebGL shader that applies scanline darkening (darken every other pixel row by 30%), barrel distortion (bulge the center slightly), and a faint phosphor glow bloom effect using a separable Gaussian blur. Let users toggle: black/white vs. color mode, charset density (basic ' .:-=+*#@%' vs. extended Braille), font size (8px to 24px), CRT effect on/off, and an invert mode. Add a photo mode button that captures the current ASCII frame as a PNG and a video mode that records a 10-second ASCII video using MediaRecorder capturing the canvas stream at 24fps.", stack: ["WebGL", "MediaDevices"], difficulty: "Intermediate", vibes: ["Retro", "Filter"] },
  { title: "LyricLoom", tagline: "Co-write song lyrics with rhyme-aware AI.", description: "Editor where the LLM stays out of your way but rhymes on demand.", prompt: "Build a Next.js lyrics editor using TipTap with a custom extension that adds a song-structure toolbar for inserting labeled section blocks (Verse, Pre-Chorus, Chorus, Bridge, Outro) — each block renders with a subtle left-border color and a collapse toggle so writers can focus on one section at a time. Implement an inline rhyme suggestion trigger: when the user types a word and presses Tab at line end, call an LLM API with the current line and the song's established rhyme scheme (auto-detected from previous lines) to suggest 5 rhyming completions ranked by naturalness, displayed as an inline autocomplete dropdown the user dismisses with Escape or confirms with Enter. Add a syllable counter that displays a live beat count per line in the right margin (using a phoneme-counting library or an LLM-powered counter for multi-syllable words), with a red indicator when a line's syllable count deviates more than ±2 from the section's established average. Include a section-template library (AABB rhyme scheme, ABAB alternating, free verse, 16-bar rap verse) that auto-formats a new section with the selected pattern and placeholder placeholder text. Export the finished lyrics as plain text, a properly formatted PDF with section labels, or a MIDI-ready lead sheet with melody notes entered via a simple piano roll that appears beneath each line.", stack: ["Next.js", "OpenAI"], difficulty: "Intermediate", vibes: ["Music", "Writing"] },
  { title: "PortalPaint", tagline: "Phone camera reveals AR paintings around you.", description: "Artists drop persistent AR pieces tied to GPS pins; viewers explore the city map.", prompt: "Build a Next.js PWA that uses WebXR's immersive-ar session (or a fallback using device camera + DeviceOrientationEvent) and the browser's Geolocation API to position 3D artwork objects in the real world anchored to GPS coordinates stored in a Postgres database with lat/lng, altitude, and a 3D model URL. Artists use a creation mode where they point their camera at a surface, tap to place an anchor, upload a GLB art asset (or generate one from a text prompt via a Shap-E Replicate endpoint), and publish it to the database with a title, description, and their profile. Viewers open the camera view and see all artworks within 200 meters rendered as Three.js meshes composited over the camera feed using CSS transforms computed from GPS distance and device orientation, with depth scaling making distant works appear smaller and closer works larger. Add a discovery map view (Mapbox GL) showing all artworks in the city as colored pins clustered by density, tappable to preview the artwork and get walking directions via Mapbox Directions API. Implement a community curation layer: artworks accumulate 'resonance' points when viewed and upvoted; works below a resonance threshold after 30 days are auto-archived, keeping the city canvas fresh.", stack: ["WebXR", "Mapbox"], difficulty: "Advanced", vibes: ["AR", "City"] },
  { title: "TattooType", tagline: "Calligraphy generator for tattoo lettering.", description: "Type words, choose a script style, swipe through 20 brush variations.", prompt: "Build a Next.js calligraphy lettering generator that loads a collection of OpenType variable fonts (or carefully selected calligraphic TTFs from Google Fonts) and renders user-typed text to an SVG canvas using opentype.js to access the raw glyph paths — enabling custom stroke width manipulation, tapering effects, and ligature control not possible through CSS alone. Implement 20 brush variation presets by programmatically adjusting the stroke offset along the glyph outline path (using normal vectors to the path) to simulate different nib angles and pressures — thin upstrokes, thick downstrokes — and render each as an SVG path with a configurable fill/stroke. Add sliders for letter-spacing (tracking), baseline shift for individual characters to simulate natural hand-lettering irregularities, and a flourish intensity slider that adds procedural swash strokes to capitals using cubic Bezier extensions from glyph terminal points. Export the design as a high-resolution SVG suitable for tattoo stencil printing, as a mirrored PDF (for tracing on skin), and as a PNG at configurable DPI (300 for print, 600 for professional use). Include a skin preview mode where users upload a photo and the SVG is composited at a chosen angle and size using CSS transform on a canvas overlay.", stack: ["OpenType.js", "SVG"], difficulty: "Intermediate", vibes: ["Lettering", "Niche"] },
  { title: "DungeonDoodler", tagline: "Hand-draw a TTRPG map, get tokens & lighting.", description: "Sketches gain a finished tabletop look with grid, fog of war, and dynamic lighting.", prompt: "Build a Next.js TTRPG map upgrader where dungeon masters photograph or upload a hand-drawn map — pass the image through a MediaPipe Image Segmentation model running in TensorFlow.js to detect walls (dark lines) versus open floor space, producing a binary wall map stored as a JSON grid. Render the detected wall map in Three.js as a 3D dungeon environment with extruded wall meshes and a textured stone floor, overlaying a configurable grid (5-foot squares with labels), and implementing a ray-casting light system where point lights placed by the DM cast real-time dynamic shadows using Three.js's shadow mapping. Add a fog of war layer: the entire map starts black, and as player tokens (draggable 3D cylinder meshes with character portraits textured on top) move across the map, reveal the area within their vision radius permanently. Generate matching token art for each character class (fighter, wizard, rogue, cleric) using Stable Diffusion on Replicate with a top-down miniature photography style, and let the DM upload custom token art for important NPCs. Export the full scene as a shareable room that players can join via a URL backed by Liveblocks, with the DM controlling fog-of-war reveals and all players seeing synchronized token positions.", stack: ["MediaPipe", "Three.js"], difficulty: "Advanced", vibes: ["TTRPG", "AI"] },
  { title: "PaperPlanetarium", tagline: "Print-friendly star charts for any night.", description: "Pick a date and place, download a poster of the stars at that moment.", prompt: "Build a Next.js star chart generator where users enter a date, time, and location (via a Mapbox geocoder or lat/lng inputs) — compute all visible stars, planets, and deep-sky objects for that sky moment using astronomy-engine (a JavaScript astronomy library) which handles sidereal time, altitude/azimuth coordinate transforms, and planetary positions via VSOP87 coefficients. Project the computed sky coordinates onto a circular stereographic sky chart (horizon = outer edge, zenith = center) using a gnomonic or stereographic projection implemented in plain JavaScript, then draw the chart as a scalable SVG: stars as circles scaled by magnitude (larger = brighter), constellation lines in a muted color, planet symbols at computed positions, and the Milky Way as a softly shaded band. Add user controls to toggle: constellation boundaries (IAU outlines), Messier objects with labels, moon position and phase (illustrated), cardinal directions around the chart edge, and a hemisphere selector (Northern/Southern). Generate constellation name labels in a vintage serif font and decorative border elements inspired by historical celestial maps (astrolabe-style) for a poster-quality aesthetic. Export as a high-resolution SVG (infinitely scalable for printing) and a 300dpi PNG at multiple poster sizes (A3, A2, 16×24 inches), with an optional custom title line ('The Night We Met').", stack: ["Astronomy.js", "SVG"], difficulty: "Intermediate", vibes: ["Astronomy", "Print"] },
  { title: "BeatBattles", tagline: "Step-sequencer 1v1 with public voting.", description: "Two producers, 60-second loops, audience votes the winner.", prompt: "Build a Next.js real-time music battle app where two producers are matched into a head-to-head room via a matchmaking queue (stored in Postgres with a status field), each getting a 16-step sequencer with 8 instrument tracks (kick, snare, hi-hat, bass, lead, pad, perc, FX) built with Tone.js, all synchronized to a shared 120 BPM master clock managed via a Liveblocks room event for perfect sync across clients. Give each producer 60 seconds to compose a loop — a countdown timer in the header shows remaining time — after which both loops are finalized and the match enters 'voting phase'. Broadcast both producers' finished loop recordings (rendered offline using Tone.Offline) to the room and to a public voting page accessible via a shareable URL, where the audience can listen to both loops toggled A/B style and cast a single vote; display a real-time vote count bar chart updating via Liveblocks. Implement an ELO rating system: the winner's rating increases by (400 - (winner_rating - loser_rating)) × 0.03, the loser's decreases symmetrically, and publish a global leaderboard sorted by ELO. Add a replay system: after voting closes, show the two sequencer grids side by side for spectators to study each producer's pattern choices.", stack: ["Tone.js", "Liveblocks"], difficulty: "Intermediate", vibes: ["Music", "Competition"] },
  { title: "FontFusion", tagline: "Blend two fonts into a brand-new one.", description: "Genetic-style interpolation between glyph shapes from two source fonts.", prompt: "Build a Next.js font interpolation tool using opentype.js to load two user-selected OTF or TTF font files, extract the glyph contour paths for the full ASCII character set from each font, and verify that matching glyphs have compatible contour structures (same number of contours and on-curve/off-curve points) — a prerequisite for interpolation. Implement contour-point linear interpolation: for each glyph, compute a weighted blend of the two fonts' point coordinates using a slider value from 0.0 (100% font A) to 1.0 (100% font B), producing intermediate glyph outlines that morph smoothly between the two typeface designs. Render the blended glyphs in real time as an SVG preview showing the full alphabet plus custom user-typed text, updating on every slider tick using requestAnimationFrame throttling for smooth performance. Implement smart contour reordering (rotate one font's point list to minimize total distance to the other font's points) to handle cases where the same glyph starts tracing from a different anchor point. Export the blended font as a valid OTF file using opentype.js's download method, with user-configurable font metadata (family name, style name, designer credit), making it immediately usable in Figma, CSS @font-face, or any desktop application.", stack: ["OpenType.js", "Wasm"], difficulty: "Advanced", vibes: ["Type Design"] },
  { title: "MovieMix", tagline: "Genre-blend trailers using AI.", description: "Drop two films, get a 30s trailer that blends their tones.", prompt: "Build a Next.js app where users select two films from a searchable database (TMDB API), and the system fetches available trailer URLs from the TMDB videos endpoint, then downloads them into a server-side temp directory using yt-dlp or direct CDN links. Run an AI mood analysis on each trailer: extract 10 evenly-spaced keyframes using FFmpeg, pass them to GPT-4o Vision to identify the emotional tone, color palette, and pacing of each film, then use Claude to write a 30-second script that blends both tones ('Action-Romance: starts with Film A's intensity, transitions to Film B's warmth at the 15-second mark'). Execute the script-driven edit using FFmpeg on the server: select and trim clips from both trailers based on mood tags, apply color grade LUT filters to blend the visual aesthetics (using FFmpeg's lut3d filter with a generated LUT that averages both films' color distributions), and add ElevenLabs TTS narration for the AI-written voiceover script. Composite everything with FFmpeg.wasm for a client-side preview, add a dramatic 'COMING SOON' title card at the end with the two film titles combined into a mashup title, and serve the final 30-second MP4 for download and social sharing.", stack: ["FFmpeg.wasm", "ElevenLabs"], difficulty: "Advanced", vibes: ["Video", "Mashup"] },
  { title: "WhimsyWeather", tagline: "Weather forecast as a stop-motion diorama.", description: "Each day's forecast is a tiny animated paper-craft scene.", prompt: "Build a Next.js weather app that fetches the 5-day forecast for the user's location (via browser Geolocation + Open-Meteo API, which is free) and renders each day's forecast not as numbers but as an animated 2.5D paper-craft diorama scene using Pixi.js. Build a library of layered SVG/PNG assets for weather states — sunny (paper sun with accordion-folded rays that spin), rainy (paper clouds with blue string-hung droplets that bounce), snowy (white paper snowflakes that drift), stormy (dark clouds with zig-zag lightning bolts that flash) — and compose each day's scene from the appropriate assets layered on a parallax depth stack (background, midground, foreground) that responds to mouse movement with subtle parallax. Animate all assets using CSS keyframe animations and Pixi.js tweens: the sun slowly rotates, raindrops fall at random intervals, snow accumulates at the bottom, and the background sky color transitions through a gradient matching the day's forecast conditions (clear blue, overcast gray, sunset orange). Display temperature and precipitation probability as hand-lettered SVG text labels attached to the scene elements (temperature on a tiny paper thermometer, wind speed as a vane spinning at the correct rate). Let users share any day's scene as an animated WebP or GIF captured via html2canvas plus gif.js, and add a 'make it a desktop wallpaper' export at 2560×1440px.", stack: ["Next.js", "Pixi.js"], difficulty: "Intermediate", vibes: ["Weather", "Whimsy"] },
  { title: "Tape Echo", tagline: "Browser-based looper pedal for live streamers.", description: "Loop, layer, and pitch-shift mic audio in real time.", prompt: "Build a Next.js app that accesses the microphone via getUserMedia and routes the audio stream through a WebAudio API processing chain: an AnalyserNode for level metering, a GainNode for input volume, a BiquadFilterNode for EQ (high-pass/low-pass), and a ScriptProcessorNode or AudioWorklet for the core looper functionality — recording a configurable loop length (1-8 bars at the current BPM) into a circular Float32Array buffer and playing it back simultaneously with new input. Implement multi-track layering: after recording the first loop, the looper automatically starts a second track while the first plays back in sync; support up to 6 simultaneous loop tracks each with independent volume, mute, and solo controls displayed as a mini mixer. Add real-time pitch shifting using a phase vocoder algorithm (or the Web Audio API's playbackRate on a BufferSource node for a lo-fi version), a delay/echo effect with configurable time and feedback, and a reverb effect using a ConvolverNode with a bundled impulse response. Map a MIDI controller to all loop controls (record, stop, mute, volume) using the WebMIDI API with a visual MIDI learn mode where users click any control and turn a physical knob to assign it. Implement audio export: when the user stops recording, mix all loop tracks offline using OfflineAudioContext and download the result as a WAV file.", stack: ["WebAudio", "WebMIDI"], difficulty: "Advanced", vibes: ["Music", "Live"] },
  { title: "PhotoAtlas", tagline: "Map your photo library to where you've been.", description: "Drop EXIF-stamped photos, see a heatmap of your travels with stories.", prompt: "Build a Next.js photo atlas app where users drag and drop a folder of JPEG photos — extract EXIF GPS coordinates and timestamp from each file using the exifr library (client-side, no upload needed), and plot each photo's location on a Mapbox GL map as a small thumbnail marker. Implement a DBSCAN clustering algorithm in a Web Worker that groups nearby photos (within 500 meters) into 'trips', identifying trip start and end dates and the geographic centroid of each cluster — display clusters as colored convex hull polygons on the map. When a user clicks any cluster, open a panel showing all photos from that trip in a lightbox gallery with a timeline, and trigger an LLM call (using the AI SDK with the photo locations, dates, and any metadata like camera model or resolution as context) that generates a suggested trip caption: '5 days in Kyoto — cherry blossoms, hidden temples, and late-night ramen'. Add a heatmap layer toggle that shows photo density across all locations using Mapbox's HeatmapLayer, revealing the user's life geography at a glance. Let users annotate any photo or cluster with a personal note, export their entire photo atlas as an interactive HTML file (self-contained with embedded thumbnails) or as a printable travel journal PDF organized chronologically by trip.", stack: ["Next.js", "Mapbox", "EXIF"], difficulty: "Intermediate", vibes: ["Travel", "Photos"] },
  { title: "PoeticPunch", tagline: "Convert any wiki article into a haiku trail.", description: "Read Wikipedia in 5-7-5, with a shareable haiku card per section.", prompt: "Build a Next.js app where users paste a Wikipedia article URL — fetch the article's content via the Wikipedia REST API (which returns clean parsed sections), split it into logical sections by the Wikipedia heading structure, and process each section through Claude with a strict constrained-output prompt: 'Convert this text to exactly one haiku. Count syllables using standard English phoneme rules. Output only the three lines, no explanation.' Use a syllable-counting validation step that calls a phoneme library to verify the 5-7-5 count before accepting the LLM's output, retrying with 'Try again — line 1 has 6 syllables, needs 5' feedback until the haiku passes validation. Render the haiku trail as a vertical scrolling experience where each section becomes a card with the haiku text in a large serif font, the section title as a small header, and a subtle background color that shifts along a gradient from card to card — creating a meditative, poetic reading experience. Add a swipe/arrow navigation mode for mobile where each haiku card fills the screen and users advance one by one through the article's story. Generate shareable individual haiku cards as PNG images (1200×630 for Twitter/OG cards) with the article title, haiku text, and a Wikipedia attribution using a server-side image generation endpoint.", stack: ["Next.js", "OpenAI"], difficulty: "Beginner", vibes: ["Reading", "Poetry"] },
  { title: "PaperToys", tagline: "Generate printable paper craft from any 3D model.", description: "Upload a GLB and get cuttable, foldable PDFs to assemble.", prompt: "Build a Next.js app that accepts GLB or OBJ file uploads, loads the 3D mesh in Three.js, and runs a planar mesh segmentation algorithm that groups faces by surface normal similarity (faces within 15 degrees of each other into one panel), then applies a UV-unfolding algorithm to lay each panel flat while recording the hinge relationships between adjacent panels. Render the unfolded net as an SVG with: solid black cut lines along the outer edges of each panel, dashed fold lines along internal edges (mountain vs. valley folds indicated by dash length), numbered flap tabs on edges that connect to other panels (matching numbers on both sides of a fold), and the original mesh's color or texture applied to each panel face using a UV-mapped fill. Let users adjust the net layout by dragging panel groups (with their connected subtrees) to reorganize for more compact or aesthetically pleasing arrangements, and preview the 3D assembly by animating the flat net folding up into the 3D shape using a Framer Motion choreographed fold sequence. Add difficulty-level controls: reduce polygon count via a Three.js SimplifyModifier slider so beginners get fewer panels to fold, and add optional score lines (lightly cut lines to guide folding) to the PDF. Export as an A4 or Letter-sized multi-page PDF using PDFKit, with a cover page showing the 3D preview render and assembly sequence diagrams.", stack: ["Three.js", "PDFKit"], difficulty: "Advanced", vibes: ["Crafts", "Print"] },
  { title: "SilentSymphony", tagline: "Compose music using only piano-roll gestures.", description: "Touch-friendly composing app where you sweep, tap, and squish notes alive.", prompt: "Build a Next.js touch-first piano roll composer using the Pointer Events API where notes are created by tap (creates a quarter note at the tapped pitch/position), drag horizontally (extends note duration), and drag vertically (pitch-bends the note using a smooth glide parameter). Implement multi-touch gesture macros: pinch on a region to quantize all notes to the nearest beat, two-finger swipe left/right to shift a selection by one beat, and a three-finger swipe down to delete selected notes — all without any UI buttons needed during composition. Build the playback engine with Tone.js using a PolySynth with multiple voice types selectable from a bottom drawer (warm piano, plucked guitar, soft strings, ambient pad, punchy keys), with each voice using a configurable ADSR envelope. Add a sample browser with 50 bundled one-shot samples (kick, snare, etc.) drag-able onto the piano roll as percussion tracks that sit above the pitch grid. Support MIDI import (parse .mid files using a MIDI parsing library and load notes into the piano roll) and MIDI export (serialize the current note grid to a valid .mid file) so the composition can continue in Ableton or GarageBand.", stack: ["Tone.js", "Touch"], difficulty: "Intermediate", vibes: ["Music", "Touch"] },
  { title: "RainbowReader", tagline: "Read with synesthetic color hints.", description: "Each phoneme maps to a soft color glow — for kids and dyslexia readers.", prompt: "Build a Next.js EPUB reader using EPUB.js to render book content in a comfortable reading layout, then run each loaded chapter's text through a phoneme-analysis pipeline: use a JavaScript phoneme dictionary (CMU Pronouncing Dictionary via a JS port, or a G2P model via Replicate) to split every word into its constituent phonemes (e.g., 'cat' → K AE T). Assign each phoneme a consistent hue from a carefully designed 40-color palette where similar phonemes have adjacent hues (all vowel sounds in warm hues, plosives in cool hues, sibilants in greens) — this palette is fixed so the mapping is the same across all books, building unconscious associations over time. Render the chapter text character by character in the DOM with each character wrapped in a span styled with a soft background-color glow (at 20% opacity to keep the text legible) matching its phoneme's assigned hue, so words become gentle multicolored strips of color. Add a settings panel for adjustable glow intensity (10%-50%), font size and family (dyslexia-friendly options like OpenDyslexic), line spacing, and a monochrome mode that shows phoneme boundaries as subtle underline patterns for users who prefer color-free reading. Include comprehension support: long-press any word to see its phoneme breakdown animated letter-by-letter with a narrator using the Web Speech API.", stack: ["EPUB.js", "Phonemizer"], difficulty: "Advanced", vibes: ["Accessibility", "Reading"] },
  { title: "MoodboardMixer", tagline: "Blend Pinterest boards into a unified style guide.", description: "Pull image URLs, auto-extract palettes, fonts, and motifs into a style sheet.", prompt: "Build a Next.js moodboard synthesis tool where users paste a list of image URLs (from Pinterest, Behance, or anywhere); the server fetches each image via a CORS proxy or server route, runs Sharp to extract a 5-color palette per image using k-means quantization, and passes each image to a CLIP embedding model on Replicate to generate a semantic vibe embedding ('minimalist', 'industrial', 'organic', 'maximalist') stored in pgvector. Aggregate the palettes across all images using a weighted k-means that produces a unified 6-color master palette (primary, secondary, accent, neutral-light, neutral-dark, text), and compute a consensus vibe by finding the centroid of all image CLIP embeddings and returning the 5 nearest vibe labels. Pass the aggregated visual data to Claude with a prompt describing the palette, consensus vibe, and top recurring visual motifs (extracted via GPT-4o Vision in a batch call) to generate a written brand style guide: voice/tone descriptors, suggested typeface pairings, photography guidelines, do/don't layout principles, and 3 suggested use cases for the aesthetic. Render the complete brand guide as a beautiful multi-page PDF using PDFKit: a cover page with the palette swatches, a typography page with the suggested fonts loaded and rendered, a visual guidelines page with the moodboard images arranged in a collage, and the written guidelines. Save each generated brand guide to a user account for future reference and iteration.", stack: ["CLIP", "Sharp", "PDFKit"], difficulty: "Advanced", vibes: ["Design", "Branding"] },
  { title: "GhostStudio", tagline: "Record a song with the ghost of yesterday's self.", description: "Sing today over yesterday's loop — the app fades old layers smartly.", prompt: "Build a Next.js PWA that opens a daily recording session via getUserMedia — the user records a 60-second vocal take, which is stored as a compressed WebM audio file in IndexedDB with the day's date as the key. Each new day, play back all previous recordings simultaneously using Web Audio API AudioBufferSource nodes, applying an automatic gain envelope that fades older layers: today's layer plays at 100% volume, yesterday's at 70%, the day before at 49%, and so on following a 0.7^(n-1) exponential decay, so older layers become ghostly and harmonic background. Use a pitch detection algorithm (YIN algorithm implemented in a Web Worker analyzing the microphone in real time) to display the user's sung pitch as a moving dot on a piano-roll grid overlay during recording, helping them stay in key with their past layers. After each recording session, automatically mix all layers using OfflineAudioContext with the fade envelope applied, and display a waveform visualization of the composite using WaveSurfer.js. On December 31st (or after 365 recordings), compile all daily recordings into a year-end collage: arrange them chronologically with crossfades between recordings using FFmpeg.wasm and export a full-year audio journey as an MP3 download.", stack: ["WebAudio", "IndexedDB"], difficulty: "Intermediate", vibes: ["Wellness", "Audio"] },
  { title: "EmojiMosaic", tagline: "Recreate any image with emojis.", description: "Slider for emoji density and palette restriction, with export to PNG.", prompt: "Build a Next.js client-side emoji mosaic generator where users upload any image — resize it to a configurable grid resolution (16×16 up to 128×128 cells) using the Canvas 2D API and getImageData, computing the average RGBA color for each grid cell. Pre-compute a lookup table of all 3,600+ Unicode emojis: render each emoji at 32×32px on an offscreen canvas, extract its average RGB color, and store the emoji character + average color in a client-side array. Run the nearest-color matching in a Web Worker for each cell: find the emoji whose average color minimizes Euclidean distance in RGB space to the cell's average color, building the complete mosaic grid as a 2D array of emoji characters. Render the mosaic grid in the DOM as a CSS grid of divs with the matched emoji as text content and no spacing, creating a pixel-art-style emoji image that the user can view and interact with. Add controls for: grid density slider, a palette restriction mode that limits the emoji pool to a subset (food emojis only, nature emojis, face emojis) using Unicode ranges, an 'export as PNG' button that renders the emoji grid to a canvas at 2× resolution using fillText for each cell, and a 'copy as text' button that copies the emoji grid as newline-separated emoji rows for pasting in Discord or Twitter.", stack: ["Canvas", "Web Workers"], difficulty: "Beginner", vibes: ["Toy", "Image"] },
  { title: "CinemaCypher", tagline: "Hidden messages in animated GIF frames.", description: "Embed text into the noise of GIF frames — only the receiver decodes it.", prompt: "Build a client-side-only Next.js steganography tool using WebAssembly (Wasm) for performance-critical pixel manipulation — the encoder accepts a GIF upload and a secret message, and encodes the message using LSB steganography: convert the message to UTF-8 bytes, then for each bit of the message, modify the least-significant bit of the red channel of the corresponding pixel across all GIF frames in a round-robin pattern, distributing the hidden bits across frames to evade frame-by-frame visual inspection. Use gif.js (a client-side GIF encoder) to reconstruct the modified GIF frame-by-frame with the altered pixel data, preserving the original animation timing, loop count, and color palette index mapping so the output GIF looks visually identical to the input. Implement the decoder as a second tool: the receiver uploads the encoded GIF, the Wasm module reads the LSB of each red channel pixel in the same round-robin frame order, reconstructs the bit string, converts it back to UTF-8 text, and displays the hidden message — no server involved at any point. Add an optional XOR encryption layer using a user-provided password (converted to a key using PBKDF2 in the Web Crypto API) that XORs the message bytes before LSB encoding, requiring both the GIF and the password to decode. Show a payload capacity indicator (max characters storable in the GIF based on total pixel count across all frames) and a visual 'noise level' meter showing how much the original image has been perturbed.", stack: ["WebAssembly", "GIF.js"], difficulty: "Advanced", vibes: ["Privacy", "Hacker"] },
  { title: "FrameForge", tagline: "Comic-book pages from a screenplay.", description: "Paste a scene, get a comic-style 6-panel layout with speech bubbles.", prompt: "Build a Next.js screenplay-to-comic generator where users paste a scene in standard screenplay format (INT./EXT., action lines, character names, dialogue) — parse the scene structure using a regex-based screenplay parser that extracts: scene heading, action descriptions (split into visual beats), and character dialogue as an ordered sequence. Pass the parsed beats to Claude to produce a 6-panel comic layout plan: for each panel, describe the camera angle (close-up, wide shot, over-the-shoulder), the characters present, the key visual element, and which dialogue lines appear in speech bubbles in that panel — output as a structured JSON array. Submit each panel description to Stable Diffusion XL on Replicate with a consistent comic book style suffix ('clean comic book art, bold outlines, flat colors, Marvel house style') and the specific panel's visual description as the prompt, then composite the returned images into a 2×3 grid panel layout using Sharp on the server. Overlay speech bubble SVGs on each panel at configurable positions using SVG foreignObject for text, with proper comic balloon tails pointing toward the speaking character, adjustable via a simple click-to-reposition UI. Export the completed 6-panel page as a high-resolution PDF comic page (A4 landscape) or a shareable PNG, and support multi-page mode where users can chain multiple scenes into a full comic book exported as a multi-page PDF.", stack: ["Replicate", "Canvas"], difficulty: "Advanced", vibes: ["Comics", "AI"] },
  { title: "Doppelganger", tagline: "Compare your selfie to historical paintings.", description: "Embedding-based similarity to find your portrait twin in art history.", prompt: "Build a Next.js face-similarity app that maintains a pre-computed database of face embeddings from 10,000+ historical portrait paintings sourced from open-access museum APIs (the Rijksmuseum API, the MET's open access collection, the Art Institute of Chicago API) — extract face bounding boxes and embeddings using a pre-trained FaceNet model served via TensorFlow.js running in a Node.js backend, storing all embeddings with artwork metadata in pgvector. When a user takes a selfie via webcam or uploads a photo, run FaceNet in the browser (TensorFlow.js) to generate a 128-dimensional face embedding from the detected face, POST it to the API, and query pgvector for the top 10 nearest neighbors by cosine distance. Display the top 3 matches as side-by-side comparison cards showing the user's selfie crop alongside the historical portrait, the artwork's title, artist, year, museum of origin, and a similarity score presented as a percentage. Add a 'explore this artist' button that queries all portraits by the matched artist and shows a gallery, and a 'find my twin across centuries' flow that shows the user's top match from each century (Renaissance, Baroque, Romantic, Modern). Ensure GDPR compliance by processing the selfie embedding entirely in the browser and never storing the raw selfie image server-side — only the computed embedding vector is sent to the API.", stack: ["TensorFlow.js", "Embeddings"], difficulty: "Intermediate", vibes: ["Art", "Selfie"] },
  { title: "QuoteCanvas", tagline: "Beautiful shareable quote cards from any text.", description: "Paste a paragraph, get a designed quote card in your brand colors.", prompt: "Build a Next.js quote card generator where users paste any paragraph of text — call Claude with a prompt to extract the single best pull-quote (the most emotionally resonant or thought-provoking sentence, ideally under 20 words) from the pasted text, returning it along with 3 suggested caption formats (attribution-style, hashtag-style, standalone). Render the selected quote on a canvas using a grid of 8 curated typographic layouts (centered large, left-aligned small, split-line dramatic, overlapping bold) combined with user-defined brand colors (primary background, text color, accent) applied via a color picker with real-time preview. Add an automatic font pairing system: based on the quote's emotional tone (detected via Claude), suggest a display + body font pairing from a library of 20 Google Fonts combinations, applying them via the Google Fonts API CSS import. Export the final card as SVG (infinitely scalable), PNG at 1200×630 (Twitter/LinkedIn optimized), 1080×1080 (Instagram square), and 1080×1920 (Stories format) — all generated from the same layout definition using different canvas dimensions. Let users save up to 20 cards in a brand kit that remembers their colors, fonts, and preferred layout so repeat use is instant, and add a shareable link that encodes the quote text and design parameters in a URL.", stack: ["Next.js", "OpenAI", "SVG"], difficulty: "Beginner", vibes: ["Design", "Social"] },
  { title: "MotionMaker", tagline: "Sketch a UI, get Framer Motion code.", description: "Draw or upload a wireframe transition and get production-ready React + Framer Motion code.", prompt: "Build a Next.js sketch-to-animation tool with a two-panel interface: on the left, a simple drawing canvas (or image upload area) where users sketch two UI states — e.g., a card in its collapsed state and its expanded state — using basic shapes and labels; on the right, a live preview panel with generated React + Framer Motion code. Pass the canvas PNG to GPT-4o Vision with a detailed prompt asking it to identify the UI components in each state, infer the transformation (what moved, scaled, faded, slid, or morphed between state A and state B), and produce a Framer Motion component that uses AnimatePresence, layout animations (layoutId for shared-element transitions), and explicit animate/initial/exit props for elements that appear or disappear. Display the generated code in a Monaco editor (read-only with syntax highlighting) with a live Sandpack iframe rendering the actual animated component on the right — so users immediately see their sketch become a working animation. Add an 'improve animation' chat input where users can type natural language refinements ('make the transition slower and add a spring bounce', 'fade in the subtext after the card expands') and GPT-4o patches the existing code accordingly. Support export as a standalone .tsx file, a Storybook story, or a CodeSandbox link.", stack: ["Next.js", "GPT-4o Vision"], difficulty: "Advanced", vibes: ["DX", "AI"] },
  { title: "NoiseGarden", tagline: "Procedural ambient soundscapes that grow.", description: "Tend a garden of generative audio plants — each emits its own loop.", prompt: "Build a Three.js ambient soundscape garden where users place up to 16 'sound seeds' on a 3D terrain — each seed grows into an animated plant over 30 seconds and emits a procedurally generated audio loop using Tone.js. Define 8 plant species, each with a unique synthesizer character: the Bellflower uses a Tone.MetalSynth with slowly swelling reverb, the Wireweed plays stochastic arpeggios on a Tone.Synth in a pentatonic scale, the Driftmoss generates rhythmic noise bursts via a Tone.NoiseSynth with a gate LFO, the Glasstree plays harmonic overtones using additive synthesis — each configurable via a small property panel that appears when a plant is clicked. Grow the 3D plants procedurally using L-System rules implemented in Three.js: at each growth tick (every 5 seconds of real time), extend stems with TubeGeometry following branching rules, add leaf meshes at specified branch internodes, and add flower geometries when the plant reaches maturity. Implement spatial audio using Tone.Panner3D so the soundscape mixes based on the Three.js camera's position — walking the camera through the garden creates a living, position-dependent ambient mix. Save garden states as compact JSON (plant positions, species, and growth ages) shareable via a URL query parameter, so users can share their exact garden soundscape with others who load it and hear the same procedural mix from the same camera position.", stack: ["Tone.js", "Three.js"], difficulty: "Intermediate", vibes: ["Ambient", "Generative"] },
];

const UTILITY: Omit<Project, "id" | "category">[] = [
  { title: "TabTamer", tagline: "AI sorts your 200 open tabs into folders.", description: "Browser extension that clusters and labels tabs, then closes the noise.", prompt: "Build a Chrome Manifest V3 extension with a popup that fetches all open tabs using chrome.tabs.query and embeds each page title and URL into a 384-dimension vector using a locally-running Transformers.js all-MiniLM-L6-v2 model so no data leaves the browser. Run HDBSCAN clustering in a Web Worker on the resulting embedding matrix, grouping tabs by semantic proximity with a configurable minimum cluster size. Send each cluster's representative embeddings to a small local LLM via chrome.runtime messaging — or optionally to the Anthropic API via a background service worker — to generate a descriptive folder name for each group. Render the clustered result in the popup UI as an accordion list of proposed tab groups with drag-and-drop to merge or split groups before applying; commit the groups using chrome.tabGroups.create and chrome.tabs.group. Save cluster history in chrome.storage.local so the user can compare tab profiles from previous browsing sessions and detect recurring domain patterns.", stack: ["Chrome MV3", "Embeddings"], difficulty: "Intermediate", vibes: ["Browser", "AI"] },
  { title: "ScreenSplit", tagline: "Smart screenshot cropper for docs.", description: "Drop a screenshot, auto-crop to the relevant UI element with annotation handles.", prompt: "Build a browser-based screenshot tool using the Screen Capture API (getDisplayMedia) that captures the current screen into an offscreen canvas, then applies Canny edge detection implemented in a WebAssembly module to find contiguous UI regions. Use Tesseract.js running in a Web Worker to identify text block bounding boxes and UI component boundaries, scoring each candidate crop region by text density and visual centering, then present the top 3 candidate crops with draggable resize handles. Annotate the final crop with callout arrows, text labels, and numbered steps via a canvas-drawing toolbar built on Fabric.js, supporting undo/redo with a history stack. Export the annotated screenshot as a PNG or WebP via the Canvas API with a configurable max resolution, copying it directly to the clipboard using navigator.clipboard.write so it can be pasted straight into docs or Slack. Add a 'saved shots' panel in a sidebar extension popup showing the last 20 captures with thumbnails, stored in chrome.storage.local.", stack: ["Tesseract.js", "Canvas"], difficulty: "Intermediate", vibes: ["Productivity", "Visual"] },
  { title: "WiFiWizard", tagline: "Scan home networks, label and tame them.", description: "Local utility to find every device on your LAN and tag them with friendly names.", prompt: "Build a Tauri desktop utility with a Rust backend that discovers LAN devices by broadcasting mDNS queries via the mdns-sd crate and sending ARP requests via the pnet crate to enumerate IP-to-MAC mappings across all active network interfaces. Query a bundled offline OUI database to resolve each MAC's manufacturer (Samsung, Raspberry Pi Foundation, etc.) and store all discovered devices in a local SQLite database via rusqlite, keyed by MAC address with a user-editable friendly name field. Display a live network map in the frontend using Cytoscape.js where each node shows the friendly name, manufacturer, IP, mDNS hostname, and open port count from a passive TCP SYN scan. Let users tag devices as trusted, guest, or unknown and configure Tauri shell command triggers (like Wake-on-LAN packets) via a right-click context menu per device. Run continuous background scans on a configurable interval and push a native OS notification via Tauri's notification API when a previously unseen device joins the network.", stack: ["Tauri", "mDNS", "Rust"], difficulty: "Advanced", vibes: ["Networking", "Local"] },
  { title: "HotkeyHero", tagline: "Learn shortcuts for every app you use.", description: "Watches what app is foreground and quizzes you on its top shortcuts.", prompt: "Build a Tauri desktop app with a Rust backend that polls the active window process name every 500ms using the winapi (Windows) or Accessibility API (macOS) crate, matching it against a curated JSON database of the top 30 shortcuts per app (VS Code, Figma, Chrome, Slack, Excel) bundled with the binary. When the user presses a configurable global hotkey registered via Tauri's globalShortcut API, a transparent always-on-top overlay appears showing 5 randomly selected shortcuts for the active application as quiz cards — the user types the key combination to mark it correct. Track correct and incorrect answers per shortcut in local SQLite and compute a per-app mastery score using the SM-2 spaced repetition algorithm to schedule which shortcuts appear next. Display a progress dashboard showing mastery heat maps per app, daily quiz streak, and a ranked list of least-mastered shortcuts across all detected applications. Supplement the bundled database with community-contributed shortcut packs fetchable from a Next.js API hosted on Vercel, letting users submit and upvote shortcut additions.", stack: ["Tauri", "Rust", "Next.js"], difficulty: "Intermediate", vibes: ["Productivity", "Learning"] },
  { title: "FocusFortress", tagline: "Block distracting sites with proof-of-work unlocks.", description: "Want Twitter? Solve a small math captcha for 60 seconds — friction kills the impulse.", prompt: "Build a Chrome Manifest V3 extension with a declarativeNetRequest rule set that intercepts navigations to a user-defined blocklist and redirects them to a local extension page. When the user visits a blocked site, show a timed breathwork animation (inhale 4s, hold 4s, exhale 4s, hold 4s) that must be completed in real time before granting a 5-minute access token stored in chrome.storage.session. Alternatively present a configurable math challenge — random two-digit multiplication or a short mental arithmetic chain — that must be solved correctly within 30 seconds; wrong answers reset the countdown with an increasing delay. Store the block schedule in chrome.storage.sync (synced across Chrome devices) and support time-based rules: Twitter blocked 9am-5pm on weekdays, Reddit blocked all day except Saturday, with calendar override URLs linked via Google Calendar OAuth. Log block attempts, successful unlocks, and bypass durations in chrome.storage.local and display a weekly focus report showing top blocked sites, total friction time, and distraction patterns by hour of day.", stack: ["Chrome MV3"], difficulty: "Beginner", vibes: ["Focus", "Wellness"] },
  { title: "PDFPamper", tagline: "Re-typeset ugly scanned PDFs into clean ones.", description: "OCR + reflow makes scanned papers readable on phones again.", prompt: "Build a Next.js web app where users upload scanned PDF files; process each page through Tesseract.js with psm 3 (fully automatic page segmentation) to detect columns, paragraphs, and reading order, extracting structured text blocks with bounding boxes and font-size estimates. Reconstruct reading order by sorting text blocks top-to-bottom and left-to-right within each detected column region, identifying headings by relative font size and paragraph breaks by vertical gap heuristics. Reflow the extracted text into a clean PDF using PDFKit with configurable body font (default Georgia 12pt), line height, margins, and page size, preserving heading hierarchy with larger bold font sizes. Optionally export to EPUB format using epub-gen, with each detected chapter section becoming an EPUB chapter with proper navigation metadata, making the output readable in any e-reader app. Add a side-by-side preview in the UI showing the original scanned page and the reflowed output, with a confidence slider controlling Tesseract's minimum word confidence threshold to balance correctness vs. coverage.", stack: ["Tesseract.js", "PDFKit"], difficulty: "Advanced", vibes: ["Reading", "OCR"] },
  { title: "ClipKeeper", tagline: "Clipboard manager with semantic search.", description: "Copy ten things, find them later by typing what they're about.", prompt: "Build a Tauri application with a Rust backend using the arboard crate to monitor the system clipboard via a 200ms polling loop, detecting new text, image, or file-path content and storing each clip in a local SQLite database via rusqlite with fields: content, type, source_app, and captured_at. For text clips, embed each item using a locally-running Transformers.js all-MiniLM-L6-v2 model called from the frontend; store the 384-dimension embedding alongside the clip in SQLite using the sqlite-vec extension so semantic similarity queries run entirely offline. Build a quick-launch popup triggered by a global shortcut that renders a fuzzy-search input overlaid on the screen; typing searches keyword matches and semantic similarity simultaneously, blending results by a configurable alpha weight. Let users star clips, add custom tags, and create clip collections (code snippets, addresses) navigable in a sidebar; clicking any clip copies it to the clipboard and dismisses the popup. Auto-expire unstarred clips older than 30 days via a Tauri background task, with a configurable retention period and a privacy mode that pauses clipboard monitoring for a set duration.", stack: ["Tauri", "pgvector"], difficulty: "Intermediate", vibes: ["Productivity", "AI"] },
  { title: "CalCompass", tagline: "Find time across messy team calendars in one click.", description: "Paste availabilities of 5 people; it negotiates a slot, not a doodle poll.", prompt: "Build a Next.js web app that connects up to 10 Google Calendars simultaneously via separate OAuth tokens per person stored encrypted in Postgres, using the Google Calendar API's freebusy endpoint to query each person's availability for a configurable date range without reading event titles or details. Apply configurable per-person preference rules — 'no meetings before 10am', 'prefer afternoons', 'block Mondays', 'lunch 12-1pm protected' — stored in a user_preferences table, and combine them with the free/busy data to produce a preference-weighted availability matrix across the group. Implement a greedy slot-finder that scores each 30-minute window by summing preference penalties across all participants and proposes the top 3 slots ranked by lowest total friction. Display results in a color-coded week-view calendar where each participant's availability is overlaid as a transparent layer, making conflict and overlap visually obvious without exposing individual event details. Auto-generate a Google Calendar invite for the chosen slot via the events.insert endpoint, emailing all attendees with the meeting link, agenda template, and a one-click reschedule URL.", stack: ["Next.js", "Google Calendar API"], difficulty: "Intermediate", vibes: ["Scheduling", "Team"] },
  { title: "ReadabilityPro", tagline: "Refactor any prose to a target reading level.", description: "Paste, set 'sixth grade', get the same idea in plainer words.", prompt: "Build a Next.js web app where users paste prose, choose a target reading level from a Grade 3 to Grade 12+ slider, and send the text to Claude with a prompt instructing it to rewrite for the target Flesch-Kincaid grade level while preserving all factual content and proper nouns marked with [KEEP] tokens. After each rewrite, compute the Flesch-Kincaid Grade Level and Reading Ease score client-side using the flesch-kincaid npm library and display them as animated gauge charts that update in real time as the user edits the output. Show a side-by-side diff of original and rewritten text using jsdiff, highlighting every changed phrase so the user understands exactly what was simplified and what was preserved. Let users lock specific sentences by selecting them and clicking 'lock' — locked blocks are passed as [LOCKED] tokens and returned verbatim, useful for preserving legal disclaimers or technical specs. Export the final document as a formatted DOCX using the docx library with the original text in comment-track annotations beside each modified sentence, ready for editorial review in Word or Google Docs.", stack: ["Next.js", "Claude"], difficulty: "Beginner", vibes: ["Writing", "AI"] },
  { title: "WatchWise", tagline: "Aggregate streaming services into one queue.", description: "See where any movie is streamable, mark watched, share lists.", prompt: "Build a Next.js web app that queries the JustWatch API for each title a user searches, returning which streaming services currently have it available with direct deep-link URLs into each platform, refreshing availability every 24 hours via a Vercel Cron job to catch library changes. Let users build a personal watchlist with 'want to watch' or 'rewatching' flags stored in Postgres, and mark titles watched with a 5-star rating and optional note when done. Support a group watchlist where multiple users contribute titles and vote on what to watch together — computing an intersection of the group's collective service subscriptions (stored per-user) to surface only titles everyone can access. Implement a 'tonight picker' that filters the watchlist by mood tag (cozy, intense, funny, mindless), intersecting services, and a runtime preference (under 90 min for weeknights), presenting 3 random candidates with trailer preview links. Send a weekly Resend email digest listing each user's top 3 watchlist additions from friends, newly available titles on their subscribed services from their existing list, and a curated 'new on [Netflix/Hulu/etc.]' section from the JustWatch new releases feed.", stack: ["Next.js", "JustWatch API"], difficulty: "Beginner", vibes: ["Movies", "Daily"] },
  { title: "VocabCloud", tagline: "Track words you read and quiz them weekly.", description: "Highlight on the web, build a personal vocabulary that surfaces in spaced reviews.", prompt: "Build a Chrome Manifest V3 extension that adds a 'Save to VocabCloud' right-click context menu for any selected word; capture the word, surrounding sentence context, page URL, and detected language, then POST them to a Next.js API route stored in Postgres. Enrich each saved word via the Merriam-Webster Collegiate Dictionary API for definition, pronunciation phonetics, and example sentences, then call OpenAI to generate 3 additional context-specific example sentences showing the word used in similar contexts to where it was found. Implement SM-2 spaced repetition: each word has an ease factor, interval, and next_review_date in Postgres; the daily review queue surfaces words due today as flashcards with definition on the front and fill-in-the-blank sentences on the back, recording correct/incorrect per review. Build a web dashboard at /vocab showing all saved words with definition, source URL, and a sparkline of review accuracy over time, filterable by language, difficulty, and date saved. Add a 'word of the day' feature that emails the user via Resend each morning with their most-overdue vocabulary word, definition, and 3 example sentences.", stack: ["Chrome MV3", "Next.js"], difficulty: "Intermediate", vibes: ["Learning", "SR"] },
  { title: "DotfileDoctor", tagline: "Diff your shell config across machines.", description: "Sync, audit, and lint your dotfiles with a friendly UI.", prompt: "Build a Tauri desktop app with a Rust backend using the thrussh crate to establish SSH connections to multiple configured machines, fetching dotfiles (~/.zshrc, ~/.bashrc, ~/.gitconfig, ~/.tmux.conf, ~/.vimrc) via SFTP to a local temp directory keyed by machine hostname. Run a diff between each pair of machines' versions of each file using the gitoxide crate and display the results in a split-pane Monaco Editor diff view in read-only mode, making differences immediately visible. For each detected difference, pass the two file versions to Claude with a prompt explaining what the change means functionally — 'machine A sets EDITOR=vim, machine B doesn't set it' — and suggest a canonical merged version that preserves the intent of both. Let users review each suggested merge interactively, approve or modify each change, and push the merged result back to one or all remote machines via SFTP. Add a local git repository in ~/.dotfiles-doctor tracking the canonical merged history of each file over time, so the user has a full audit trail of which machine introduced which configuration change and when.", stack: ["Tauri", "SSH"], difficulty: "Advanced", vibes: ["DevTools", "Sysadmin"] },
  { title: "PantryPal", tagline: "Scan your fridge, get dinner ideas.", description: "Photo of contents, AI proposes 3 recipes that minimize waste.", prompt: "Build a Next.js web app where users upload a photo of their fridge or pantry; send it to GPT-4o Vision with a structured prompt returning a JSON array of identified ingredients with quantity estimates (e.g., {ingredient: 'half bell pepper', quantity: '0.5', unit: 'whole'}). Pass the ingredient list to GPT-4o with a recipe-generation prompt instructing it to propose exactly 3 recipes ranked by 'waste reduction score' — a heuristic based on how many near-expiry or partial ingredients each recipe uses. For each recipe, generate a title, 3-sentence description, full ingredient list with quantities, numbered steps, estimated cooking time, and difficulty rating, all returned as structured JSON stored in Postgres. Display the 3 recipe cards with expandable instructions, a shopping-list button showing what extra ingredients to buy, and a 'cook this' toggle opening the recipe in a printable step-by-step view with checkable steps. Let users save recipes to a personal recipe book, rate them after cooking, and view a weekly ingredient-efficiency score showing how much of their detected pantry they used in logged meals.", stack: ["Next.js", "GPT-4o"], difficulty: "Beginner", vibes: ["Cooking", "AI"] },
  { title: "MicroMacros", tagline: "Build personal automations like Shortcuts but cross-OS.", description: "Drag-and-drop blocks for repeatable web flows, no-code.", prompt: "Build a Next.js web app with a visual drag-and-drop automation builder using React DnD where users assemble sequences of web-action blocks: Open URL, Click Selector, Fill Input, Wait for Selector, Extract Text, and IF/THEN conditional branches, each configured via a right-side properties panel. Store each automation as a JSON workflow document in Postgres with a workflow_revisions table for rollback, and expose a 'Run Now' button that submits a job to a Vercel background function. Execute automations server-side using a Playwright worker that spins up headless Chromium, runs each block in sequence, and captures screenshots at each step stored in S3 as a step-by-step execution log. Support parameterized automations: define variable placeholders like {{email}} in any block's input field, and let users pass values at runtime via a form auto-generated from the variable list — making the same automation reusable with different inputs. Add schedule triggers via Vercel Cron and webhook triggers accepting a POST with JSON payload to fill automation variable slots, enabling event-driven execution from external tools.", stack: ["Next.js", "Playwright"], difficulty: "Advanced", vibes: ["Automation", "No-Code"] },
  { title: "EmojiKey", tagline: "Smart emoji autocomplete that knows context.", description: "Tab to autocomplete the right emoji for the sentiment of your sentence.", prompt: "Build an Electron application using iohook for cross-platform keyboard hook access, monitoring the last 30 characters of keyboard input in any application's text field to build a running context buffer — kept entirely in-memory with no disk writes. After each word boundary, classify the sentiment and topic of the buffer using a locally-running ONNX model (DistilBERT or similar) via onnxruntime-node, producing a top-3 category vector (positive/negative/neutral + food/tech/love/etc.). Display a floating emoji suggestion bar as a transparent always-on-top Electron BrowserWindow positioned above the cursor, showing the top 5 emojis ranked by category and sentiment match from a curated emoji-to-category mapping JSON bundled with the app. When the user presses Tab, inject the selected emoji using robotjs's typeString method to simulate keyboard input into the active application, then dismiss the suggestion bar. Let users add custom emoji-to-topic associations, train on their own writing style via a thumbs-up/thumbs-down feedback button, and configure a blacklist of application process names where the overlay should never appear.", stack: ["Electron", "Native input"], difficulty: "Advanced", vibes: ["Input", "Daily"] },
  { title: "PriceProwl", tagline: "Track prices on niche product sites.", description: "Plug in any product page; get email alerts when it dips.", prompt: "Build a Next.js web app where users paste any product page URL; extract the page HTML using a Playwright headless browser to handle JavaScript-rendered prices, then pass a 2000-character snippet of the visible text and DOM structure to GPT-4o asking it to identify the primary price element's CSS selector, returning the selector, price, currency, and sale_flag as JSON. Store the product URL, extracted selector, latest price, and price_history array in Postgres, then run a Playwright-based Vercel Cron job daily that visits each tracked product page using the stored selector, extracts the current price, and appends a new history record. When price drops below the user's configured target or drops more than a configurable percentage from the last recorded price, send a Resend email alert with the product name, current price, original price, savings amount, and a direct link. Display a dashboard showing all tracked products with a Recharts line chart of price history per product, the lowest recorded price, and days since last change. Add a browser bookmarklet that pre-fills the URL input on the tracking page with the current tab's URL, allowing one-click product addition.", stack: ["Next.js", "Playwright", "GPT-4o"], difficulty: "Intermediate", vibes: ["Shopping", "Alerts"] },
  { title: "SnoozeScreen", tagline: "Schedule per-app screen blackouts.", description: "Slack only between 9-5, Twitter only Saturdays — set it and forget it.", prompt: "Build a Tauri desktop app where users define schedules per application — 'Slack: Mon-Fri 9am-6pm', 'Twitter: Sat-Sun only', 'Email: no notifications after 8pm' — stored as JSON rules in local SQLite, with a week-grid UI showing colored schedule blocks per app that can be dragged to adjust time ranges. Implement macOS enforcement by calling launchctl via Tauri's shell command API to unload LaunchAgents for target apps during blocked windows, and activate Focus modes via NSDistributedNotificationCenter through a Swift helper bundled with the app. On Windows, use CreateJobObject and TerminateJobObject Win32 APIs via a Rust FFI layer to suspend/resume target process trees by executable name during blocked periods. Show a system tray icon that updates to reflect how many apps are in enforced quiet mode, and display a 30-second warning notification before a block period ends to prevent disruptive context switches. Log all enforced quiet periods to local SQLite and display a weekly 'enforced focus' chart showing total hours blocked per app alongside self-reported productivity ratings logged at end of day.", stack: ["Tauri", "macOS Focus API"], difficulty: "Advanced", vibes: ["Focus", "OS"] },
  { title: "DocDrop", tagline: "Drag any doc, get a 1-paragraph summary anywhere.", description: "Tray app: drag PDF/DOCX/MD, summary lands in clipboard or Notion.", prompt: "Build a Tauri application that registers a drop target in the macOS Dock and Windows taskbar by creating a custom URL scheme handler, accepting dropped PDF, DOCX, and Markdown file paths from the OS file manager. For each dropped file, extract text client-side: use pdf-parse for PDFs via a Tauri sidecar Node.js process, mammoth for DOCX extraction, and read Markdown directly — then pass the text to Claude's Messages API requesting a 1-paragraph summary, 5 bullet-point key takeaways, and a list of action items or decisions found in the document. Display the summary in a popover window near the tray icon with a 'Copy Summary' button writing to the clipboard via the arboard Rust crate, and a 'Send to Notion' button creating a new Notion page via the Notion API with the full extracted text as toggle blocks under a summary header. Support a hotkey-triggered mode where the tool summarizes whatever file is currently selected in Finder or Explorer, reading the active selection via an AppleScript sidecar or Windows Shell COM API call. Store the last 50 summarized documents in a local SQLite history accessible from the tray icon's menu, searchable by filename and summary keywords.", stack: ["Tauri", "Claude"], difficulty: "Intermediate", vibes: ["Productivity", "AI"] },
  { title: "BillSpotter", tagline: "Detect surprise charges in bank statements.", description: "Drop a CSV, see anomalies and untagged subscriptions.", prompt: "Build a Next.js web app where users upload CSV bank statement exports; automatically detect column layouts for Chase, Bank of America, and Wells Fargo formats using a header-sniffing heuristic, then normalize all transactions into a unified schema (date, description, amount, category) stored in Postgres. Apply an Isolation Forest anomaly detection algorithm in a Python FastAPI microservice using scikit-learn, training per user on their own transaction history to adapt to their spending baseline, and returning an anomaly_score per transaction; scores above a configurable threshold are flagged. Pass all flagged transactions plus the user's historical category labels to Claude, which classifies each flagged item as: unknown subscription, duplicate charge, amount spike (unusually large for that merchant), or new recurring charge — returning structured JSON per item with a plain-English explanation and recommended action. Display flagged transactions in a review table with color-coded anomaly type badges; the user marks each as 'OK' (training the model to not flag similar future transactions) or 'Dispute' (generating a copy-paste dispute letter for the bank). Show a monthly spending dashboard with category breakdowns, subscription total, anomaly count trend, and a 'shadow subscriptions' panel listing all detected recurring charges regardless of anomaly status.", stack: ["Next.js", "Python service"], difficulty: "Intermediate", vibes: ["Finance", "Anomaly"] },
  { title: "TripTrim", tagline: "Multi-city trip optimizer.", description: "Add cities, get the cheapest order with flight + hotel costs.", prompt: "Build a Next.js web app where users add a list of cities they want to visit with optional date range constraints per city, then fetch real route prices between all city pairs via the Skyscanner Flights API and Booking.com affiliate API, modeling each edge's cost as the sum of cheapest available flight and lowest accommodation per city-pair. Solve the routing problem as a weighted directed graph using a Traveling Salesman heuristic: nearest-neighbor initialization followed by 2-opt local search to minimize total trip cost, respecting any date constraints the user has locked per city. Display the top 3 optimized itineraries in a Mapbox GL JS route visualization showing the trip path, total estimated cost, and per-city stay duration, with a cost breakdown table per leg. Let users lock specific city-to-city legs (e.g., 'already booked Paris → Rome') or fix absolute dates for certain cities as constraints that the optimizer respects when re-running. Generate a Google Calendar .ics export per itinerary with each leg as a calendar event containing flight date/time, accommodation check-in, and a description block with booking links and estimated costs, downloadable with a single click.", stack: ["Next.js", "Skyscanner API"], difficulty: "Advanced", vibes: ["Travel", "Optimization"] },
  { title: "MeetingAxer", tagline: "Suggest meetings to cancel based on past attendance.", description: "Looks at your calendar history and proposes recurring meetings to ditch.", prompt: "Build a Next.js web app connecting to Google Calendar via OAuth, ingesting the last 90 days of calendar events and identifying all recurring meeting series by their recurringEventId field; compute engagement metrics per series: attendance rate (accepted vs. declined/tentative), average duration, and meeting frequency relative to the user's total weekly meeting hours. For each recurring series, fetch Gmail thread activity within 30 minutes after each meeting occurrence using the Gmail API's search endpoint, analyzing whether follow-up emails were sent — as a proxy for whether the meeting produced actionable output. Pass compiled engagement metrics, attendee list, meeting title, and post-meeting email activity to Claude, which produces a recommendation (cancel, reduce frequency, shorten duration, or keep) with a one-sentence justification for each meeting series. Present recommendations in a prioritized list sorted by estimated hours saved per month, with a 'Request Cancellation' button per meeting that drafts and queues a politely worded email to the meeting organizer via the Gmail API. Display a time-savings forecast showing hours per week reclaimed if all recommendations are accepted, and a calendar heatmap showing which days are most overloaded with meetings that produce no follow-up.", stack: ["Google Calendar", "Gmail"], difficulty: "Intermediate", vibes: ["Productivity", "Audit"] },
  { title: "TaxTagger", tagline: "Auto-categorize freelancer expenses for taxes.", description: "Snap receipts year-round, get a Schedule C draft in April.", prompt: "Build a Next.js web app where freelancers photograph receipts on mobile or upload images and PDFs; process each file through GPT-4o Vision with a structured JSON schema prompt extracting vendor name, transaction date, total amount, currency, tax amount, and individual line items, stored in Postgres in a normalized expenses schema. Train a personalized categorization model per user using their own labeled data: maintain a labeled_examples table where each expense is tagged with an IRS Schedule C category (Advertising, Business Meals 50%, Office Supplies, Software Subscriptions, Home Office, Vehicle), then use Claude few-shot classification with the user's top 10 examples per category to predict labels for new receipts. Implement a 'learn mode' swipe-based review queue where the user confirms or corrects each predicted category — each correction adds back to labeled_examples so future predictions improve — showing a category confidence score and flagging low-confidence items for manual review. Generate a Schedule C draft CSV organized by IRS line item code at any time of year: subtotals per category, grand total deductible amount, and a list of flagged expenses needing professional review (meals above $75 without attendee names, mixed-use items). Integrate the Stripe API to automatically import Stripe payouts as income records alongside expenses, computing estimated quarterly self-employment tax liability shown as a real-time tax estimate widget.", stack: ["Next.js", "GPT-4o Vision"], difficulty: "Advanced", vibes: ["Finance", "Freelance"] },
  { title: "DriveDeclutter", tagline: "Auto-sort messy Google Drive folders.", description: "Detects duplicates, suggests merges, and renames consistently.", prompt: "Build a Next.js web app that authenticates with Google Drive via OAuth and recursively walks the folder tree using the files.list API, fetching all files with their id, name, mimeType, md5Checksum, size, parents, and modifiedTime into a Postgres database, refreshing via a Vercel Cron job every 24 hours. Detect exact duplicates by grouping uploaded binary files by MD5 checksum, and near-duplicates by embedding each file's title using OpenAI text-embedding-3-small and querying pgvector for files with cosine similarity above 0.9. Present an interactive duplicate review UI where each duplicate group shows a side-by-side preview via the Drive Files.get?alt=media endpoint for small files, modification dates, parent folders, and a radio selector to choose the canonical version — with a 'Move to Trash' action calling the Drive API's files.trash endpoint on all non-canonical copies. Propose batch rename operations for inconsistently named files in the same folder — 'Q1_Report.pdf', 'q1 report final.pdf', 'Q1Report_v2.pdf' flagged as a rename cluster — generating standardized names following a configurable naming convention defined in settings. Generate a Drive health report as a downloadable CSV listing all identified duplicate groups, near-duplicates, orphaned files, and proposed renames with estimated storage savings from deduplication.", stack: ["Google Drive API", "Embeddings"], difficulty: "Intermediate", vibes: ["Files", "Cleanup"] },
  { title: "QuickQR", tagline: "Stylized QR codes with embedded logos.", description: "Drag a logo, choose a palette, get a high-error-correction designed QR.", prompt: "Build a Next.js web app where users paste a URL, WiFi credential string, or contact vCard; generate a QR code using the qrcode library at error correction level H (30% damage tolerance, required for logo overlays) with a configurable module size. Add a logo overlay workflow: the user uploads a PNG or SVG logo, the server resizes it to fit within the safe 30% center region of the QR using the sharp library, composites it onto the QR canvas, and immediately validates the result by passing it back through jsQR to confirm it still decodes correctly before showing it to the user. Offer a visual style editor with gradient fill options (linear and radial across individual modules), module shape variants (square, rounded, circular), finder pattern style options, and background color with transparency support for PNGs used on colored backgrounds. Render a live preview updating in real time as the user adjusts style sliders, and run the jsQR validation check after each style change to show a 'Scannable' or 'Fix required' badge. Export the final QR in SVG format for print at any size, PNG at configurable DPI, and a sticker-print layout PDF with multiple sizes on one page generated using PDFKit, all downloadable from a single button row.", stack: ["Canvas", "QR libs"], difficulty: "Beginner", vibes: ["Design", "QR"] },
  { title: "PetPanic", tagline: "Lost-pet alerts that ping nearby neighbors.", description: "Drop a flyer, the system pings users within X miles via push and posters.", prompt: "Build a Next.js Progressive Web App where pet owners register their pets with photos, breed, color, and microchip number stored in Postgres; when a pet goes missing, the owner submits a lost report with the last known location on a Mapbox GL JS interface, and the system generates a styled printable PDF poster using PDFKit with the pet's photo, description, and owner contact info. Use the Web Push API with VAPID keys to send push notifications to nearby registered community members whose lat/long — stored in Postgres as PostGIS points — is within a configurable radius of the pet's last known location, defaulting to 3 miles and expanding to 5 miles if fewer than 20 subscribers are found. Allow community members to submit sighting reports with a photo, GPS coordinates, and description via a mobile-optimized form; display all sightings on a Mapbox map as timestamped markers connected to the original missing location, giving the owner a trail of the pet's movement. Notify the owner via Twilio SMS when a new sighting is submitted within 10 miles, and send a thank-you email via Resend to all community members who submitted sightings when the pet is marked found. Add neighborhood stats on a public community page showing total pets reunited, active lost reports in the area, and a leaderboard of community members by sighting contributions to encourage engagement.", stack: ["Next.js", "Web Push", "Mapbox"], difficulty: "Intermediate", vibes: ["Pets", "Local"] },
  { title: "Subspace", tagline: "Manage subscriptions to creator content.", description: "Aggregate Patreons, Substacks, OnlyFans receipts; pause/cancel from one screen.", prompt: "Build a Next.js web app that connects to Gmail via OAuth and scans the inbox for subscription-related emails from Patreon, Substack, Buy Me a Coffee, Memberful, and similar platforms using sender domain matching and Gmail API category filters, extracting subscription name, billing amount, billing frequency, and next billing date from email bodies parsed by Claude into structured JSON. Normalize all subscriptions into a unified Postgres table with fields for platform, creator name, monthly_equivalent_cost, status (active, paused, canceled), and a notes field, computing a monthly burn total across all subscriptions displayed in the dashboard header. For each active subscription, query the platform's public API for the creator's latest content activity — Substack's /api/v1/posts endpoint, Patreon's campaign API — to show the last publication date and content frequency, flagging subscriptions where the creator hasn't posted in over 60 days as 'potentially inactive'. Build a 'cancel guide' panel for each subscription showing the exact cancellation steps for that platform alongside a 'Request pause email' button that drafts a polite message to the creator via the Gmail API compose draft endpoint. Display a subscription timeline calendar showing all upcoming billing dates color-coded by platform, total projected annual spend, and a 'value score' per subscription based on content frequency and user-assigned enjoyment rating.", stack: ["Gmail API", "Next.js"], difficulty: "Intermediate", vibes: ["Finance", "Creator Econ"] },
  { title: "InboxArchaeo", tagline: "Find old emails by vibe, not keyword.", description: "Search 'happy email from mom' and find it.", prompt: "Build a Next.js web app that connects to Gmail via OAuth and progressively indexes emails from the last 2 years using the Gmail API's messages.list endpoint; for each email, extract subject, snippet, sender, and date, pass the subject + snippet through OpenAI text-embedding-3-small, and store the resulting vector in pgvector alongside the Postgres email metadata record. Build a semantic search interface where users type queries like 'happy birthday message from grandma' or 'the email with my lease agreement from 2022' — embed the query and retrieve the top 15 nearest emails by cosine similarity from pgvector, displaying each result as an expandable card with sender, date, subject, and the relevant snippet highlighted. For each search result, add a 'View Full Email' button that fetches the complete email body via the Gmail API's messages.get endpoint on demand — not stored, to preserve privacy — and renders it in a modal with HTML sanitized via DOMPurify. Show a search history sidebar with the user's last 20 queries and a 'Save Search' feature that stores a named query and sends a weekly Resend digest when new emails match. Display an inbox overview showing email volume by year, top senders, and a topic cluster map generated by running k-means on the stored embeddings and labeling each cluster with Claude.", stack: ["Gmail API", "pgvector"], difficulty: "Intermediate", vibes: ["Search", "Email"] },
  { title: "CodeCovTrack", tagline: "Personal code-coverage history per repo.", description: "See your coverage % over time and where you broke ground last week.", prompt: "Build a Node.js CLI tool published to npm as code-cov-track that wraps Jest, Pytest, and nyc/Istanbul by injecting a post-test hook; after each test run it reads the generated coverage JSON, extracts per-file line coverage percentages, and POSTs the result to a central API with the git commit SHA, branch name, and timestamp. Store all coverage snapshots in Postgres normalized as coverage_snapshots (run metadata) and file_coverage (per-file percentages per run), and expose a Next.js dashboard where users sign in with GitHub OAuth to see their repositories. Visualize overall coverage % as a Recharts line chart over the last 100 commits with branch annotations, and a per-file heat map where each row is a source file and each column is a recent commit — cells colored red (dropped), green (improved), or gray (unchanged) so coverage drift is visible at a glance. Alert via a GitHub Status Check (posted via the Checks API) when coverage drops more than a configurable threshold from the previous commit's baseline — failing the PR check so low-coverage changes are surfaced in code review. Allow team owners to set per-directory minimum coverage thresholds (e.g., 'src/api must stay above 90%') enforced in the same GitHub Status Check, with a drill-down view showing the specific files that caused the threshold violation.", stack: ["Node CLI", "Next.js"], difficulty: "Intermediate", vibes: ["DevTools", "Tests"] },
  { title: "TimezoneTango", tagline: "Find a humane meeting time across continents.", description: "Visualize working hours overlap with circadian-friendly hints.", prompt: "Build a Next.js web app where users search for team members by name or email, each assigned a timezone stored in Postgres; display a 24-hour timeline grid where each participant's working hours (configurable 8am-6pm local) are shown as colored bands on a shared vertical axis using a custom React SVG component, making overlap zones immediately visible. Color-code meeting time slots by a humanity score: ideal (green, within 9am-5pm for all), acceptable (amber, someone works evening), difficult (orange, someone is pre-8am), and inhumane (red, someone is in the middle of their night) — calculated using a per-slot weighted penalty function. Add circadian-aware scheduling hints noting that many East Asian cultures have normative work patterns extending later into the evening than Western norms, adjusting humanity score based on configurable cultural presets per timezone region. Render an optional 3D globe visualization using Three.js with @react-three/fiber where each participant's city is a glowing pin and a rotating illumination overlay shows day/night zones in real time. Export the chosen meeting time as a Google Calendar invite or .ics file with timezone-correct start times per attendee, and generate a copy-paste snippet like '10am ET / 3pm GMT / 11pm SGT' formatted for Slack.", stack: ["Three.js", "TZ DB"], difficulty: "Beginner", vibes: ["Remote Work"] },
  { title: "FormulaForage", tagline: "Convert plain English to spreadsheet formulas.", description: "Paste your data, ask 'sum of column B where date is in 2024'.", prompt: "Build a Next.js web app where users paste a sample of their spreadsheet data as CSV (up to 50 rows) and type a natural-language description of the formula they need — 'sum of sales in column C where the date in column A is in 2024 and region in column B is West'. Send the CSV headers, first 5 data rows, and the natural-language request to Claude, which outputs the Excel formula string, the Google Sheets formula string (handling syntax differences like ARRAYFORMULA vs. XLOOKUP), a plain-English step-by-step explanation, and the expected output value when applied to the sample data. Render the formula in a Monaco Editor instance with syntax highlighting for Excel formula syntax using a custom Monaco language definition, and display the step-by-step explanation in a collapsible accordion below. Validate the formula by executing it against the sample data using the formulajs library and displaying the computed result alongside Claude's expected result, showing a green checkmark when they match. Add a formula history sidebar storing the last 20 generated formulas per user session in localStorage, with one-click reload of any prior query, plus 'copy for Excel' and 'copy for Sheets' buttons that copy the correct syntax variant to the clipboard.", stack: ["Next.js", "OpenAI"], difficulty: "Beginner", vibes: ["Spreadsheets", "AI"] },
  { title: "VoiceMemoVault", tagline: "Searchable, transcribed personal voice memos.", description: "Auto-transcribe, tag, and search every voice memo you've ever made.", prompt: "Build a Progressive Web App with a mobile-first UI where users tap a record button that activates the browser's MediaRecorder API to capture audio as WebM or MP3, uploading to S3 via a pre-signed URL as soon as recording stops, with offline queuing via a Service Worker if the network is unavailable. Process each uploaded file through Whisper (via the OpenAI API or self-hosted on Replicate) to produce a timestamped word-level transcript, segment the transcript into topic chunks using Claude, and store full transcript, segments, and Whisper word timestamps in Postgres. Embed each voice memo's full transcript using OpenAI text-embedding-3-small and store in pgvector; build a semantic search bar where users type queries like 'the idea about the marketing campaign' and retrieve the top 5 most relevant memos ranked by cosine similarity, displayed with the matching segment highlighted and a 'jump to' timestamp deep-link that seeks the audio player. Generate a daily digest by grouping that day's memos by topic cluster and passing them to Claude to produce a 3-5 bullet summary of the day's ideas, sent via Resend at a configurable evening time as a personal 'idea recap'. Add a tagging UI where users manually tag memos with project names or context labels, combine tag filters with semantic search in the query interface, and export all memos for a given tag as a concatenated transcript PDF.", stack: ["Whisper", "PWA"], difficulty: "Intermediate", vibes: ["Voice", "Personal KB"] },
  { title: "SshSimplify", tagline: "Pretty UI for managing SSH config.", description: "GUI to add hosts, test connections, and back up your ~/.ssh/config.", prompt: "Build a Tauri desktop application that reads the user's ~/.ssh/config file using a custom Rust parser, parsing all Host blocks with their HostName, User, Port, IdentityFile, and ForwardAgent fields into an in-memory data model displayed in a sidebar list of configured hosts. Implement a visual host editor in the webview where users add or edit entries via labeled inputs — friendly label, hostname/IP, username, port, key file picker using Tauri's file dialog API — validate the form before writing, and serialize changes back to ~/.ssh/config via the Tauri FS API, preserving any existing comments or custom directives the editor doesn't model. Add a one-click connection test button per host that spawns an SSH session via the openssh-client CLI (using Tauri's shell command API with a 5-second timeout) and reports 'Connected successfully' or the error message as a colored status badge. Implement config file versioning by initializing a local git repository at ~/.ssh/config-history using shell git commands via Tauri, committing after every save with a timestamp and changed host name message, and exposing a version history sidebar with one-click restore to any prior version. Add a secure backup feature that exports the entire ~/.ssh directory as an encrypted ZIP using the zip and aes-gcm Rust crates protected by a user-set passphrase, so the config can be restored on a new machine without exposing key material.", stack: ["Tauri", "Rust"], difficulty: "Intermediate", vibes: ["DevTools", "Sysadmin"] },
  { title: "CalendarConfetti", tagline: "Celebrate your past wins from the calendar.", description: "Bot reads completed events and suggests journaling-prompted reflections.", prompt: "Build a Next.js web app that connects to Google Calendar via OAuth and GitHub via a personal access token, running a weekly cron job every Sunday evening that fetches all completed calendar events from the past 7 days via the events.list endpoint filtered by timeMax: now, alongside all commits made by the user during the same period from the GitHub API's /users/{username}/events endpoint filtered by PushEvent type. Pass the week's events (title, duration, attendee count) and commits (repo, message, files changed) to Claude with a prompt identifying the most meaningful accomplishments — filtering out recurring meetings, transit events, and trivial commits — and organizing them into 3-5 highlight bullets written in a warm, encouraging second-person tone. Generate a journal reflection prompt tailored to the week's actual content: if the user had many meetings, ask about relationships and collaboration; if mostly solo commits, ask about problem-solving and focus. Send the personalized email via Resend with a styled HTML template featuring the weekly summary, 5-minute journaling prompt, and a mood check-in link that records a mood score in Postgres for trend visualization. Build a web dashboard showing mood score history as a line chart, average productive hours per week based on calendar analysis, and a word-cloud of most common activity keywords from the last 3 months of weekly summaries.", stack: ["Google Calendar", "Resend"], difficulty: "Beginner", vibes: ["Wellness", "Journaling"] },
  { title: "PullPilot", tagline: "Personal PR queue manager.", description: "See all your open PRs across orgs with health and reviewer signals.", prompt: "Build a Next.js web app that authenticates with GitHub, GitLab, and Bitbucket via separate OAuth tokens, using each platform's REST API to fetch all open pull requests assigned to, created by, or requesting review from the authenticated user — normalizing them into a unified PRs table in Postgres with fields for platform, repo, PR number, title, status, draft flag, last_activity, review_state, and reviewer list. Compute a health score per PR by combining: days since last activity (staleness), whether all CI checks have passed, count of requested changes from reviewers, and whether the PR has unresolved review comments — color-coding each PR as healthy (green), attention needed (amber), or stalled (red). Implement smart reviewer nudging: for PRs where no reviewer has responded in 48 hours, generate a politely worded reminder message pre-populated using Claude that the user can edit and send as a PR comment via the platform API with one click, tracking nudge history per PR to avoid double-nudging. Display a daily focus queue showing 'Your top 3 PRs to move forward today' — prioritized by age, CI status, and reviewer activity — as actionable cards with deep links into each platform's PR view and a status refresh button. Add a weekly PR metrics email via Resend showing PRs merged this week, average time-to-merge, review turnaround time, and a summary of the code surface area touched across all platforms.", stack: ["Next.js", "GitHub API"], difficulty: "Intermediate", vibes: ["DevTools", "OSS"] },
  { title: "ListLasso", tagline: "Snip selectable items from any webpage to a CSV.", description: "Browser overlay turns rows on a page into structured spreadsheet data.", prompt: "Build a Chrome Manifest V3 extension with a page overlay that activates when the user clicks the extension icon, entering an 'example selection mode' where the user clicks two or three example items (rows, cards, or list elements) to establish a pattern. Analyze the clicked elements in the content script using DOM path analysis — computing the least-common ancestor and the minimal CSS selector that matches exactly those elements — then highlight all matching elements on the page as visual confirmation before the user confirms the pattern. For each matched element, recursively walk its child nodes to identify repeating sub-fields (text nodes, links, images, data attributes) and propose a column mapping: 'Name (h3.card-title)', 'Price (.price-tag)', 'URL (a[href])' shown in an editable column-name input panel. Extract all matched rows using the confirmed column mapping, displaying a paginated preview table in the extension popup with row count, and a 'Load more' button that scrolls the page if paginated content is detected by checking for next-page buttons using common selectors. Export the extracted data as a UTF-8 CSV downloaded via a Blob URL, or POST it to a configured webhook URL stored in chrome.storage.sync for direct integration with Airtable, Google Sheets, or any HTTP endpoint.", stack: ["Chrome MV3", "DOM"], difficulty: "Advanced", vibes: ["Scraping", "DataOps"] },
  { title: "DraftDoctor", tagline: "Resurrect old document drafts.", description: "Surfaces docs you haven't opened in 90 days that look incomplete.", prompt: "Build a Next.js web app that connects to Google Drive via OAuth and Notion via the Notion API, indexing all documents modified in the last 18 months; for Drive, use the files.list API to get DOCX and Google Docs files, and for Notion, use the search endpoint to list all pages the user has access to, storing title, last_modified_date, and URL in Postgres. Detect incompleteness signals: fetch the last 500 words of each document (using the Drive export endpoint for plain text or the Notion blocks.children.list endpoint) and pass to Claude with a prompt identifying signals of an unfinished document — TODOs, placeholder text like '[insert here]', open questions without answers, incomplete lists, or a missing conclusion section — returning incompleteness_type and confidence score as JSON. Score each document by incompleteness confidence × recency factor (documents 90-180 days old score higher than either very new or very old), sort by score descending, and bundle the top 10 into a weekly 'finish me' digest delivered via Resend with the document title, link, and specific incompleteness signals detected. Display a web dashboard showing all flagged documents organized by platform and incompleteness type, with a 'Dismiss — this is done' button removing a document from future scoring, and a 'Start writing' button that opens it directly. Add a Slack command /draft-doctor that returns the top 3 stale drafts for the requesting user from the last digest, making the nudge accessible without visiting the web UI.", stack: ["Drive API", "Claude"], difficulty: "Intermediate", vibes: ["Writing", "Cleanup"] },
  { title: "BookmarkBrain", tagline: "Bookmarks that summarize themselves.", description: "Save a URL, get an auto-summary + tags, search across them by meaning.", prompt: "Build a Next.js web app and companion Chrome Manifest V3 extension where clicking the extension icon on any page sends the URL and page title to a Next.js API route, which fetches the full page content using a server-side Playwright headless browser to handle JavaScript-rendered pages, summarizes it in 2-3 sentences using Claude, and generates 3-5 topic tags — storing everything in Postgres. Embed each bookmark's summary and tags using OpenAI text-embedding-3-small and store in pgvector so the web dashboard's search bar supports semantic queries like 'that article about async communication in remote teams' — returning the top 10 most relevant bookmarks ranked by cosine similarity. Display bookmarks in a masonry grid layout with the auto-generated summary visible below the title and auto-tags shown as colored chips; clicking any tag filters the view to matching bookmarks, and clicking any bookmark opens the URL in a new tab alongside the saved summary in a sidebar. Implement a reading queue mode where bookmarks marked 'read later' appear in a daily digest of 3-5 unread bookmarks sent via Resend email with summaries and a 'mark as read' link that updates status in Postgres via a signed URL. Add a public 'digital garden' mode that publishes a subset of bookmarks as a searchable public page for sharing curated reading lists, and a Pocket-compatible JSON export of all bookmarks.", stack: ["Next.js", "Embeddings"], difficulty: "Intermediate", vibes: ["Reading", "Personal KB"] },
  { title: "RoutePilot", tagline: "Personal weekly errand optimizer.", description: "Add stops, get the lowest-effort order with traffic-aware ETAs.", prompt: "Build a Next.js web app where users create a weekly errand list by adding stops (business name or address) with optional time windows (e.g., 'pharmacy: must be before 5pm'), duration estimates per stop, and a starting location stored in their profile in Postgres. Resolve each stop's latitude and longitude using the Google Geocoding API and fetch a distance matrix between all stop pairs using the Google Distance Matrix API with traffic_model: pessimistic for realistic ETA estimates, storing the matrix in Postgres for the week to avoid redundant API calls. Solve the Time-Windowed Traveling Salesman Problem using nearest-neighbor heuristic initialization followed by 2-opt edge-swap improvements, respecting time window constraints per stop and re-optimizing automatically when stops are added or removed. Display the optimized route on an embedded Google Maps API static map with numbered waypoints, a turn-by-turn summary listing each stop in order with estimated arrival time and time window compliance indicator, and total estimated driving time and distance. Export the route as a Google Maps multi-waypoint URL that opens in Google Maps or Apple Maps on mobile with all waypoints pre-loaded, and as a Google Calendar .ics file with each stop as a timed event including the business address and navigation link in the description.", stack: ["Next.js", "Google Maps"], difficulty: "Intermediate", vibes: ["Daily", "Optimization"] },
  { title: "DigitalDeclutter", tagline: "Audit your digital footprint and shrink it.", description: "Scan accounts you've forgotten and queue 'delete me' requests.", prompt: "Build a Next.js web app that connects to Gmail via OAuth and scans for account-related emails using the Gmail API search query 'subject:(welcome OR verify your email OR confirm your account OR account created)' looking back 5 years, then passes each email's subject, sender name, and sender domain to Claude to classify whether it represents a created account — extracting service name, website domain, and account email used. Cross-reference the discovered services against the HaveIBeenPwned API's breach database using each service's domain to flag which accounts have been involved in known data breaches, showing breach name, date, and compromised data types (password, email, DOB). Display a prioritized account list ordered by breach risk score (breached + old + forgotten = highest priority), with filter panels for 'breached only', 'no login in 2+ years', and 'service no longer operating' checked via DNS lookup of the domain. For each account, generate a GDPR/CCPA deletion request email using Claude with the correct legal language citing the applicable regulation based on the user's country, then draft it in Gmail via the API's drafts.create endpoint so the user can review and send in one click. Track the status of each deletion request (not started, email sent, confirmed deleted, no response) in Postgres and set 30-day follow-up reminders that queue a second more formal request via Resend if the first goes unanswered.", stack: ["HIBP API", "Gmail"], difficulty: "Advanced", vibes: ["Privacy", "Cleanup"] },
  { title: "ToastTrigger", tagline: "Web-page lifecycle webhooks.", description: "Get notified when a page changes, breaks, or matches a selector.", prompt: "Build a Next.js web app where users define monitoring rules by specifying a URL, a CSS selector for the element to watch, a condition type (content changed, element appeared, element disappeared, text matches regex, numeric value crossed threshold), and a check_interval in minutes stored in Postgres. Run a Vercel Cron job on a 1-minute tick that dispatches pending checks, using Playwright in a headless Chromium serverless function to visit each URL, wait for networkidle, evaluate the CSS selector, extract the element's textContent, and compare against the last-known state stored in Postgres. When a condition is triggered, fire an HMAC-signed POST webhook to the user's configured endpoint within 30 seconds of detection, with a JSON payload containing the URL, rule ID, condition type, previous value, new value, and a captured full-page screenshot URL stored in S3. Send an optional Resend email alert for rules configured with email notification, including an inline screenshot and a before/after text comparison. Build an admin dashboard showing all monitoring rules with last-check timestamp, current status, a 30-day trigger history chart per rule, and a webhook delivery log with HTTP status codes and response times for debugging failed deliveries.", stack: ["Next.js", "Playwright"], difficulty: "Intermediate", vibes: ["Monitoring", "DevTools"] },
  { title: "MealMath", tagline: "Macro tracker that's just a chat box.", description: "Type 'two slices of pizza', it logs macros — no scrolling food databases.", prompt: "Build a Next.js web app with a chat-first UI where users type natural-language food logs — 'I had a large iced coffee with oat milk and a blueberry muffin from Starbucks' — sent to Claude with a structured food-parsing prompt that extracts individual food items and quantity estimates, returning a JSON array with food_name, quantity, unit, and a USDA FoodData Central search query for each item. Query the USDA FoodData Central API's /foods/search endpoint for each extracted food item, selecting the best match by name similarity, and pull the per-100g macros (protein, carbohydrate, fat, calories, fiber) to compute totals for the logged quantity. Display each logged meal in a conversational chat format with an expandable macro breakdown panel showing computed protein/carbs/fat/calories for the meal and a running daily total bar chart that updates in real time as meals are added. Store all logs in Postgres (meals → meal_items → nutrition tables) tied to the user's account, and show a weekly macro dashboard with daily average intake vs. configurable targets displayed as Recharts bar and area charts. Add a HealthKit-compatible XML export for Apple Health import, and a weekly Resend email summary showing average daily intake, protein target hit rate, and a 3-meal highlight from the week.", stack: ["Next.js", "USDA API"], difficulty: "Beginner", vibes: ["Health", "Chat"] },
  { title: "SnoozeStudio", tagline: "Customizable alarm clock with smart snoozes.", description: "Snoozes get harder until you solve a captcha or scan a QR in the kitchen.", prompt: "Build a Progressive Web App with full offline support via a Service Worker that registers alarms using the Web Notifications API and BackgroundSync API (for Android Chrome), with a fallback Notification push from a Vercel endpoint for iOS Safari where background wake is unreliable. Implement progressive snooze friction: the first snooze is free (10 minutes); the second presents a 5-question mental arithmetic challenge (random 2-digit multiplication generated client-side); the third requires scanning a QR code displayed at a physical location (kitchen counter, front door) configured during setup — the QR URL contains a one-time token validated server-side to prevent screenshotting and cheating. Integrate Spotify via the Web API's playback control endpoints, letting users choose a wake-up playlist per alarm; the app sends a play request at alarm time and gradually increases volume from 20% to 100% over a configurable ramp-up duration using the /me/player/volume endpoint polled every 5 seconds. Store alarm history in IndexedDB synced to Postgres when online, tracking snooze count, friction type used, and time between alarm and final wake, visualized as a calendar heat map of easy-wake vs. snoozed days with monthly statistics. Add a smart wake window: set a 30-minute alarm range and use DeviceMotionEvent accelerometer data to detect light-sleep movement patterns and trigger the alarm during a light-sleep moment within that window.", stack: ["PWA", "Spotify"], difficulty: "Intermediate", vibes: ["Wellness", "Habits"] },
  { title: "DiffMate", tagline: "Side-by-side semantic diff for prose.", description: "Highlights tone, intent, and key fact changes — not just whitespace.", prompt: "Build a Next.js web app with a split-pane text editor where users paste an original document on the left and a revised version on the right, then click 'Analyze Diff' to run a character-level diff using the diff library and separately pass both versions to Claude with a semantic analysis prompt. The Claude prompt produces a structured JSON: a list of meaning-level changes each with a type (tone_shift, fact_change, nuance_added, nuance_removed, intent_changed, key_information_added), the original phrase, the new phrase, and a one-sentence explanation of why the change matters semantically. Render the diff in a color-coded highlighted view where literal whitespace changes are gray, tone changes are blue, fact changes are red, nuance additions are green, and key information changes are orange — with a legend and a summary count per change type in a sidebar metrics panel. Let users click any highlighted segment to open a tooltip showing the semantic change type and explanation from Claude, and filter the view to show only specific change types (e.g., 'show only fact changes') using a checkbox filter row. Export the annotated diff as a DOCX document using the docx library with comment annotations in Word's built-in comment format for each semantic change, suitable for editorial review in Word or Google Docs.", stack: ["Next.js", "Claude"], difficulty: "Intermediate", vibes: ["Writing", "AI"] },
  { title: "PenPalPlus", tagline: "Auto-translate snail-mail letters.", description: "Photograph a handwritten foreign letter, get a translated readable version.", prompt: "Build a Next.js web app optimized for mobile where users photograph a handwritten letter (supporting English, Spanish, French, German, Japanese, Chinese, Korean, Portuguese, Italian, and Russian scripts) and upload it via drag-and-drop or mobile camera input; process the image through Google Vision API's DOCUMENT_TEXT_DETECTION feature, which handles handwriting significantly better than OCR-only tools, extracting full text with per-word confidence scores. Post-process the OCR output by passing it to Claude with a prompt that identifies low-confidence words flagged by Google Vision and infers the correct word from surrounding context, producing a cleaned transcription with uncertain words marked in brackets. Detect the source language automatically using the lingua-rs library server-side or the Google Language Detection API, then translate the cleaned transcription using DeepL's neural translation API, returning both a literal translation and a natural-sounding idiomatic translation for languages where idioms differ significantly from literal meaning. Display the result in a three-column layout: original photo on the left with pinch-to-zoom, transcribed text in the middle (editable so the user can correct OCR errors before re-translating), and the translation on the right with a highlighted line connecting the photo region to its corresponding transcription when the user taps a paragraph. Add a pen-pal reply feature where users compose a reply in their native language and the app generates a handwriting-style font rendering of the translated reply using a handwriting CSS font, downloadable as a printable letter PDF.", stack: ["Tesseract.js", "DeepL"], difficulty: "Intermediate", vibes: ["Travel", "Personal"] },
  { title: "SiteShield", tagline: "Personal uptime monitoring for indie sites.", description: "Free tier hits all your sites every minute and pings you on issues.", prompt: "Build a Next.js web app where indie developers add website URLs to a monitoring list, configuring per-URL check frequency (1 min, 5 min, 15 min), expected HTTP status code, an optional response body substring assertion, and a TLS certificate expiry check; store all monitor configurations in Postgres. Deploy check runners as Vercel Edge Functions on Vercel Cron Jobs — for each URL, make authenticated fetch requests from 3 different geographic regions (US East, EU West, Asia Pacific), recording HTTP status, response time in ms, and whether the body assertion matches, storing results in a check_results table. When a URL fails 2 consecutive checks from at least 2 regions (filtering false positives from single-region blips), trigger a Twilio SMS alert and a Resend email alert within 60 seconds, including the URL, last successful check timestamp, current error type, and a link to the incident timeline in the dashboard. Display an uptime dashboard with per-URL uptime % over configurable periods (24h, 7d, 30d), response time trend charts per URL and region, and a public status page at status.yourdomain.com regenerated via on-demand ISR every 60 seconds. Add SSL certificate expiry monitoring that sends Resend email reminders at 30, 14, and 7 days before expiry with the certificate's expiry date and a recommended renewal checklist link.", stack: ["Edge Fns", "Twilio"], difficulty: "Intermediate", vibes: ["DevTools", "Reliability"] },
  { title: "DoneList", tagline: "Today-only todo with friction to add tomorrows.", description: "Anti-procrastination by capping to 3 tasks/day with carryover penalty.", prompt: "Build a Next.js web app with an ultra-minimal single-screen interface showing exactly 3 task slots for today, each with a text input and a large circular complete button; tasks are stored in Postgres keyed by user + date, and new tasks can only be added to the next day's list once all 3 for today are filled — preventing endless task accumulation. Implement a gamified carryover penalty: any incomplete task that carries over to tomorrow automatically increments a visible 'debt counter', and the user must mark a 3-word-minimum reason for the carry before it's allowed, creating a brief moment of reflection on the root cause of the delay. Build a lifetime 'done streak' visualization as a GitHub-style contribution heatmap calendar where each day is colored by completion rate (all 3 done = deep green, 2/3 = medium green, 1/3 = light green, 0/3 = gray), shown below the task list so users can see long-term completion patterns at a glance. Generate a weekly completion report via Resend sent every Monday showing last week's completion rate, most common task categories inferred by Claude from task text patterns, accumulated debt counter, and a motivational summary in a concise, non-condescending voice. Add a 'today's intention' prompt appearing when the user first opens the app each day, asking for one sentence describing their top priority — stored in Postgres and shown as a reminder at the top of the screen until at least one task is marked complete.", stack: ["Next.js"], difficulty: "Beginner", vibes: ["Focus", "Habits"] },
  { title: "DateDuel", tagline: "Compare two dates across timezones, history, and weather.", description: "Useful for trip planners, historians, and curious nerds.", prompt: "Build a Next.js web app where users pick two arbitrary dates using a calendar date picker and click 'Compare' to initiate a multi-API data fetch: query the Visual Crossing Weather API's historical weather endpoint for both dates to get temperature, precipitation, and conditions for a configurable location stored in the user profile, defaulting to the user's detected geolocation. Fetch historically significant events for both dates from the Wikipedia API by querying 'Events on [Month Day]' and filtering to a curated list of event types (world news, scientific discoveries, cultural milestones), then use Claude to write a 3-sentence comparison narrative about what was happening in the world on each date and how the two moments relate. Compute and display: the absolute time difference in years, months, days, hours, and minutes; the day of the week for each date; sunrise and sunset times using the sunrise-sunset.org API for the user's location; and whether a full moon occurred within 2 days of either date using the SunCalc library. Add a 'personal events' feature where users register their own dates (birthdays, anniversaries, life milestones) stored in Postgres, and when a compared date matches a personal event it's highlighted with a special callout card and a notes field for adding memories. Present all results in a split layout with each date in a column, using a custom SVG timeline illustration showing the two dates as points on a historical continuum with major world events marked between them for scale.", stack: ["Next.js", "Visual Crossing"], difficulty: "Beginner", vibes: ["Calendar", "Trivia"] },
  { title: "PaperTrail", tagline: "Document version control for non-devs.", description: "Drag DOCX in, get GitHub-like history with friendly diffs.", prompt: "Build a Tauri desktop application with a Rust backend that watches user-configured folders for changes to DOCX, Markdown, PDF, and TXT files using the notify Rust crate, triggering an automatic snapshot within 5 seconds of any detected modification. For each captured snapshot, extract the document's text content using the docx crate (DOCX), pulldown-cmark (Markdown), or pdf-extract (PDF), compute a SHA-256 hash to detect true content changes vs. metadata touches, and store snapshots in local SQLite via rusqlite with fields for file path, version number, capture timestamp, and compressed text content using flate2 gzip compression. Generate a character-level unified diff between any two versions using the similar Rust crate, display it in the frontend as a split-pane view with added text in green and removed text in red, rendered with paragraph context so the user understands what section each change belongs to without reading the whole document. Implement one-click restore: selecting any prior version from the version history sidebar and clicking 'Restore' writes the stored snapshot content back to the original file path via the Tauri FS API, with a confirmation dialog and a 'create backup of current version first' checkbox. Add folder-level version statistics displayed as a dashboard: most-changed files this month, average edit frequency per file, and a stacked area chart of total document edits per day over the last 90 days, helping users understand their writing and revision patterns.", stack: ["Tauri", "Diff"], difficulty: "Intermediate", vibes: ["Writing", "Version"] },
  { title: "SilenceSeeker", tagline: "Trim ums and silences from podcast files.", description: "Local audio scrubber that strips silence and filler words.", prompt: "Build a Tauri desktop application where users open audio files (MP3, WAV, FLAC, M4A) via a file picker, then trigger a two-stage processing pipeline: first run silence detection using an FFmpeg static binary bundled as a Tauri sidecar, applying a configurable silence threshold in dB and minimum silence duration to generate a list of silence intervals with start and end timestamps. In parallel, run the audio through Whisper.cpp (compiled as a Rust static library via the whisper-rs crate) using the small or medium model to produce a word-level transcript with per-word timestamps, detecting filler words (um, uh, like, you know, basically, literally, right?) from a configurable filler word list stored in a settings JSON file. Merge the silence intervals and filler word timestamps into a unified cut list — an array of {start_ms, end_ms, type: 'silence' | 'filler'} intervals — and display it as an interactive waveform visualization in the frontend using WaveSurfer.js where silence segments are overlaid in red and filler words in orange, allowing the user to click any segment to preview how the cut would sound before committing. Present a configurable auto-edit mode: the user sets minimum silence duration to cut (e.g., silences over 400ms), whether to remove fillers, and a 'natural pause preservation' setting that keeps silences shorter than 200ms for sentence-level rhythm. Process the cut list through an FFmpeg filter_complex command specifying trim and concat operations, producing a cleaned audio file exported to a user-specified path with a summary showing original duration, new duration, and percentage of audio removed.", stack: ["Tauri", "Whisper.cpp"], difficulty: "Advanced", vibes: ["Audio", "Podcasts"] },
  { title: "KeepKey", tagline: "Local password manager with passphrase generator.", description: "No cloud, no auto-fill nonsense — just your encrypted vault.", prompt: "Build a Tauri desktop application with a Rust backend where all passwords are stored in a single encrypted SQLite database file encrypted at rest using libsodium's secretstream_xchacha20poly1305 cipher via the sodiumoxide crate, with the master key derived from the user's master passphrase using Argon2id key derivation (configurable memory cost 64MB minimum to resist brute-force). Implement a diceware-based passphrase generator that reads from the EFF Large Wordlist (bundled as a text file, 7776 words), randomly selects 5-8 words using the OS secure random number generator via Rust's rand::rngs::OsRng, displays them as a readable phrase, and computes the entropy in bits shown alongside the passphrase. Build a password vault UI with a searchable list of entries (site, username, notes, created date, last modified), an entry editor with a password strength meter using the zxcvbn algorithm, and a password generator for arbitrary character-class combinations (length, uppercase, lowercase, digits, symbols) in addition to the diceware option. Implement clipboard auto-clear: when the user clicks 'Copy Password', start a 30-second countdown timer visible in the status bar, then overwrite the clipboard with a random 32-character string via the arboard crate when the timer expires — preventing accidental clipboard leaks if the user switches apps. Add a TOTP (Time-based One-Time Password) field per entry that generates 6-digit codes using the totp-rs crate with a live countdown indicator, making KeepKey a combined password and authenticator app with zero cloud dependencies.", stack: ["Tauri", "libsodium"], difficulty: "Advanced", vibes: ["Security", "Local"] },
];

const SOCIAL: Omit<Project, "id" | "category">[] = [
  { title: "PartyParrot Rooms", tagline: "Drop-in rooms with ambient cohabitation.", description: "Open a tab, join a 'study room', see avatars working alongside you.", prompt: "Build a Next.js web app where users join named virtual rooms (Quiet Library, Focus Forest, Cafe Mode) and each participant's browser sends a presence pulse via a Liveblocks room containing activity state detected by analyzing MediaRecorder audio amplitude every 2 seconds — if amplitude is above a typing threshold the avatar shows 'active', otherwise 'resting', without ever sending video. Design a custom SVG avatar system where each user selects an animal avatar and color scheme stored in their profile, with avatars gently animating (bobbing, blinking) using CSS keyframe animations that subtly shift for active vs. resting state so roommates can see engagement without a camera. Implement ambient audio options per room — lo-fi coffee shop, rain on windows, soft library ambiance — served as looping MP3 streams from S3 with per-user volume control and the host's audio choice applying to the whole room. Add a synced Pomodoro timer that any participant can start: a 25-minute focus and 5-minute break cycle is broadcast to the room via Liveblocks presence, showing a shared countdown in the corner of all participants' screens with a gentle audio chime at each transition. Let rooms have a max capacity of 8 and a public/private toggle; public rooms are listed in a lobby directory updated every 30 seconds showing room name, occupant count, activity level, and ambient audio type.", stack: ["WebRTC", "Liveblocks"], difficulty: "Advanced", vibes: ["Coworking", "Presence"] },
  { title: "VinylVibes", tagline: "Listen-along sessions with text reactions.", description: "Spotify-synced listening parties with low-latency reactions.", prompt: "Build a Next.js web app where a room host connects their Spotify account via OAuth and drives playback for up to 50 listeners; the host's Spotify client plays via the Spotify Web Playback SDK while a Vercel cron polls the Spotify /me/player endpoint every 2 seconds and publishes the current track URI, progress_ms, and is_playing state to a Liveblocks room via broadcast channel. Listeners subscribe to the Liveblocks broadcast and seek their own Spotify Web Playback SDK instance to match the host's current track and progress_ms within a 500ms drift tolerance, re-syncing automatically on each cron tick to maintain tight alignment. Build a reaction layer rendered as an HTML canvas overlay on the album art where users type emoji or short text reactions that animate upward in a floating arc using requestAnimationFrame physics, disappearing after 3 seconds — reaction events are published to Liveblocks presence so all participants see reactions simultaneously. Show a live listener sidebar displaying each participant's avatar and a 'host vibe' indicator showing whether the host has been playing the current track from the beginning or jumped in mid-song. Add a queue suggestion system where listeners can submit upcoming track suggestions that the host sees in a pending queue, accepting or rejecting each with a single tap that adds the approved track to their Spotify queue via the /me/player/queue endpoint.", stack: ["Spotify API", "Liveblocks"], difficulty: "Intermediate", vibes: ["Music", "Social"] },
  { title: "CityChat", tagline: "Geo-fenced chatrooms by neighborhood.", description: "Open the app downtown, join the chat with people within 1km right now.", prompt: "Build a Next.js Progressive Web App that requests the browser's Geolocation API permission on first launch, hashing the user's lat/long to a 1km² H3 hexagonal grid cell using the h3-js library, then assigning the user to the corresponding neighborhood chat room stored in Postgres as chat_rooms keyed by H3 cell index with a 24-hour TTL, auto-expiring all messages and the room at end of each UTC day. Implement real-time messaging via Supabase Realtime scoped to the room's H3 cell ID, where each message is tagged with a pseudonymous handle (animal + adjective combo generated on first visit and stored in localStorage), message text, and timestamp. Run each incoming message through the Perspective API's toxicityScore endpoint and auto-hide messages scoring above 0.85, flagging them for a community moderator review queue where self-nominated local moderators can approve or remove flagged content. Display an animated city map using Mapbox GL JS showing the user's current H3 cell highlighted, with neighboring cells showing a pulse animation when they have active messages in the last 15 minutes. Generate a daily 'neighborhood digest' — the top 5 most-upvoted messages from the last 24 hours — rendered as a public shareable page at /digest/{h3cell}/{date} with anonymized handles for passive participants who want to follow local conversations without joining actively.", stack: ["Next.js", "Geofire", "WebSockets"], difficulty: "Advanced", vibes: ["Local", "Anonymous"] },
  { title: "PromptParty", tagline: "Multiplayer Cards-Against-Humanity for AI prompts.", description: "Players answer prompts and rate each other's AI generations.", prompt: "Build a Next.js multiplayer party game backed by Liveblocks where 3-8 players join a game room via shareable code; the game master submits a creative theme like 'show loneliness in a futuristic city' and each other player has 90 seconds to craft an image generation prompt in a text input. Submit all player prompts to Replicate's Stable Diffusion XL endpoint simultaneously via a server-side API route that fans out the requests, stores prompt and resulting image URL in Postgres, and emits a game_round_reveal event to the Liveblocks room with all images and their anonymous authorship when all results are ready. Reveal all generated images simultaneously to all players in a gallery grid — still anonymous, no player names attached — and open a 60-second voting phase where each player votes for their favorite with a heart button, with live vote-count updates streamed via Liveblocks presence and self-voting blocked by comparing voter session ID against submitted prompts server-side. After votes are tallied, reveal which player submitted which prompt with a dramatic animation, awarding points for votes received and a bonus for the most creative prompt as voted by the group. Display a leaderboard across rounds with player avatars, cumulative point totals, and a 'hall of fame' of the highest-scored images from all rounds in a scrollable gallery with the winning prompt text below each image.", stack: ["Liveblocks", "Replicate"], difficulty: "Intermediate", vibes: ["Game", "AI"] },
  { title: "ReadingRoom", tagline: "Real-time book-club annotations.", description: "Sync EPUBs across friends, see their highlights in real time.", prompt: "Build a Next.js web app using EPUB.js to render uploaded EPUB files in a browser reader where users form private reading groups of 2-10 members, each uploading the same EPUB stored client-side in IndexedDB keyed by the book's ISBN or title hash to avoid storing copyrighted content server-side. Implement real-time highlights and annotations using Yjs CRDTs with a y-websocket provider: when a user highlights a passage, the highlight range as an EPUB CFI string (which uniquely identifies any text range within an EPUB) is stored in a Yjs shared document along with the user's display name and timestamp — all connected readers see the highlight appear within 100ms. Build a threaded comment system per highlighted passage: clicking any highlight opens a sidebar panel showing the thread of comments, where any group member can reply in real time via Yjs without a server round-trip. Add a 'current page' presence feature where each reader's reading position is shared via Yjs awareness, showing a live 'where is everyone reading?' sidebar with member names, chapter position, and a 'sync to my page' button that jumps other members to the requesting reader's current position. Generate a weekly reading report from Postgres session logs showing each member's reading progress percentage, number of highlights, comment count, and a word-cloud of the most-highlighted passages, emailed via Resend to all group members on a schedule the group configures.", stack: ["EPUB.js", "Yjs"], difficulty: "Advanced", vibes: ["Books", "Async"] },
  { title: "WaveCheck", tagline: "Group surf-condition voting.", description: "Local surfers vote on conditions every dawn — beach map updates live.", prompt: "Build a Next.js PWA where verified local surfers submit surf condition reports for specific named breaks stored in a surf_breaks table in Postgres with lat/long coordinates; each condition report captures wave height, wave period, crowd level, swell direction, wind direction, and the reporter's confidence level (just a guess vs. in the water now). Implement verifier trust scoring: each reporter earns trust points when their conditions match subsequent reports within a 2-hour window at the same break, incrementing a reputation_score in Postgres; reports from high-reputation locals are weighted more in the aggregate condition score using a weighted mean calculation. Display an interactive Mapbox GL JS map with a break marker per surf break, colored by aggregate condition score with a confidence circle whose radius scales with the number of recent reports and whose opacity reflects recency — reports older than 3 hours fade to gray. Add a crowd-sourced break directory where any user can suggest a new break with a name and GPS pin, moderated by the existing community via upvotes; once a suggested break reaches 5 upvotes from verified surfers in the same geographic region, it's added to the official break list. Send push notifications via the Web Push API to subscribers of specific breaks when new condition reports arrive between 5am and 8am local time, including a 1-sentence condition summary, wave height, and crowd level.", stack: ["Next.js", "Mapbox"], difficulty: "Intermediate", vibes: ["Sports", "Community"] },
  { title: "PenPalPlanet", tagline: "Slow social: one letter a day, anonymous.", description: "You're matched with one stranger; one written exchange daily for two weeks.", prompt: "Build a Next.js web app where users create an interest profile and opt into daily matching, paired once per week with a single stranger using cosine similarity on interest embeddings stored in pgvector; each matched pair has exactly 14 days of daily exchange where both users must write and submit their letter for the day before midnight UTC for the next day's letters to unlock. Impose a 500-word maximum and 50-word minimum per letter enforced client-side with a live character counter; after submission, each letter is encrypted client-side using the Web Crypto API's AES-GCM algorithm with a shared key derived from both users' session tokens so only the matched pair can read each other's letters. After 14 days, both users are invited to an 'ending ritual': a final prompt asking them to write one thing they'll remember about their pen pal, delivered simultaneously to both with the option to exchange real first names and a social link if both opt in. Store all letter metadata (sent_at, word_count, day_number) in Postgres for platform analytics while keeping letter content encrypted at rest, and generate a 'conversation keepsake' PDF export for both users after day 14 using PDFKit containing all letters in a formatted book layout. Display a progress indicator showing the current day of the exchange and a 'days remaining' countdown visible on each user's dashboard, with a Resend email notification each morning reminding both users it's time to write.", stack: ["Next.js", "Postgres"], difficulty: "Beginner", vibes: ["Wholesome", "Anti-Social"] },
  { title: "TripeTribe", tagline: "Ratings for hole-in-the-wall food spots only.", description: "Strict rule: chains banned, queues over 1 hour banned. Just real gems.", prompt: "Build a Next.js food-discovery app where users can only post about restaurants that are independently owned (verified by checking against a chain-restaurant blocklist maintained by admins), visited in person (verified by requiring a geo-tagged photo within 100m of the business), and don't have a reservation waitlist longer than 30 minutes — with community flags for misleading reports. Implement reputation-weighted reviews: each user starts with a base reputation of 1.0, which increases when their recommendations receive subsequent positive reviews from others and decreases when their posts are flagged as inaccurate — so reviews from long-standing trusted community members carry more weight in the aggregate score. Build a discovery map using Mapbox GL JS where each business pin is sized by reputation-weighted review volume and colored by cuisine category, with a filter panel for cuisine, neighborhood, reputation threshold, and max price range. Generate walking-distance city tour itineraries: given a starting location and 3 hours of time, select the top-rated nearby places in a logical walking order using the Google Distance Matrix API and present a printed tour PDF using PDFKit with each stop's name, address, 3-sentence community description, and a neighborhood map. Add a 'locals only' trust tier: users who have 50+ accepted reviews from a single city's neighborhood earn a 'local' badge that allows them to designate posts as 'hidden gem — please keep quiet', suppressing those posts from the main feed and sharing them only in a private 'local secrets' feed visible to other locals in the same neighborhood.", stack: ["Next.js", "Supabase"], difficulty: "Intermediate", vibes: ["Food", "Niche"] },
  { title: "RoomieRules", tagline: "Co-living chore + bill split with social proof.", description: "Streaks for cleaning, public badges, and conflict resolution prompts.", prompt: "Build a Next.js household management app where roommates create a shared household by invitation, with a fair chore rotation algorithm that accounts for member preferences (stored as ranked preference lists) and availability patterns using a weighted round-robin assignment that balances total chore burden across a rolling 4-week window. Implement bill splitting with multiple split modes — equal, percentage-based, or item-based — using the Stripe Connect API to facilitate peer-to-peer settlement between roommates who link their bank accounts, tracking all expenses and settlements in Postgres with settlement status. Add gamification: each completed chore earns streak badges (3-in-a-row, 7-in-a-row, 30-day) displayed on the household profile, and a weekly 'house meeting agenda' is auto-generated by Claude using the week's complaint flags, unsettled bills, and pending chore reassignment requests, sent as a Slack message or Resend email on Sunday evening. Generate a monthly household health report showing total bills paid, chore completion rate per member, longest streaks, and a conflict resolution summary if any items were flagged during the month — all shareable as a PDF. Build a chore fairness dashboard showing each member's current and projected load as a bar chart, with a one-click 'swap this chore' request system that proposes an equivalent-duration chore swap to the target roommate via an in-app notification.", stack: ["Next.js", "Stripe"], difficulty: "Intermediate", vibes: ["Living", "Community"] },
  { title: "SilentSocial", tagline: "Like-only social — no replies allowed.", description: "Post photos, see hearts. That's the whole product. It's calmer.", prompt: "Build a Next.js social network where users upload square photos (max 1 per day, enforced by checking last_posted_at in Postgres against the current UTC date) with all images resized to 800x800 WebP using sharp and stored in S3, with the user adding only a title — no caption, no hashtags, no location tags. Implement a pure reverse-chronological feed with absolutely zero algorithmic ranking: the feed shows the last 48 hours of posts from followed accounts in strict descending order of created_at, with no 'because you liked' sections, no suggested accounts, and no external share buttons anywhere in the UI. Provide exactly one interaction — a heart button that increments a like_count in Postgres — with no comments, no reposts, no DMs, no mentions, and no reply threads; clicking a photo opens it full-screen with like count and nothing else. Implement a 'rest mode': after the user has scrolled through all new posts from the last 24 hours from followed accounts, the feed shows a gentle plant illustration and the message 'You're all caught up — you saw everything' rather than recycling content or padding with suggested accounts. Add an optional weekly digest email via Resend showing each user's top photo (most hearts this week), their total hearts received, and the photos from their followed accounts that received the most hearts — designed as a simple email that encourages opening the app rather than replacing it.", stack: ["Next.js", "Supabase"], difficulty: "Beginner", vibes: ["Anti-Social", "Photo"] },
  { title: "QuestQueue", tagline: "Find adventure buddies in your city.", description: "Post a quest (hike, board game, museum), match with willing strangers.", prompt: "Build a Next.js web app where users post 'quests' — one-time shared activities with a specific date, time, location, and capacity (e.g., 'Urban sketching at the park, Sunday 10am, 3 spots') with a vibe level from casual to serious, stored in a quests table in Postgres with a PostGIS point for the location. Implement a discovery feed where users browse quests within a configurable radius using PostGIS ST_DWithin, filtered by category (outdoor, food, arts, sports, learning, random), vibe level, date, and capacity — sorted by a score combining proximity to the user's home neighborhood and number of open spots remaining. Allow users to request to join any quest; the quest creator reviews the requester's profile (photos, bio, past quest participation history) and accepts or declines — accepted participants receive a calendar invite and a shared private chat room via Supabase Realtime that activates 24 hours before the quest. After the quest date passes, prompt all participants to leave a 'public vibe' review of each other using a 3-emoji scale (would quest with again, fine, not my vibe), displayed as an aggregate vibe score on each user's profile — 5 positive vibes from 5 different quests unlock a 'trusted quester' badge. Implement optional Stripe payment for paid quests (a ticketed art workshop or cooking class) using Stripe Checkout with automatic refunds via the Stripe API if the creator cancels fewer than 24 hours before the event.", stack: ["Next.js", "Stripe"], difficulty: "Intermediate", vibes: ["IRL", "Events"] },
  { title: "BabblerBot", tagline: "Conversation-starter prompts for couples.", description: "Daily randomized question for partners — ranging from light to deep.", prompt: "Build a Next.js web app where two users link their accounts as a 'pair' using a shareable invite code stored in Postgres, and both receive the same daily question at 7pm in their configured timezone via a Resend email or Web Push notification, drawn from a curated 500+ question database spanning 5 depth levels (light, curious, reflective, vulnerable, profound) organized as a slowly escalating deck. Implement the mutual-reveal mechanic: both partners submit their answers independently — invisible to the other until both have answered — with the app showing a 'Waiting for [partner] to answer' placeholder and optionally sending a gentle Resend nudge after 12 hours if the partner hasn't responded. After both answers are revealed simultaneously (a Postgres function unlocks visibility when both answered_at fields are set), display them side-by-side with an emoji reaction row beneath each answer visible in real time via Supabase Realtime. Track vulnerability depth progression over time: as the couple answers questions at each depth level, the app gradually unlocks deeper questions while occasionally revisiting lighter topics for playfulness, showing a visual 'depth tree' of their conversation history as a branching illustration. Generate a monthly 'couple's digest' PDF using PDFKit — a keepsake document of all questions and answers from the month, formatted as a booklet with a cover photo from each partner's profile, downloadable as a memento and printable as a gift.", stack: ["Next.js", "PostHog"], difficulty: "Beginner", vibes: ["Relationships"] },
  { title: "FoundFamily", tagline: "Connect adult adoptees to support communities.", description: "Private spaces for niche identity groups, vetted onboarding.", prompt: "Build a Next.js community platform where each community (adult adoptees, third-culture kids, recovery communities, etc.) has a custom application form of 2-5 open-ended questions set by the founding members, and applicants are reviewed by a moderation panel of 3 existing members who each independently rate the application — admission requires 2 of 3 approvals within 72 hours. Implement anonymous-by-default identity: all community members have a randomly generated pseudonymous handle from a curated gentle noun list, with their real profile only visible to themselves and moderators who need it for safety concerns — members can optionally reveal their real first name per community. Build a rich post type system with Story (long-form markdown), Ask (question with threaded answers), Milestone (achievements with celebratory emoji reactions), and Resources (links to counselors, legal orgs, community organizations) — each post type has a different card design in the feed. Implement multi-layer moderation: automated Perspective API toxicity filtering on submission, community member flagging with a 3-strike system, moderator 'soft remove' that hides a post from the feed while leaving it accessible to the author with an explanation, and a 'trauma-sensitive flag' that adds a content warning overlay before the content is visible. Store all moderation actions in a full audit log in Postgres — accessible to community founders and the platform trust-and-safety team — ensuring accountability and the ability to review appeals.", stack: ["Next.js", "Postgres"], difficulty: "Advanced", vibes: ["Community", "Trust"] },
  { title: "BackchannelBuddy", tagline: "Private group chat per Twitter thread.", description: "Browser overlay that opens a private group chat tied to any tweet.", prompt: "Build a Chrome Manifest V3 extension that adds a subtle icon to every tweet on twitter.com via a MutationObserver content script; clicking the icon opens a floating chat panel creating or joining a private group chat scoped to that tweet's canonical URL. Use the browser's crypto.subtle API to generate a room key derived from the tweet's URL using HKDF, and encrypt all messages client-side before sending them to a WebSocket server — the server only sees encrypted blobs, never plaintext, implementing a zero-knowledge architecture for message content. Allow users to invite others to a backchannel room by sharing the tweet's URL — any other BackchannelBuddy user who opens the same tweet automatically sees and can join the existing room, since the room ID is derived deterministically from the tweet URL. Implement ephemeral rooms with a 72-hour message TTL: all messages are automatically deleted from the server's Redis store after 72 hours, with no persistent storage of message content — only a message_count per room stored in Postgres for anti-spam rate limiting. Add a presence indicator showing how many BackchannelBuddy users are currently viewing the same tweet, displayed as a subtle badge on the extension icon overlay so users know when a backchannel is active before clicking.", stack: ["Chrome MV3", "WebSockets"], difficulty: "Advanced", vibes: ["Side-Channel"] },
  { title: "VolunteerVerse", tagline: "Match local nonprofits with skilled volunteers.", description: "Post a 2-hour skill, get matched to a need within 24 hours.", prompt: "Build a Next.js marketplace where volunteers post micro-help offers specifying a skill (Figma design review, React debugging, grant writing, financial modeling, SEO audit), an available 2-hour block via Google Calendar OAuth, and a description of what they can help with in a single session — stored in Postgres with a PostGIS location for optional in-person sessions. Nonprofits create verified organization profiles requiring a 501c3 EIN checked against the IRS Exempt Organizations Business Master File API, then browse available skill offers filtered by category, remote/local, availability date, and skill keyword search. Implement a matching algorithm that ranks volunteer offers by relevance to the nonprofit's posted need using cosine similarity via pgvector between the need description and offer description embeddings, presenting the top 5 matches with a 'Request session' button. After a confirmed session (both parties mark it complete), send automated thank-you emails via Resend with a personalized message generated by Claude incorporating the skill type, organization name, and the help provided, and award the volunteer a verified skill badge for their profile. Track platform impact metrics on a public dashboard: total volunteer hours facilitated, nonprofit categories served, skills most frequently offered vs. most requested, and a map of sessions by city — generating a monthly impact report PDF for volunteer organizations to share with their own donors.", stack: ["Next.js", "Stripe Connect"], difficulty: "Intermediate", vibes: ["Civic", "Match"] },
  { title: "MoodMingle", tagline: "Anonymous voice rooms by current emotion.", description: "Pick your mood, drop into a 4-person voice room with similar vibes.", prompt: "Build a Next.js Progressive Web App where users select their current emotion from a palette (lonely, anxious, excited, grateful, frustrated, melancholy, content, silly) and are anonymously matched into a 4-person voice room with other users who selected similar or complementary emotions — determined by a configurable compatibility matrix stored in Postgres. Implement WebRTC voice using LiveKit with a server-side SFU so audio is relayed server-side rather than peer-to-peer, ensuring all participants remain anonymous with no IP address exposure; each user is represented by an animated emoji-face avatar that pulses with their voice amplitude detected via the Web Audio API's AnalyserNode. Set a hard room time limit of 12 minutes enforced with a visible countdown timer for all participants; when time expires, all audio connections close and users are individually asked to rate whether they felt better, worse, or the same — aggregated as anonymous wellbeing data. Enforce anonymity: no persistent user accounts (rooms are joined with a session-only pseudonymous handle), no text chat, no profile photos, and no reconnection to the same participants — each app entry creates a fresh anonymous session. Add a daily capacity system: each user can enter a maximum of 3 rooms per day tracked by a hashed device fingerprint in Postgres, and gently surface a crisis resource link when a user opens the app more than 5 times in a day.", stack: ["WebRTC", "Liveblocks"], difficulty: "Advanced", vibes: ["Voice", "Anonymous"] },
  { title: "GameNightGenie", tagline: "Find board-game friends by collection overlap.", description: "BoardGameGeek profile + city -> matched groups.", prompt: "Build a Next.js web app where users connect their BoardGameGeek account via their BGG username, fetching their game collection using the BGG XML API's /xmlapi2/collection endpoint and importing all owned games with ratings into a Postgres user_collections table — storing BGG game ID, name, year, weight, and user rating for each game. Compute friend compatibility using Jaccard similarity: overlap of owned games ÷ union of owned games, supplemented by a playstyle compatibility score based on the distribution of game weight ratings (do both players prefer heavy euros or light gateway games?). Build a matchmaking view where users browse potential game night partners in their city sorted by Jaccard similarity, each showing a preview of the top 3 shared games with their BGG weight and category, and a 'propose game night' button. When two users confirm a game night, use GPT-4o to analyze both players' collections and suggest the top 3 games for the evening based on number of players, estimated duration from BGG data, average ratings from both collections, and the session's mood tag (competitive, social, strategic). After the game night date passes, prompt both users to log which game they played, who won, and a 1-5 fun rating — this data feeds back into a personal 'win rate' and 'games played' profile and improves future matchmaking by weighting games the user has actually played and enjoyed.", stack: ["BGG API", "Next.js"], difficulty: "Intermediate", vibes: ["Tabletop", "IRL"] },
  { title: "BridgeBuddy", tagline: "Find a language partner with reciprocal needs.", description: "I learn Japanese, you learn Spanish — match on perfect mutual exchange.", prompt: "Build a Next.js web app where users create a language exchange profile specifying their native language, the language they're learning, their current CEFR level (A1 through C1), available timezone slots for calls, preferred topics (food, travel, business, pop culture), and a short audio intro stored in S3. Match users with optimal exchange partners where Person A learns what Person B teaches and vice versa, using a compatibility score combining level complementarity, topic overlap, timezone overlap score, and time since last match refresh to prevent the same matches appearing repeatedly. Build a 30-minute structured call interface using LiveKit WebRTC with a shared timer: the first 15 minutes are in Person A's learning language, the second 15 minutes automatically flip to Person B's learning language with a prominent language flag swap animation. During each call, show a shared cue-card panel using Liveblocks where both participants can add vocabulary words, phrases, or corrections in real time — saved to a shared session vocabulary log in Postgres. After each session, both users rate each other on teaching clarity, patience, and fun; the cue-card vocabulary from the session is added to each learner's personal flashcard deck reviewed via a spaced repetition system built into the dashboard using SM-2 scheduling.", stack: ["Next.js", "WebRTC"], difficulty: "Intermediate", vibes: ["Language"] },
  { title: "FocusFleet", tagline: "Body-doubling rooms for ADHD-friendly work.", description: "Silent rooms with optional Pomodoro and a 'wave at me' button.", prompt: "Build a Next.js PWA with a room browser where users join a named silent focus room (Deep Work, Homework, Light Tasks, Creative) by clicking a room card showing current occupant count and a live activity pulse — with low-bandwidth WebRTC presence using LiveKit's data channel (no audio or video, just presence metadata) where each participant sends a heartbeat every 5 seconds containing their activity state and optionally a 50-character current task description visible in the participant list. Add an optional synced Pomodoro timer: the room host can initiate a 25-minute focus and 5-minute break cycle broadcast to all participants via Liveblocks presence, with a shared countdown in the corner and a gentle notification chime at each transition using the browser's audio API. Implement an 'emergency cue' button that any participant can activate to trigger a brief subtle visual ripple animation across all participants' screens — providing the accountability of mutual nudging without the interruption of direct communication. Generate a personal focus report from Postgres session logs: total focus hours this week, average session length, most productive time of day based on session start times, and a streak calendar showing consecutive days with at least one completed Pomodoro, emailed weekly via Resend. Offer a 'room atmosphere' selector where each participant independently chooses their ambient audio (lo-fi, brown noise, silence) from locally stored audio files, played in their own browser without affecting other participants' audio environment.", stack: ["WebRTC", "Liveblocks"], difficulty: "Intermediate", vibes: ["ADHD", "Focus"] },
  { title: "Sketchat", tagline: "Drawing-only chat app.", description: "No text — just doodles in real-time conversations.", prompt: "Build a Next.js web app with real-time collaborative canvases using Liveblocks Storage for persistent canvas state and Liveblocks Presence for cursor tracking, where conversation threads consist entirely of sequential drawing panels — no text input fields exist anywhere in the conversation thread. Each canvas panel supports basic drawing tools: pen (smooth bezier curves via Catmull-Rom spline interpolation), shapes, fill colors from a limited 12-color palette, eraser, and undo up to 15 steps, all implemented on HTML5 Canvas with each stroke serialized as a path command stored in Liveblocks Storage so latecomers see the full drawing. Implement voice notes as the only non-drawing content: a microphone button records a clip up to 30 seconds via MediaRecorder, uploads to S3, and attaches the signed URL to the canvas panel as a playable audio element positioned at a user-draggable anchor point. Export a conversation as an animated GIF: use the gif.js library to capture each drawing step from the Liveblocks Storage history at 2-second intervals, producing an animated playback of the conversation's creative evolution downloadable as a shareable file. Add notifications where users receive a push notification via the Web Push API when a new drawing panel is added to a thread they've participated in, with a thumbnail preview generated server-side using node-canvas from the latest canvas state.", stack: ["Liveblocks", "Canvas"], difficulty: "Intermediate", vibes: ["Visual", "Niche"] },
  { title: "Dilemma", tagline: "Anonymous polls for life decisions.", description: "Post your dilemma, the crowd votes with brief explanations.", prompt: "Build a Next.js web app where users post binary-choice dilemmas anonymously (no account required, just a generated session handle): a question with exactly two options, a category tag (career, relationships, finance, lifestyle), and an optional 3-sentence context paragraph — all run through the Perspective API before saving with auto-rejection for content scoring above 0.8 toxicity. Voters choose an option and are required to write a minimum 20-word explanation for their choice before the vote is submitted, ensuring every vote is accompanied by reasoning stored in a votes table linked to the dilemma; votes are surfaced as a community advice thread structured around the binary choice. Display results as a live-updating horizontal bar chart showing vote percentages per option via Supabase Realtime, with a scrollable explanation wall beneath sorted by most helpful (upvoted by other visitors) to surface the most thoughtful reasoning. Auto-resolve polls after 72 hours: send the poster a Resend email with the final vote split, the top 3 explanations per side, and a Claude-generated 2-paragraph synthesis of the reasoning patterns on each side — making the poll genuinely useful as a decision-support tool. Add a 'most consequential dilemmas' trending feed showing the 10 dilemmas with the most evenly split votes (closest to 50/50), surfacing genuinely hard decisions where community wisdom is most divided and therefore most interesting.", stack: ["Next.js", "Postgres"], difficulty: "Beginner", vibes: ["Anonymous", "Polls"] },
  { title: "VillageView", tagline: "Live cam from small towns around the world.", description: "Town committees host a single webcam — viewers tune in to ambient places.", prompt: "Build a Next.js web app where town councils or interested residents submit a live webcam by providing a public HLS stream URL, a town name, country, population range, and description, moderated by a volunteer team who verify the stream is live and public-space-only before marking as approved and adding it to the public directory. Display the stream directory as a searchable grid sorted by country, population, and recently active, with each card showing a static thumbnail screenshot captured every 15 minutes using FFmpeg via a background job that extracts a frame from the HLS stream and stores it in S3. Implement a 'one stream at a time' viewer mode: the main viewing UI shows a single stream full-screen with an ambient-mode option using the Fullscreen API that removes all browser chrome, no chat, no commenting, just the live feed and the location name and current local time. Add a 'suggest a tune' feature that sends a song title to the stream owner via a Resend-forwarded anonymous message, and a donation link per stream directing to the town's own donation page configured by the stream owner. Track anonymous viewer counts per stream using a Redis counter incremented on stream open and decremented on disconnect via a Vercel Edge Function, displayed as a live 'X people watching' badge on each stream card in the directory to surface the currently most-popular ambient destinations.", stack: ["Next.js", "WebRTC"], difficulty: "Intermediate", vibes: ["Ambient", "Travel"] },
  { title: "CinemaSync", tagline: "Watch indie films together on a synced timeline.", description: "Upload an MP4 to a private room; everyone sees the same frame.", prompt: "Build a Next.js web app where room creators upload an MP4 file to S3 via a multi-part upload API route (max 2GB enforced server-side), generating a room with a unique code shared with up to 10 invited participants who authenticate via email magic link. Implement synced video playback using Liveblocks: the room host's play, pause, and seek events are broadcast via a Liveblocks Broadcast channel, and each participant's HTML5 video player seeks to the host's current_time within a 500ms tolerance on each received event, re-syncing every 30 seconds to correct drift. Build a real-time annotation system using Liveblocks Storage: any participant can pause the video and draw a simple shape or text label onto the current video frame using a Canvas overlay, persisting the annotation at that timestamp so other participants see it when they reach that moment — useful for marking important scenes during a film discussion. Add text reactions as ephemeral floating text sent via Liveblocks Broadcast where participants type a short reaction that floats over the video frame for 3 seconds, creating a commentary layer without a full chat sidebar that competes with the film. Support room moderation via a per-room access code and host-only kick capability, with session logs stored in Postgres for abuse reporting and a public shareable link that requires the room code to join.", stack: ["Next.js", "Liveblocks"], difficulty: "Intermediate", vibes: ["Video", "Watchparty"] },
  { title: "OpenMicNight", tagline: "Weekly virtual open mics.", description: "Sign up to perform a 4-min set; audience votes via emoji.", prompt: "Build a Next.js web app running a weekly event: every Friday at 8pm ET, a virtual open mic stage opens with a 4-minute performance slot per performer, up to 20 performers per night, and unlimited audience members — performers sign up via a form (name, act type, description) that opens Monday and closes Thursday midnight, with confirmation emails sent via Resend. Build the live event interface using LiveKit WebRTC: each performer gets 5 minutes of green room preparation in a private room before their slot, then the host transitions them onto the stage where only the current performer has video and audio enabled and the audience has listen-only subscriber connections supporting up to 1000 simultaneous viewers. Implement audience emoji voting: every 30 seconds during a performance, each audience member can tap one emoji from a set of 10 (applause, fire, heart, laugh, mindblown, music, sparkle, brokenheart, rofl, pray) stored in Postgres linked to the performer slot and timestamp, displayed as a live floating emoji animation overlay on the stage view. After each OpenMicNight, export a 3-minute highlight reel: use FFmpeg to splice the peak 30-second segments from each performance based on emoji reaction volume (most reactions per 30-second window), upload to S3, and share the highlight reel URL via a public event recap page. Archive all past events in a searchable history page with each performer's name, act type, and performance stats (total emojis received, most common emoji), building a community hall-of-fame for returning performers.", stack: ["WebRTC", "Liveblocks"], difficulty: "Advanced", vibes: ["Performance"] },
  { title: "PetParade", tagline: "Photo of your pet earns daily compliments.", description: "Post one pet photo a day, get only positive comments. Toxicity blocked.", prompt: "Build a Next.js web app where users post exactly one pet photo per day (enforced by checking last_posted_at in Postgres against the current UTC date) via a mobile-optimized upload form that converts photos to WebP using sharp and stores them in S3, requiring the user to tag the pet species and give the photo a title. Filter all photo captions and comments through the Perspective API's toxicity analysis before saving: any content scoring above 0.75 on toxicity, identity attack, or insult scores is rejected with a gentle 'try a kinder phrasing' message — no moderator review needed for obvious positivity violations. Build a positivity-only comment system where the only available options are pre-written positive phrases ('What a cutie!', 'This made my day!', 'So fluffy!', 'I am restored.') plus a free-text field that passes Perspective filtering — ensuring every comment thread maintains the 'only good vibes' promise without heavy moderation burden. Generate a weekly highlight reel for each user: take their last 7 photos and use GPT-4o Vision to write a playful 2-sentence caption for each one, then combine them into a scrollable 'weekly pet diary' card displayed on their profile and optionally shareable as an image grid exported via html2canvas. Add a 'Daily Rescue Spotlight' feature where the platform features one adoptable shelter animal per day sourced from the Petfinder API, displayed at the top of the feed with an adoption link — tying the platform's warmth to a real-world positive action.", stack: ["Next.js", "Perspective API"], difficulty: "Intermediate", vibes: ["Wholesome", "Pets"] },
  { title: "DataDate", tagline: "Match dating profiles by music + book overlap.", description: "Spotify + Goodreads profiles -> compatibility score with explainability.", prompt: "Build a Next.js app where users create a profile and connect their Spotify account via OAuth to import their top 50 tracks and top 20 artists, their Goodreads 'read' shelf via the Goodreads RSS feed, and their Letterboxd watched list via a CSV export upload — all stored in Postgres as cultural taste profiles. Compute a multi-factor compatibility score between any two users: Spotify artist Jaccard overlap, genre similarity from the Spotify audio feature distributions across top tracks, Goodreads genre overlap using Open Library genre tags for matched ISBNs, and Letterboxd director and genre overlap from the scraped CSV data — weighted 40% music, 30% books, 30% film. Build an explainability panel showing the top 3 specific data points behind the score: 'You both love Khruangbin (artists)', 'You both read Ursula Le Guin (books)', 'You both watched Bong Joon-ho films' — making the match feel personal and conversation-starting rather than algorithmic. Display a browse view where users see profiles with compatibility percentages and the top 3 shared data points, click to see the full breakdown across all cultural dimensions, and send a 'conversation starter' message pre-generated by Claude using their specific overlapping favorites. Add a 'musical chemistry' visualization: an interactive radar chart showing both users' Spotify audio feature averages (danceability, energy, valence, acousticness, tempo) overlaid for comparison, making personality-through-music differences visually interpretable.", stack: ["Spotify API", "Goodreads"], difficulty: "Advanced", vibes: ["Dating", "Match"] },
  { title: "LeftoverLanding", tagline: "Neighborhood food-share board.", description: "Post leftovers near you; neighbors claim within an hour.", prompt: "Build a Next.js Progressive Web App where users post leftover food available for pickup: a title, short description with dietary info and allergens, a photo uploaded to S3, an approximate pickup location hashed to a neighborhood grid cell using H3 for privacy, an earliest and latest pickup time, and a quantity in servings — all stored in Postgres. Implement a 30-minute exclusive claim window: when someone claims a post, it shows 'Claimed by someone nearby' for all other users and sends the claimant the exact pickup address via Resend email; if the claimant doesn't show up within 30 minutes of the earliest pickup time, the post automatically re-opens for new claims. Score users by reputation: completed pickups (both poster confirms food was taken, claimant confirms food was received) increment both users' reputation scores in Postgres; no-show claims after the claim window decrement the claimant's score and flag their account after 3 no-shows. Implement location-aware push notifications via the Web Push API: users who subscribe with their location receive notifications when a new post appears within a configurable radius, with the notification title showing the food category, distance in walking minutes, and time left before the offer expires. Add a weekly neighborhood digest emailed via Resend showing total meals shared, top posters, most-claimed food types, and total food-waste weight prevented estimated by average portion weight per food category from a lookup table.", stack: ["PWA", "Postgres"], difficulty: "Intermediate", vibes: ["Civic", "Local"] },
  { title: "RecipeRiff", tagline: "Remix friends' recipes and credit them.", description: "Fork a recipe like code; show lineage trees of how a dish evolved.", prompt: "Build a Next.js recipe sharing app where each recipe is stored as a structured JSON document in Postgres (title, ingredients with quantities and units, step-by-step instructions, cook time, servings, tags, and a hero photo URL) with a 'fork' operation that creates a copy linked to the original by a parent_recipe_id — forming a lineage graph of recipe evolution. Build a version diff view for forked recipes using jsdiff on the serialized recipe JSON, displaying changes to ingredients (new/removed/quantity changed) and instructions (steps added/removed/reworded) in a color-coded diff panel similar to a GitHub PR review — so riff authors can explain in a PR-style description what they changed and why. Implement a community PR system: riff authors can submit their changes back to the original recipe author as a 'recipe PR', which the original author can review, comment on inline at the ingredient or step level, merge (updating the canonical recipe), or close with a note. Visualize recipe lineage as an interactive D3.js force-directed graph where each node is a recipe version, edges show fork relationships, and node size scales by the number of forks or hearts received — making the evolutionary history of popular community recipes visually compelling and browsable. Generate a weekly 'community kitchen' digest via Resend showing the most-forked recipes this week, the most creative riff (most ingredient substitutions), and a featured lineage graph snippet showing a recipe that spawned the most forks across multiple generations.", stack: ["Next.js", "Postgres"], difficulty: "Advanced", vibes: ["Cooking", "Community"] },
  { title: "GreenGreet", tagline: "Plant-care community with photo journaling.", description: "Daily check-in photos build a timeline of your plant's life.", prompt: "Build a Next.js web app where users add houseplants to a personal collection by photographing them, with the photo sent to a TensorFlow.js plant species classifier running in the browser using the PlantNet model (loaded via TensorFlow.js's loadLayersModel) to suggest the species before the user confirms. For each confirmed plant, create a care timeline: a chronological photo journal with date-stamped photos, a care log entry (watered, repotted, fertilized, trimmed, treated for pests), and an optional 2-sentence journal note about the plant's current state — all stored in Postgres with S3 photo URLs. Build a friend-circle rescue alert system: users can mark a plant as 'struggling' with a description of symptoms, triggering a push notification via the Web Push API to all friends in their plant circle showing the struggling plant's photo and symptoms alongside a 'share advice' button that opens a comment thread. Generate AI care advice when a user marks a plant as struggling: pass the plant species, recent care log entries (last 30 days), and symptom description to Claude, which produces a 3-bullet diagnostic with most-likely cause, recommended immediate action, and a preventive care tip. Add a community milestone system: plant care streaks (30 consecutive days of care log entries), growth milestones (survived first winter, first new leaf), and first-bloom events each trigger a celebratory card displayed in the user's plant journal and shared to their followers' feeds as a gentle positive update.", stack: ["TensorFlow.js", "Next.js"], difficulty: "Intermediate", vibes: ["Plants", "Wellness"] },
  { title: "MentorMingle", tagline: "Free 15-minute career chats with vetted mentors.", description: "Mentors offer slots, mentees book. Donation-funded.", prompt: "Build a Next.js career mentorship platform where mentors create a profile listing expertise areas, current role (verified by email domain matching via Clearbit Company API), available time slots linked via Google Calendar's freebusy API, and a short bio — with a Stripe Checkout flow for a suggested donation amount directed to a mentor-chosen nonprofit via Stripe Connect. Job-seekers browse mentors filtered by expertise, industry, company size, and career stage, then book a 15-minute slot using the Google Calendar API's events.insert endpoint with a Google Meet link auto-generated via the Google Calendar Conference endpoint. Generate a structured 15-minute agenda for each session using Claude: given the mentee's submitted question or goal, the mentor's background, and the 15-minute constraint, produce a 4-item agenda and email it to both parties 1 hour before via Resend. After the session, prompt both parties to leave a review: mentees rate usefulness and communication clarity; mentors rate the mentee's preparation — aggregated scores displayed on both profiles. Display mentor profile momentum metrics: sessions completed, mentee industries served, average ratings, and a word-cloud of topics mentees most frequently raise (extracted by Claude from post-session mentee notes), helping future mentees quickly gauge whether the mentor is the right fit.", stack: ["Next.js", "Stripe"], difficulty: "Intermediate", vibes: ["Career"] },
  { title: "RuneCircle", tagline: "Tarot reading exchanges between friends.", description: "Pull cards in a shared canvas, both interpret out loud.", prompt: "Build a Next.js web app where two friends enter a shared tarot session via shareable invite link, each seeing a synced canvas powered by Liveblocks Storage displaying a configurable spread (Celtic Cross, Three-Card, Single Card, Horseshoe) with face-down card placeholders rendered as illustrated card backs using custom SVG artwork. Implement a synchronized card draw mechanic: either participant can tap a face-down card to trigger a flip animation that simultaneously reveals the drawn card (randomly selected from the 78-card Rider-Waite-Smith deck stored as PNGs in S3) to both participants via a Liveblocks Broadcast event — with upright or reversed drawn with equal 50% probability. For each revealed card, display a concise interpretation pulled from a curated 78-card meaning database stored in Postgres (different interpretations for upright vs. reversed), and allow both participants to add their personal interpretation in a shared text note attached to the card position, synced via Liveblocks Storage. Add a voice channel using LiveKit WebRTC so both participants can speak their interpretations aloud as cards are revealed, making the session feel like a real-life reading rather than a text exchange — the voice channel auto-activates when both participants join the session. Generate a session journal entry after the reading concludes: Claude synthesizes all revealed cards, their positions in the spread, and both participants' written interpretations into a 3-paragraph narrative reading stored in Postgres as part of a personal session journal, searchable and browsable over time as a record of recurring themes across readings.", stack: ["Liveblocks", "WebRTC"], difficulty: "Intermediate", vibes: ["Spiritual", "Niche"] },
  { title: "ConcertCarpool", tagline: "Find rideshares to concerts and festivals.", description: "Match by venue, time, and rough route. Anonymous chat until confirmed.", prompt: "Build a Next.js PWA where users post concert attendance by selecting an event from a database populated by the Songkick or Bandsintown API, specifying their starting neighborhood (resolved to an H3 grid cell for privacy) and preferred timing, then optionally marking themselves as a driver with available seats. Match riders with drivers: when a user marks themselves as a driver, the system finds other attendees of the same event whose H3 origin cells are within 2 cells of the driver's route path (approximated via Google Directions API waypoints), ranks matches by route detour percentage, and notifies both parties of potential carpool matches via Resend email. Implement anonymous pre-match chat using a Supabase Realtime ephemeral channel scoped to the match_id — both parties can exchange messages without seeing each other's full names until one clicks 'Confirm carpool', at which point both receive the other's first name and mobile number if added to their profile. Build a post-event review system where carpool participants rate each other on safety and punctuality, with ratings aggregated into a trust score displayed on each user's profile and visible before a carpool is confirmed. Add event-level analytics showing total attendees who've used the app for a specific concert, estimated CO2 saved from carpooling (average miles × passengers × EPA car emission factor), and a leaderboard of most-helpful drivers by event.", stack: ["Next.js", "Mapbox"], difficulty: "Intermediate", vibes: ["IRL", "Events"] },
  { title: "WordSwap", tagline: "Trade physical books with neighbors via mailbox.", description: "List a book, swap one for one, track delivery via barcode scans.", prompt: "Build a Next.js Progressive Web App where users add books to their 'available for swap' shelf by scanning the ISBN barcode using the browser's BarcodeDetector API or a camera-based fallback via zxing-js, fetching book metadata (title, author, cover, page count, genre) from the Open Library API and storing each listing in Postgres with the user's H3 neighborhood cell and swap status. Implement a swap proposal mechanic: User A proposes a swap by selecting a book from User B's shelf and offering one of their own books in return; both parties must approve the swap before contact info is exchanged, with the proposal and acceptance states stored in Postgres and Resend emails sent at each transition. Protect physical addresses: once a swap is confirmed, the app uses a P.O. box-style address forwarding service or concierge system where the app receives both books and forwards them, with per-swap tracking via a generated tracking_code stored in Postgres and shared with both parties. Add a reputation system where users earn 'bookworm badges' based on completed swaps (5, 25, 100 swaps), response speed (how quickly they approve incoming proposals), and condition accuracy (how well their described book condition matches what the recipient received, rated post-swap). Build a public 'community shelf' view showing all available books in the user's neighborhood within a 10km radius in a grid with filters for genre, page count, and language, with a 'someone in your area has this' push notification when a book matching a user's wishlist ISBN is added nearby.", stack: ["PWA", "ISBN API"], difficulty: "Intermediate", vibes: ["Books", "Local"] },
  { title: "SilenceSends", tagline: "Send a friend a 'just thinking of you' nudge.", description: "One button, one recipient, one ping per week. Nothing else.", prompt: "Build a Next.js Progressive Web App where users add friends by exchanging invite codes (a unique 8-character code valid for 72 hours) — creating a reciprocal friendship in Postgres visible to both parties only when both have added each other's code — keeping the social graph intentionally small and close. Display a contact list where each friend's name is a single large button; tapping the button sends a 'ping' — a timestamped delivery_events record in Postgres, a Web Push notification to the recipient's device with the text '[Name] is thinking of you ✨', and a soft haptic vibration on supporting mobile devices via the Vibration API. Enforce the weekly cap: each user-to-user ping relationship has a weekly_count field in Postgres reset every Monday at midnight UTC; once the weekly limit (1 ping per friend per week, hard limit) is reached, the button dims with a '1 week cooldown' tooltip and cannot be tapped again until the reset. Show delivery state: after sending a ping, the button displays a small animated heartbeat for 5 seconds confirming dispatch, then shows 'Sent' with the date, fading back to the default state after 60 seconds — with no persistent 'read receipt' shown to the sender, preserving the low-pressure nature of the interaction. Build a minimal settings screen where users configure their display name, notification quiet hours (no pings between 10pm and 8am in their timezone), and a list of up to 10 friends — keeping every aspect of the experience intentionally small and deliberate.", stack: ["PWA", "Push API"], difficulty: "Beginner", vibes: ["Wholesome", "Minimal"] },
  { title: "WatchWithMe", tagline: "Sync up Twitch streams between friend groups.", description: "One person picks the stream; everyone joins synced and chats privately.", prompt: "Build a Next.js web app where users create a private viewing group by pasting a Twitch channel URL, generating a unique group room code — other friends paste the code to join, with the room storing up to 10 participants in Postgres. Implement synchronized Twitch playback using the Twitch Embed Player API's play() and seek() JavaScript methods: the room host's player events are broadcast via a Liveblocks Broadcast channel with a current_time payload, and each participant's embedded player seeks to match within a 2-second tolerance every 30 seconds to correct natural drift between streams. Build a private group chat using Supabase Realtime scoped to the room's unique code, where participants exchange text messages and optional emoji reactions that float over the Twitch embed for 3 seconds using CSS animations — with messages stored in Postgres for 24 hours then auto-deleted. Implement shared clip marking: any participant can tap a 'clip this' button that calls the Twitch API's helix/clips endpoint to create a clip of the last 30 seconds, storing the clip ID in Postgres and sharing it instantly in the group chat with a preview thumbnail. Show a participant sidebar with each member's viewer status (watching, away, disconnected) updated via Liveblocks presence, and a 'sync all' button the host can press to force all participants' players to seek to the host's current position — useful after a stream interruption leaves participants out of sync.", stack: ["Twitch API", "Liveblocks"], difficulty: "Intermediate", vibes: ["Streaming"] },
  { title: "RoadCrew", tagline: "Pickup-truck moving help by neighborhood.", description: "Need to move a couch? Match with someone willing to help and pay.", prompt: "Build a Next.js web app where people with pickup trucks or cargo vans list their availability to help with small moves: posting their vehicle type, cargo dimensions, available dates/times, coverage area (stored as a PostGIS polygon), and a fixed hourly rate — with Stripe Connect onboarding for payment collection. Job-posters create a move request with a description, item list, pickup and drop-off addresses, and a requested time window; the system matches available helpers by computing whether the move's origin PostGIS point falls within the helper's coverage polygon using ST_Contains. Implement a trust verification system: helpers verify their vehicle via photo upload and upload a government ID scanned via the Persona API for identity verification, earning a 'road clearance' badge visible to all job-posters before booking. Build a booking flow where the job-poster pays a configurable deposit (20% of the estimated job cost) via Stripe Checkout; on job completion, both parties confirm via the app and the remaining balance is charged automatically with the helper receiving payout via Stripe Connect's transfer endpoint. Add a mutual review system where both parties rate each other after the job with public reviews, and implement a damage protection flow where the job-poster can file a dispute within 24 hours of job completion supported by pre-job and post-job photos uploaded by both parties.", stack: ["Next.js", "Stripe Connect"], difficulty: "Intermediate", vibes: ["Local", "Marketplace"] },
  { title: "LangCircle", tagline: "Language learning with dropping-into-conversations.", description: "Practice conversations randomly assigned by topic and level.", prompt: "Build a Next.js web app where users create a language learner profile (native language, languages learning, CEFR level per language, available hours per timezone) and opt into a 'conversation drop' pool — when matched with a 4-person group by a matchmaking algorithm, they receive a 10-minute Web Push notification before their assigned conversation slot. Each conversation drop uses LiveKit WebRTC for multi-party audio with a designated language for the session (the shared learning language at a similar level for all 4 participants), and a conversation topic card displayed in all participants' browsers sourced from a curated topic database (weekend plans, local traditions, recent books, favorite foods). Integrate Whisper auto-correction: transcribe the conversation using OpenAI's Whisper API via server-sent audio chunks, detect grammatical errors in the target language by passing 30-second transcript windows to Claude with a correction-detection prompt, and surface corrections as non-intrusive 'suggestion cards' below the conversation topic panel without interrupting the flow. After each 5-minute conversation (hard time limit enforced by LiveKit room disconnect), prompt each participant to rate the conversation topic quality and flag any vocabulary they want to review later — stored in a personal vocabulary log reviewed via SM-2 flashcards in the dashboard. Generate a weekly language learning report via Resend: minutes of immersive conversation, accuracy trend from Whisper corrections, most common error types, and a suggested topic category to focus on based on weak spots identified from correction data.", stack: ["WebRTC", "Whisper"], difficulty: "Advanced", vibes: ["Language"] },
  { title: "DadJokeDuel", tagline: "Daily dad-joke tournament.", description: "Submit one joke per day, bracket-style voting, last laugh wins.", prompt: "Build a Next.js web app where users submit exactly one dad joke per day (enforced by checking last_submitted_at in Postgres against current UTC date) via a text-entry form with a 10-200 character limit; jokes are run through Claude with a prompt confirming they follow the 'setup + pun' dad joke format — rejecting stand-up bits or dark humor with a friendly 'try a dad-er joke' error message. Organize daily submissions into single-elimination bracket tournaments: at midnight UTC, the day's submitted jokes are shuffled and seeded into a bracket with head-to-head matchups assigned; users receive a Resend email or Web Push notification showing their first matchup. Each matchup presents both jokes side-by-side without author attribution — voters choose the funnier one by clicking it; votes are collected for 12 hours after which the winner advances and the loser receives a consolation 'groaner badge' in their profile. Display a live bracket visualization using D3.js showing round-by-round progression with matchup results, authors revealed after voting closes, and a championship final at the end of each day — the daily champion's joke and username headline the next day's homepage with a trophy animation. Archive every daily champion in a 'Hall of Groan' leaderboard showing champion username, winning joke, vote percentage in the final, and total championships won, with a monthly greatest-of-all-time bracket where the month's daily champions compete for the 'Dad of the Month' trophy.", stack: ["Next.js", "Postgres"], difficulty: "Beginner", vibes: ["Humor", "Daily"] },
  { title: "LocalLine", tagline: "Live calls between strangers in your city.", description: "Voice-only random calls, capped at 3 minutes. Pure curiosity.", prompt: "Build a Next.js Progressive Web App where users join a call queue by tapping 'Call a Stranger' and selecting their current city from a list of 200 major cities — generating an anonymous session token in Postgres with a city_id, a randomly generated pseudonym, and a queue_joined_at timestamp. Match two users from the same city's queue using a first-in-first-out pairing algorithm running every 5 seconds via a Vercel Edge Function polling Postgres, creating a match_id record and notifying both users' browsers via Supabase Realtime — the matching process completes within 10 seconds of two compatible users being in queue. Connect matched users via LiveKit WebRTC for audio-only calls (no video, camera permissions never requested), with a hard 3-minute call duration enforced by a server-side LiveKit room expiry set at room creation time — when the timer reaches 0 the room closes automatically and both users are returned to the main screen. Display a minimal call interface: a circular animated waveform showing voice activity, a 3-minute countdown timer, a mute button, and a large 'End call early' button — nothing else, no text input, no emoji, no selfie view. Implement abuse prevention: a daily call limit of 5 calls per device fingerprint in Postgres, a one-tap 'skip and report' button that disconnects the call and logs a report_event, and an automatic 24-hour block for device fingerprints that receive 3 reports in one day.", stack: ["WebRTC", "TURN"], difficulty: "Advanced", vibes: ["Voice", "Stranger"] },
  { title: "TimeCapsule", tagline: "Send messages to your future friends.", description: "Write to a friend now, delivered on a date you both choose.", prompt: "Build a Next.js web app where two friends create a TimeCapsule together: they both agree on a future unlock date (minimum 3 months, maximum 10 years), each write their message in a rich text editor built with TipTap, and both click 'Seal capsule' — at which point both messages are encrypted client-side using the other person's public key via the Web Crypto API's RSA-OAEP algorithm, with ciphertexts stored in Postgres; only the recipient can decrypt their message using their stored private key on the unlock date. Build a countdown UI showing the sealed capsule as a visual illustration on each user's dashboard with a countdown timer displaying years, months, and days remaining — no peek is possible because neither party holds the key to decrypt their received message before the unlock date. On the unlock date, trigger a dramatic unlock notification via Resend email with the subject 'Your TimeCapsule with [name] is ready to open' and a 'Reveal' button linking to the app. Implement a synchronized unlock ritual: both users must be on the TimeCapsule page simultaneously (detected via Supabase Realtime presence) within a 24-hour window after the unlock date to trigger the simultaneous reveal animation — if only one person opens it, they see a 'waiting for [name] to open theirs' screen that updates in real time when the second person joins. Allow optional 'time capsule additions': during the waiting period, either friend can add emoji reactions to the capsule's cover page via Supabase Realtime, visible to both as small floating emojis on the sealed capsule illustration — a subtle way to acknowledge the passage of time during the wait.", stack: ["Next.js", "Cron"], difficulty: "Beginner", vibes: ["Wholesome"] },
  { title: "GraffitiGo", tagline: "AR graffiti tags persistent in real places.", description: "Tag a wall in AR; passersby see it through the same app.", prompt: "Build a Next.js Progressive Web App using the WebXR Device API's AR capabilities on Android Chrome and a GPS-anchored camera overlay fallback for iOS, letting users 'tag' a real-world location by drawing on a canvas overlaid on their camera view — the drawing is anchored to the GPS coordinates and compass heading captured at creation time, stored in Postgres as a graffiti_tag with lat/long, heading, and a serialized canvas drawing stored in S3. Implement a map-based discovery mode using Mapbox GL JS where all tags in the area are shown as colored pin markers, with each pin color indicating the tag's community upvote score; tapping a pin switches to AR view pointed at that tag's anchor location. Build a community moderation queue: any user can flag a tag as offensive, and tags with 3 or more flags enter a review queue visible to volunteer moderators — approved tags are restored with a 'community approved' badge while rejected tags are deleted and creators warned. Generate a 'best of city' weekly roundup: aggregate the top 10 most-upvoted tags from the past 7 days within a given city's bounding box, render each tag's canvas drawing as a static image using node-canvas, and publish a curated gallery page at /city/{slug}/gallery/{week} showing the week's most-loved urban art with creator pseudonym and neighborhood. Implement tag decay: all tags automatically expire after 30 days unless they've received at least 5 upvotes, with a 'renew my tag' option allowing the creator to extend for another 30 days — ensuring the map stays fresh rather than accumulating years of inactive content.", stack: ["WebXR", "Mapbox"], difficulty: "Advanced", vibes: ["AR", "Community"] },
  { title: "CritParty", tagline: "Async writing crits for fiction.", description: "Drop chapters; readers leave timestamped voice notes.", prompt: "Build a Next.js web app where fiction writers upload chapter-length prose documents (DOCX or plain text, max 5000 words per submission) extracted using mammoth for DOCX and stored as structured blocks in Postgres, with each paragraph having its own paragraph_id to support per-paragraph annotations. Build an annotation layer using Supabase Realtime where readers leave timestamped voice notes (recorded via MediaRecorder, uploaded to S3, linked to a paragraph_id) or text comments at the paragraph level — the writer sees annotations as colored margin markers when reading their own work, clicking each to play the voice note or read the text comment. Support anonymity options per project: the writer can set anonymous mode (readers see only 'Critiquer 1', 'Critiquer 2' during the critique window, with reveals after it closes) or open mode (all critiquer names visible immediately) — anonymity enforced server-side by stripping user identifiers from annotation queries during the blind window. Curate genre-specific critique groups (fantasy, literary fiction, romance) each with their own submission queue where works enter a waiting list and are assigned to readers by the group coordinator. Generate an AI-assisted critique synthesis after the critique window closes: pass all text annotations and voice note transcripts (via Whisper) to Claude, which produces a 4-section synthesis report (top praised strengths, top suggested improvements, pattern of feedback across all critiquers, key sentence-level issues flagged by multiple readers) stored in Postgres and emailed to the writer via Resend.", stack: ["Next.js", "Whisper"], difficulty: "Intermediate", vibes: ["Writing"] },
  { title: "SkillSwap", tagline: "Trade hours of your skill for hours of someone else's.", description: "Time-bank model — no money exchanged, just hours.", prompt: "Build a Next.js web app where users list both skills they offer and skills they need using a structured taxonomy (design, development, cooking, language tutoring, legal advice, home repair, music lessons) — each offer specifying the skill, proficiency level, preferred session format (in-person or video), and availability hours per week, stored in Postgres. Implement a time-bank accounting system: each new user receives 2 hours of starting credit in a time_accounts table; when User A teaches User B for 1 hour, 1 credit is deducted from User B's balance and 1 credit added to User A's balance — transactions stored as signed_timesheets with both user IDs and a completed_at timestamp that neither party can edit after 30 minutes of creation. Build a session completion flow: after a session, both parties independently mark it complete and log the actual duration; if durations match within 15 minutes the transaction commits automatically; if they diverge, the platform presents both logged durations and asks each party to confirm via a timestamped acknowledgment before committing the average. Implement fraud detection signals: flag accounts whose time_account balance exceeds 20 hours (potential time accumulation without giving back), whose session completion rate is below 50%, or who have received 3 or more dispute flags — flagged accounts enter a review queue for a volunteer trust-and-safety team. Generate a quarterly community impact report: total skill-exchange hours facilitated, most-common skills exchanged, geographic distribution of sessions, estimated dollar value of services exchanged based on average market rate per skill category from a lookup table.", stack: ["Next.js", "Postgres"], difficulty: "Intermediate", vibes: ["Civic", "Barter"] },
  { title: "PartyPin", tagline: "Drop-in micro-events on a city map.", description: "House parties, jam sessions, picnics — all pinned and time-boxed.", prompt: "Build a Next.js PWA where users pin micro-events on a live Mapbox GL JS map by dropping a pin at a location, adding: event title, category (house party, jam session, picnic, study group, game night, impromptu sports), start time, end time, capacity, and an optional short description — stored in Postgres as events with a PostGIS point and auto-expiry after end_time. Implement identity verification using a lightweight phone number confirmation via Twilio Verify (6-digit OTP via SMS) required before posting an event — creating a verified host record in Postgres — to prevent spam event pins while keeping the barrier low for genuine hosts. Build a map view where event pins are colored by category with a pulsing animation if the event is happening right now (start_time < current UTC < end_time), and sized by remaining capacity; clicking a pin shows the event card with description, current attendee count, capacity, and a 'Join' button. Implement capacity controls: joining increments an attendee_count field in Postgres with a database CHECK constraint enforcing attendee_count ≤ capacity, sends a Resend confirmation email to the joiner, and removes the pin from the map when capacity is reached — making small events feel exclusive and curated. Require a post-event recap: 2 hours after the event end_time, send the host a push notification asking them to upload 1-3 recap photos and a one-line summary, stored in Postgres and displayed on the pin's archived event page — building a gallery of neighborhood micro-moments over time.", stack: ["Mapbox", "Next.js"], difficulty: "Intermediate", vibes: ["IRL"] },
  { title: "NightOwl", tagline: "Find someone awake right now to chat.", description: "Insomnia chat that respects time-zone offset and matches kindred night-owls.", prompt: "Build a Next.js Progressive Web App where users log in anonymously using a generated 'night handle' (random animal + moon phase adjective combo, stored in localStorage with a session_token in Postgres) and are only visible in the app between 11pm and 4am in their configured local timezone — the app's UI shows a 'Come back after 11pm' screen during other hours, enforcing the night-owl identity. Match users for text-only 1:1 conversations using a compatibility score combining timezone offset (prefer matching users within 3 hours of each other), declared insomnia category (anxiety insomnia, pain insomnia, can't stop thinking, just a night owl, working late), and a shared interest from a short 5-tag profile — connections are offered as 'someone nearby in time and mood' without specific metrics. Implement a 3-message-per-match icebreaker system: the app sends both users an opening prompt based on their shared insomnia category and each can exchange up to 3 messages before seeing a 'continue chatting?' mutual-opt-in prompt — preventing one-sided message floods. All messages are written to Postgres for 7 days only before auto-deletion, with no long-term message archive — keeping the app feeling ephemeral and low-stakes, like a late-night conversation that dissolves with the dawn. Add a gentle 'sunrise signal' feature: at 6am in each user's timezone, if they're still active in a conversation, the app displays a soft animated sunrise background behind the chat and a message 'The night is almost over — goodnight 🌅', automatically archiving the conversation and returning both users to the offline state.", stack: ["Next.js", "WebSockets"], difficulty: "Beginner", vibes: ["Wellness"] },
  { title: "SongSwap", tagline: "Daily song exchange with one friend.", description: "Both must send a song before either can read what the other sent.", prompt: "Build a Next.js web app where two users link their accounts as a 'music pair' using a shareable invite code, and both receive a daily prompt at 7pm in their local timezone via Resend email or Web Push saying 'Time to share a song with [name]!' along with an optional lyric or mood prompt. Implement the mutual-reveal mechanic using Spotify integration: each user searches for a song using the Spotify Search API and submits their choice (storing the Spotify track URI, track name, artist, and album art URL in Postgres) — both users' submissions are hidden until both have submitted, enforced by a visibility flag that flips only when both pair_member_submitted fields are set. After both songs are submitted, reveal them simultaneously with a 'flip card' animation: each user sees their own submission on the left and their partner's surprise pick on the right, with a 30-second preview playable via the Spotify Web Playback SDK for Premium users or from the Spotify preview_url for non-premium users. Track streaks: consecutive days where both users submitted before midnight increment a pair_streak counter in Postgres with milestone badges (7-day, 30-day, 100-day streaks), resetting if either user misses a day with a 2-hour-past-midnight grace period for late submitters. Generate a weekly Spotify playlist export: on Sunday, compile all 7 songs submitted by both users into a 14-track playlist via the Spotify API's playlist creation endpoint and share the URL in a Resend email summarizing the week's exchange with each song's submitter and a 'week in music' card showing both users' genre distribution from the week's picks.", stack: ["Spotify API", "Next.js"], difficulty: "Beginner", vibes: ["Music", "Wholesome"] },
  { title: "ProtestPing", tagline: "Verified safe-route alerts at protests.", description: "Trusted volunteers ping safe streets; map shows live status.", prompt: "Build a Next.js Progressive Web App where trusted volunteer pingers (vetted by event organizers via invite-only onboarding with email verification in Postgres) submit real-time safety status pins on a Mapbox GL JS map by tapping their approximate current location: each pin specifies a status type (clear path, police presence, crowd dispersion, medical station, exit route, closed street), a short description (max 100 characters), and an automatic 15-minute expiry time. Implement an anti-surveillance architecture: no continuous user location tracking between pin submissions (location data captured only at the moment of submission), no IP address logging in Postgres, no third-party analytics or advertising SDKs, and a 'purge all pins' button accessible to event organizers that deletes all pins and submission metadata from Postgres within 5 minutes. Display pins on the Mapbox map with a 15-minute live countdown per pin and automatic greying-out when expired — viewers see real-time conditions from the last 15 minutes only, never older historical data, reducing the risk of acting on stale safety information. Encrypt supplementary text notes client-side using libsodium's key exchange primitives before sending to the server — the server stores only ciphertext and never accesses message content, with pin metadata (coordinates and status type only) stored unencrypted for the map overlay. Generate an encrypted post-event report available only to the event's designated organizer contact: total pins submitted, status type distribution, geographic heat map of activity, and a timeline of conditions sourced from the pin expiry sequence — useful for after-action review without exposing individual pinger identities.", stack: ["Mapbox", "Signal Protocol"], difficulty: "Advanced", vibes: ["Civic", "Safety"] },
  { title: "BoardroomBard", tagline: "Async storytelling games for remote teams.", description: "5-minute team rituals: pick prompts, write stanza, vote favorite.", prompt: "Build a Slack app using Bolt for JavaScript that installs into any Slack workspace and fires a creative writing prompt to a designated channel on a configurable cadence (weekly Monday morning, bi-weekly Friday afternoon, monthly) selected from a rotating curated database of 500 prompts organized by type (story stanza, sentence completion, six-word memoir, would-you-rather scenario). Each prompt post includes a '5-minute timer' message: Bolt posts the prompt with a visible contributing window indicator, starts a countdown using Slack's scheduled_messages.create to post a 'Time's up!' follow-up exactly 5 minutes later, and collects all thread replies posted during the window. After the timer closes, Bolt reads all contributions from the thread, posts a summary round-up message listing all contributors with their stanzas, and runs a Slack reaction-based vote where whichever contribution gets the most diverse emoji reactions wins the 'crowd favorite' distinction. Store all prompts, contributions, and votes in a Postgres database and generate a monthly 'team creativity zine': a PDF compiled using PDFKit featuring the month's contributions with contributor names, participation stats, and the top-voted contribution from each week — sent to a configurable Slack DM or email via Resend. Add an admin dashboard at an external URL where the ritual coordinator can customize prompt types, set the cadence, add custom prompts specific to their team's culture, and review participation trends over the last 6 months.", stack: ["Slack", "Next.js"], difficulty: "Beginner", vibes: ["Team", "Async"] },
  { title: "QuoteQuilt", tagline: "Friend-curated quote scrapbook.", description: "Friends submit lines from books they read; the quilt grows shareable.", prompt: "Build a Next.js web app where users form reading groups of 2-10 friends, each submitting quotes from books they've read: they paste the quote text, add the book title and author (looked up via the Open Library API to fetch the ISBN, cover image, and publication year), and optionally add a 2-sentence personal reflection on why the quote resonated — all stored in Postgres. Display the group's collected quotes in a masonry 'quilt' layout where each quote is a card with a randomized accent color from a warm print-inspired palette, the book cover thumbnail, a fragment of the author's name, and the submitter's initials — creating a visual collage that feels handcrafted and grows richer over time. Generate a bi-weekly 'quilt PDF' using PDFKit on a Vercel Cron job: select the last 2 weeks of submitted quotes, arrange them in a multi-column print layout with decorative borders and the group name as a header, and email the PDF to all group members via Resend as a shareable keepsake formatted to print on A4 or Letter paper as a double-sided sheet. Build a 'book radar' feature: when a new quote is submitted from a book, check if any other group member has already submitted a quote from the same book — if yes, show a notification 'You and [Name] are both reading [Book]!' in both members' feeds, surfacing serendipitous parallel reading. Add a public-sharing mode where the group can make their quilt public at a URL like quotequilt.app/groups/the-wednesday-readers, allowing friends to browse and link-share individual quote cards as images exported via html2canvas.", stack: ["Next.js", "Open Library"], difficulty: "Beginner", vibes: ["Books"] },
  { title: "ChoreCircle", tagline: "Roommate chores as a tiny social game.", description: "Wheel-spin assignments, public shame badges, weekly leaderboard.", prompt: "Build a Next.js Progressive Web App where households of 2-6 members create a shared household by invitation, configure their chore list (name, estimated duration, weekly/bi-weekly/monthly frequency), and spin a randomized assignment wheel at the start of each week — implemented as a CSS animation spinning an SVG wheel with each member's name as a segment, landing on the assignee using crypto.getRandomValues for true randomness rather than Math.random. Track chore completion: each assigned member receives a daily Resend email or Web Push reminder for uncompleted chores with a single 'Done!' call-to-action link that updates the chore's completed_at field in Postgres via a signed URL without requiring login — making completion as frictionless as possible. Award badges for completion milestones: first chore completed (Welcome to the Circle), 7-day streak (Dependable Roomy), 30-day streak (House Hero), and 100 total chores (Legend of the Household) — stored per-member in Postgres and displayed on a household member leaderboard showing this week's chore counts, current streaks, and total badges earned. Build a 'gentle shame' mechanic: on Sunday evenings, the app posts to a household Slack channel or sends a Resend digest email showing a weekly leaderboard with each member's completion rate — not harsh, but enough social visibility to motivate consistent participation without a punitive tone. Add a household 'house meeting mode': tap a button to generate a short agenda using Claude, which reads current chore completion rates, any flagged issues logged by members, and upcoming shared expenses to produce a 5-item agenda posted to the household Slack channel or emailed to all members.", stack: ["Next.js", "PWA"], difficulty: "Beginner", vibes: ["Living", "Game"] },
];

const DEEPTECH: Omit<Project, "id" | "category">[] = [
  { title: "EdgeLLM Gateway", tagline: "Run open-source LLMs on Cloudflare Workers AI.", description: "Drop-in OpenAI-compatible proxy that routes to the cheapest open model with caching.", prompt: "Build a TypeScript Cloudflare Worker that exposes a fully OpenAI-compatible /v1/chat/completions endpoint, accepting standard request bodies and translating them to backend-specific formats for Cloudflare Workers AI, Together AI, and Groq based on a routing config stored in KV. Implement a two-level cache: first check Workers KV for an exact prompt hash match with a configurable TTL, then fall back to semantic similarity using a lightweight embedding stored alongside each cached response so near-duplicate prompts still hit cache. Add per-API-key budget tracking stored in Durable Objects — each key has a monthly token limit and the gateway returns 429 with a Retry-After header when the budget is exhausted, preventing runaway costs in multi-tenant deployments. Route intelligently by maintaining a rolling latency leaderboard updated every 60 seconds via a scheduled Workers Cron: weight faster backends higher for streaming requests and cheaper backends higher for batch jobs based on the request's stream flag. Expose a lightweight admin dashboard as a Workers-served HTML page showing per-backend uptime, cache hit rates, and per-key spend, authenticating with a hashed admin token stored in Workers Secrets.", stack: ["Cloudflare Workers", "Workers AI"], difficulty: "Advanced", vibes: ["LLM", "Infra"] },
  { title: "VectorVet", tagline: "Quality assurance for embeddings drift.", description: "Detects when your production embedding distribution drifts from training.", prompt: "Build a Python FastAPI service backed by PostgreSQL with pgvector where teams upload a golden embedding set at release time — each vector stored with metadata like label, source_hash, and timestamp — and production embedding batches are compared against it on a nightly schedule via Celery. Compute Maximum Mean Discrepancy (MMD) and Wasserstein-2 distance between the golden set and the incoming batch using scipy and ot libraries, flagging drift when either metric exceeds a configurable sigma threshold, and retrieve the five nearest golden-set neighbors of each outlier using pgvector's cosine similarity index for explainability. Implement a labeled anomaly log where drift events store the full incoming batch, the MMD/Wasserstein scores, and a LLM-generated natural-language summary of which semantic clusters have shifted, giving engineers a prose explanation alongside raw numbers. Surface results in a Next.js dashboard with time-series charts of drift scores, a 2D UMAP scatter plot comparing golden vs. production distributions rendered with Plotly, and a per-label breakdown showing which classes are drifting most. Add a GitHub Actions integration that posts a drift report as a PR comment after each deployment, blocking merge if the drift score exceeds a hard-coded safety threshold set per project in a config file.", stack: ["Python", "Next.js"], difficulty: "Advanced", vibes: ["MLOps", "Quality"] },
  { title: "PromptCompress", tagline: "Auto-shrink LLM prompts without losing accuracy.", description: "Pipeline that trains a compressor specific to your task and validates fidelity.", prompt: "Build a Python service that implements an LLMLingua-style iterative token-importance scorer: given a system prompt and a set of example query-response pairs, it runs the target LLM on each example, records attention gradients over the prompt tokens, and learns a logistic regression head that predicts per-token importance scores to guide aggressive token deletion. The compression pipeline removes low-importance tokens in passes, re-scoring after each removal step and stopping when an answer-equivalence metric (BERTScore or a secondary LLM judge call) drops below a configurable fidelity threshold, yielding a compressed prompt that preserves task performance. Add a quality-vs-savings scorecard that benchmarks the compressed prompt on a held-out eval set, reporting token reduction percentage, latency improvement, cost savings (dollars per 1M calls), and the fidelity score so teams can make an informed trade-off decision before switching to the compressed version. Build a Next.js dashboard where users paste or upload their prompt and example pairs, see the compression progress in real time as a streaming token-deletion visualization, and download a JSONL file of compressed/original pairs with scores for audit. Implement versioned prompt storage in Postgres so teams can track compression history, roll back to a previous version, and A/B test compressed vs. original prompts via a split-traffic config flag without redeploying application code.", stack: ["PyTorch", "Next.js"], difficulty: "Advanced", vibes: ["LLM", "Optimization"] },
  { title: "SignalForge", tagline: "Generate synthetic time-series for ML testing.", description: "Configurable seasonality + anomaly injection with statistical validation.", prompt: "Build a Next.js web app backed by a Python FastAPI service that generates synthetic time-series data using a composable signal model: users configure additive components including multiple seasonal periods (hourly, daily, weekly), a linear or exponential trend, Gaussian noise with configurable sigma, and mean-reverting ARMA residuals, previewing the resulting waveform in real time with a Recharts line chart. Implement a labeled anomaly injection panel where users drag event markers onto the preview chart to place point spikes, level-shift change points, or variance-change regime breaks — each anomaly is stored with its ground-truth label and timestamp so the exported dataset is ready for supervised anomaly-detection model training. Run automated statistical sanity checks before export using the Augmented Dickey-Fuller test for stationarity, Ljung-Box for residual autocorrelation, and a KS test comparing the marginal distribution to the user-specified target, surfacing warnings in a badge-style report card if the generated series has unexpected properties. Support multi-variate generation with configurable cross-correlation matrices between channels, enabling realistic sensor-array simulations where correlated channels fail together during anomaly events. Export as CSV, Parquet, or a Hugging Face dataset card with a pre-filled datacard YAML so the synthetic dataset can be immediately pushed to the Hub for reproducible ML experiments.", stack: ["NumPy", "Next.js"], difficulty: "Intermediate", vibes: ["Data", "Synth"] },
  { title: "TokenTracer", tagline: "Visualize attention paths in any open LLM.", description: "Inspector that shows which prior tokens drove the current generation step.", prompt: "Build a Python FastAPI backend that loads open-weight models via the Hugging Face transformers library and exposes an inference endpoint that returns both the generated text and a full attention tensor dump, computing gradient-weighted attention saliency by multiplying attention weights with the gradient of the output log-probability with respect to each attention head's value matrix. Process the raw attention tensors into a per-generated-token attribution vector that assigns an importance score to each context token, averaging across heads and layers with configurable aggregation strategies (mean, max, rollout), and normalize the result to a [0,1] scale for consistent cross-model comparison. Build a React frontend with a dual-pane view: the left pane shows the input prompt with tokens highlighted in a heatmap gradient from cold-blue (low attribution) to hot-red (high attribution) for whichever output token is selected on the right; clicking any output token rerenders the input heatmap instantly using precomputed attribution data. Add a head-by-head explorer modal that shows a 12×12 (or model-appropriate) grid of attention matrices as small heatmap thumbnails, letting researchers click into any single head to see its full attention pattern rendered as a D3 matrix with row/column token labels. Store session results in a SQLite database so researchers can save named runs, compare attributions across prompts side-by-side, and export a JSON bundle of all attention data for offline analysis in Jupyter notebooks.", stack: ["transformers.js", "D3"], difficulty: "Advanced", vibes: ["Interpretability"] },
  { title: "BrowserBench", tagline: "Benchmark headless browsers for AI agents.", description: "Compare Playwright/Browserbase/Steel on agent-task success rates.", prompt: "Build a benchmark harness in TypeScript that defines a fixed suite of deterministic agent tasks — form filling, multi-step navigation, JS-heavy SPA interaction, file download — stored as JSON task specs, and runs each task against Playwright (local Chromium), Browserbase cloud, and Steel browser in parallel via provider-specific adapters. Implement a generic agent loop that interprets each task spec, issues browser actions through a uniform BrowserAction interface, captures screenshots at each step, and records a structured trace including timestamps, DOM state hashes, and action sequences, enabling post-hoc replay and debugging of failures. Score each run on task success (binary), time-to-completion, screenshot-based UI fidelity using SSIM comparison to a golden reference screenshot, and provider cost computed from published per-minute pricing, then aggregate across 5 randomized runs to produce mean and p95 statistics that smooth over flaky network conditions. Persist all benchmark results in a Postgres database and expose them as a public Next.js leaderboard page showing a comparison table with sortable columns, historical trend charts per provider per task type, and a side-by-side screenshot diff viewer for failed runs. Add a GitHub Actions workflow that runs the full benchmark suite on a schedule and opens a PR with updated leaderboard data if any provider's success rate changes by more than 5 percentage points, keeping the leaderboard automatically up-to-date without manual intervention.", stack: ["Playwright", "Next.js"], difficulty: "Advanced", vibes: ["Benchmark", "Agents"] },
  { title: "GraphGuru", tagline: "Knowledge-graph builder from your docs.", description: "Drop a corpus, get an explorable knowledge graph with type inference.", prompt: "Build a Next.js app with a Python extraction backend where users upload a document corpus (PDF, Markdown, DOCX) and a pipeline chunks each document into 512-token passages, embeds them with text-embedding-3-small, and stores them in pgvector for retrieval. Run an LLM entity and relation extraction pass over each chunk using a structured JSON output schema that captures entity type, canonical name, and relation triples (subject, predicate, object), then deduplicate entities across chunks by clustering their embedding vectors and picking the most-frequent name as canonical, merging all incoming and outgoing edges in the process. Build a Cytoscape.js graph renderer in the frontend that lays out the merged entity graph using a force-directed layout, color-codes node types (Person, Organization, Concept, Event) with distinct neon palette entries, and supports zooming into subgraphs by double-clicking a node to expand its one-hop neighborhood. Implement semantic search over the graph: the user types a query, it's embedded and matched against entity embeddings, and the top-5 matching entities are highlighted in the graph with their connected edges pulsing to show paths between them, enabling exploratory navigation of the knowledge space. Add a LLM-powered Q&A panel that retrieves the most relevant entity neighborhoods for a user question via pgvector similarity search, feeds the subgraph triples as context, and streams an answer citing the specific entities and relations it used, making the graph an explainable RAG data source.", stack: ["Cytoscape", "Claude"], difficulty: "Advanced", vibes: ["KG", "Search"] },
  { title: "EnclaveExpress", tagline: "Run code in a TEE from a one-line API.", description: "AWS Nitro / Intel TGX abstracted as POST your script -> get attested result.", prompt: "Build a Rust-based orchestration service on an EC2 host that manages a fleet of AWS Nitro Enclaves, accepting API requests that include a container image hash and input payload, verifying the image hash against an allowlist before launching the enclave, and using the Nitro attestation document to prove to the caller which exact code ran. Implement the enclave-side runtime as a minimal Rust binary that receives the input payload over vsock, executes the user's containerized workload in a seccomp-sandboxed subprocess, captures stdout and exit code, signs the output with an ephemeral enclave key whose public key is included in the attestation document, and returns both the signed output and the attestation. Expose a developer-friendly REST API where a POST /run body includes container_image, input_data, and an optional timeout_seconds; the host process launches the enclave, waits for the result, and returns a response envelope containing the output, the attestation document, and a verification_url pointing to a public endpoint that any client can hit to independently verify the attestation chain against AWS root certificates. Build a TypeScript client SDK that wraps the API call, verifies the attestation locally using the AWS Nitro root certificate, checks that the enclave's PCR values match the expected values for the submitted image, and throws if anything doesn't match, giving application developers a single verified() call to trust TEE results. Add a dashboard page showing per-enclave uptime, average attestation latency, and a live log of recent runs with their verification status, letting platform operators monitor the TEE fleet health without exposing any run inputs or outputs.", stack: ["AWS Nitro", "Rust"], difficulty: "Advanced", vibes: ["Privacy", "Infra"] },
  { title: "FedFlow", tagline: "Federated learning sandbox for non-PhDs.", description: "Drag training data nodes around, watch the model learn without sharing data.", prompt: "Build a Next.js web app with a Python simulation backend where users create a federated learning experiment by dragging data-node cards onto a canvas — each card represents a simulated client with an assigned data shard from MNIST, CIFAR-10, or a user-uploaded CSV — and configure heterogeneity settings like label skew (Dirichlet alpha parameter) and data volume imbalance to simulate realistic non-IID conditions. Implement the FedAvg aggregation loop as a Python async server that sends model weights to each active node's simulated trainer, collects locally computed gradient updates after a configurable number of local steps, applies weighted averaging by data volume, and optionally clips gradients with DP-noise injection at a configurable epsilon budget. Visualize convergence in real time with a streaming Recharts panel showing global validation loss and accuracy per round, plus per-client loss curves with hover-to-expand so users can spot free-rider nodes or poisoning attacks by their divergent training trajectories. Add a partition simulator where users can temporarily disconnect a subset of nodes mid-training and observe how FedAvg degrades with fewer participants, then reconnect them and watch the global model recover, building intuitive understanding of federated resilience. Export the final aggregated model as an ONNX file with a one-click download, and generate a privacy report card showing the effective DP epsilon consumed across all training rounds so users understand the formal privacy guarantee before deploying.", stack: ["PyTorch", "Next.js"], difficulty: "Advanced", vibes: ["Privacy", "ML"] },
  { title: "QuantumQuiver", tagline: "Visual quantum-circuit playground.", description: "Drag gates onto qubits, see Bloch spheres update in real time.", prompt: "Build a browser-based quantum circuit editor in React where users drag standard gates (H, X, Y, Z, CNOT, Toffoli, Phase, T) from a palette onto a qubit-row timeline, with the circuit automatically recompiled and simulated in a Web Worker using Qiskit-WASM every time the circuit changes, keeping the UI thread responsive even for 8-qubit circuits. Render the quantum state after each gate layer as an array of Bloch spheres using Three.js — each single-qubit state is displayed as a point on the unit sphere with axes labeled, animating smoothly as the user adds or removes gates so they can develop geometric intuition for superposition and entanglement. Implement a state vector inspector panel that shows the full statevector as a bar chart of probability amplitudes sorted by basis state, highlighting basis states with non-zero imaginary components in a distinct color and displaying phase angles on hover to give a complete picture of the quantum state beyond just measurement probabilities. Add a measurement simulator that samples from the statevector distribution to produce a histogram of 1024 simulated measurement outcomes, displayed alongside the theoretical probability bars so users can see the gap between finite-sample measurement noise and the true distribution. Support shareable circuit URLs by serializing the gate sequence as a compact base64 JSON string in the URL hash, so users can share circuits with colleagues or embed them in blog posts without a backend.", stack: ["Qiskit-WASM", "Three.js"], difficulty: "Advanced", vibes: ["Quantum", "Edu"] },
  { title: "BlockBuilder", tagline: "Visual smart-contract IDE for Solidity beginners.", description: "Drag-and-drop modules compile to safe-by-default Solidity.", prompt: "Build a React drag-and-drop IDE styled like Scratch where each block corresponds to a safe smart-contract pattern — ERC-20 token, ERC-721 NFT, access control role, pausable emergency switch, payment splitter — and composing blocks generates syntactically valid Solidity that inherits from the matching OpenZeppelin base contracts, preventing common misconfigurations like missing reentrancy guards. Implement a real-time Solidity compiler using solc-js in a Web Worker that recompiles on every block change, streams compiler errors back to the canvas as red inline annotations on the offending block, and displays ABI and bytecode size in a status bar so beginners see the direct impact of each design choice. Run an automated security fuzzer using Foundry's fuzzing engine (invoked via a serverless function) against the generated contract: it generates 1000 random input combinations targeting overflow, underflow, and access-control bypass scenarios, then surfaces any found invariant violations as plain-English warnings with the specific input that triggered the bug. Build a Hardhat-based test deployment flow where clicking Deploy provisions a local Anvil fork of mainnet in a Docker container, deploys the generated contract, and opens an interactive ABI explorer panel so the user can call each function and observe state changes without writing test scripts. Add a Gas Lens overlay that annotates each block with its estimated gas contribution based on the compiled bytecode, turning the visual canvas into a gas profiler that teaches beginners which patterns are expensive before they hit a mainnet gas bill.", stack: ["Hardhat", "Next.js"], difficulty: "Advanced", vibes: ["Web3", "DX"] },
  { title: "MeshMaestro", tagline: "Photorealistic 3D mesh from a single phone video.", description: "Capture a 30s pan around your subject; get a Gaussian Splat scene.", prompt: "Build a Next.js web app where mobile users record a 30-second orbital video of a subject and upload it directly to S3 via a presigned URL, triggering a GPU-backed processing pipeline on a Replicate or Modal serverless GPU that runs COLMAP for structure-from-motion camera pose estimation followed by 3D Gaussian Splatting training for 7000 iterations to produce a .ply splat scene file. Implement a web-based 3DGS renderer using Three.js with a custom WebGL shader that reads the Gaussian splat .ply, sorts splats by depth per frame using a CPU-side sort on a Uint32Array in a Web Worker, and renders with alpha blending to achieve photorealistic novel views navigable via mouse orbit controls directly in the browser without any plugin. Send the user an email notification with a link when the job finishes, storing job state (pending, processing, done, error) in a Postgres table polled by the frontend with SWR so the waiting page updates automatically and shows elapsed processing time from a displayed timer. Add a quality selector at upload time (Draft: 3k iterations / Standard: 7k / High: 20k) that adjusts the Replicate GPU time and warns the user of the estimated processing duration and credit cost before submission, preventing surprise billing. Let users download the final .ply splat file for use in native 3DGS viewers, and generate a shareable iframe embed URL that loads the Three.js renderer with the splat pre-loaded so creators can embed their 3D captures directly in portfolio sites.", stack: ["Three.js", "GS"], difficulty: "Advanced", vibes: ["3D", "AI"] },
  { title: "SatelliteSpotter", tagline: "Identify objects in your sky from a phone tap.", description: "Point at a star, get the satellite/aircraft/planet name in real time.", prompt: "Build a Progressive Web App using the DeviceOrientation API to read gyroscope and compass heading data, combined with the Geolocation API for GPS coordinates, feeding both into a Sky Coordinate transform that converts the phone's pointing vector (altitude + azimuth) into right ascension and declination at the current UTC time using the VSOP87 algorithm for high-accuracy planetary positions. Fetch and cache Two-Line Element (TLE) sets for all tracked Earth satellites from Celestrak's active satellite catalog, propagating each satellite's current position using the satellite.js SGP4 library, and spatially index the computed az/alt positions using a k-d tree so a single tap on the sky sphere queries the nearest satellite, star, planet, or Messier object within a 2-degree cone in under 10ms. Render the sky overlay as a WebXR-compatible full-screen canvas overlaid on the device camera feed, drawing constellation stick figures from the Yale Bright Star Catalog in a faint neon grid, placing satellite trajectory arcs as animated dotted lines showing the next 5 minutes of predicted ground-track, and labeling the ISS with a special badge showing seconds to overhead pass. Implement push notifications (via the Push API) for ISS and Starlink train passes over the user's location: the service worker fetches the next 7 days of predicted passes from Heavens-Above's API on install, stores them in IndexedDB, and schedules a notification 10 minutes before each visible pass with the maximum elevation and compass bearing. Add a satellite detail sheet triggered by any tap that shows the object's NORAD ID, launch date, orbital period, inclination, and a live Cesium globe mini-map showing the current ground track, orbit, and the user's position to give context for the object's geometry relative to Earth.", stack: ["WebXR", "TLE"], difficulty: "Advanced", vibes: ["Astro", "AR"] },
  { title: "CodonCraft", tagline: "Visual gene-editor playground.", description: "Browser-only drag tool to design plasmids and optimize codons — no install required.", prompt: "Build a browser-only React app using the BioBricks registry data where users construct circular plasmid maps by dragging standard genetic parts (promoter, RBS, CDS, terminator, origin of replication, antibiotic resistance marker) onto a circular SVG canvas that auto-labels each element with its name, direction arrow, and base-pair length so the plasmid looks like a proper molecular biology diagram. Implement a restriction enzyme digestion simulator backed by a JSON database of 200+ restriction enzymes and their recognition sequences: users select one or more enzymes from a searchable dropdown, the app scans the current plasmid sequence for cut sites, marks them on the circular map with colored triangles, and renders a simulated gel electrophoresis lane on a side panel showing the expected fragment sizes as bands at correct relative positions on a log-scale migration axis. Add a codon optimization panel where the user selects a target expression organism (E. coli K-12, S. cerevisiae, CHO cell line), and the app looks up the organism's published codon usage table to replace rare codons in any selected CDS region with synonymous high-frequency codons, highlighting the changed positions in the sequence viewer and reporting the before/after Codon Adaptation Index score. Support FASTA and GenBank flat file import so users can paste real sequences from NCBI, with automatic feature annotation parsed from the GenBank FEATURES table rendered on the circular map with proper color-coding by feature type. Enable export to GenBank flat file format, Benchling-compatible JSON, and a shareable base64 URL so researchers can share their plasmid designs with collaborators who can open them instantly without installing any software.", stack: ["Next.js", "BioJS"], difficulty: "Advanced", vibes: ["Bio", "Edu"] },
  { title: "RoboRoam", tagline: "Train a wheeled robot in a browser sim, deploy to Raspberry Pi.", description: "Sim-to-real RL pipeline with code generation for the on-device controller.", prompt: "Build a Three.js physics simulation using Cannon.js where a configurable two-wheeled differential-drive robot navigates a maze environment, with the physics running at 60Hz in a Web Worker and the render thread consuming the state at display framerate, keeping the UI responsive during training; the robot's sensor suite includes four distance raycasts (front, front-left, front-right, rear) and wheel encoder odometry, matching the capabilities of a physical Raspberry Pi robot. Implement Proximal Policy Optimization (PPO) in TensorFlow.js running entirely in the browser: the policy network is a 3-layer MLP (sensor_dims → 64 → 32 → action_dims) that outputs wheel velocity commands, trained against a reward function that gives +1 for each meter traveled forward, -10 for wall collisions, and +100 for reaching the goal, with episode resets after collision or timeout. Visualize training progress with a live Recharts dashboard showing episode return, policy entropy, and value function loss across iterations, plus a heatmap overlay on the maze showing the agent's cumulative visitation frequency so users can see if the robot is stuck in local optima or exploring effectively. Generate deployable controller code when training is complete: export the trained TFLite flatbuffer, wrap it in a Python script that reads the Pi's GPIO-connected HC-SR04 ultrasonic sensors, runs inference using TFLite Runtime, and outputs PWM duty cycles to the motor driver, with a single pip install + python robot.py command to get the trained policy running on physical hardware. Include a domain randomization panel where users can add noise to sensor readings and randomize maze layouts across episodes during training, improving sim-to-real transfer and making the exported policy more robust to the inevitable discrepancies between the simulated and physical robot dynamics.", stack: ["Three.js", "tfjs"], difficulty: "Advanced", vibes: ["Robotics", "RL"] },
  { title: "FluxForge", tagline: "Drop-in image fine-tuner with LoRA budgets.", description: "Upload 20 photos, get a Flux LoRA in 6 minutes with quality preview.", prompt: "Build a Next.js web app where users upload 10-30 training images of a subject (person, product, style) to S3, then trigger a Replicate fine-tuning job using the Flux LoRA trainer with sensible defaults (rank 16, alpha 32, 1000 steps, learning rate 4e-4) while the frontend polls the job status every 5 seconds via SWR and displays a training progress bar estimated from elapsed time vs. expected duration. Implement budget-aware preset tiers displayed as radio buttons before training starts — Quick ($0.50, 500 steps, rank 8, ~3 min), Standard ($1.20, 1000 steps, rank 16, ~6 min), Quality ($2.80, 2000 steps, rank 32, ~14 min) — calculating costs from Replicate's published GPU-second pricing so users make an informed choice before spending credits. Automatically generate 8 validation samples upon training completion using the finished LoRA weights and a fixed set of diverse test prompts (studio portrait, outdoor, different lighting), displaying them as a 2×4 image grid comparison with the pre-training Flux base output in the same prompts so users can visually assess the quality of the fine-tune before sharing the weights. Store each user's LoRA runs in a Postgres table with the Replicate weights URL, training metadata, and a user-assigned project name, building a personal model library page where they can one-click load any saved LoRA into the generation playground without re-entering the weights URL. Add a generation playground where the user types a prompt, selects any of their saved LoRAs from a dropdown, adjusts LoRA strength (0.6–1.2) with a slider, and generates via Replicate's Flux inference endpoint with the selected weights, displaying results in a masonry grid with a copy-prompt button on each image for easy iteration.", stack: ["Replicate", "Next.js"], difficulty: "Intermediate", vibes: ["AI Image"] },
  { title: "SoCSurf", tagline: "Visual System-on-Chip explorer for FPGA newbies.", description: "Drag IP blocks together; preview Verilog with synthesis stats.", prompt: "Build a React drag-and-drop SoC composer using ReactFlow where users place pre-built IP blocks — a RISC-V CPU core (VexRiscv), UART controller, SPI master, GPIO bank, BRAM block, and Wishbone interconnect — onto a canvas and connect them by drawing Wishbone bus wires between master and slave ports, with the canvas validating connection legality (bus width match, no two masters on a slave port without an arbiter) and showing error badges on invalid connections. Generate synthesizable Verilog from the canvas state by templating each IP block's Verilog module instantiation and wiring them through a generated Wishbone crossbar switch, outputting a single top-level Verilog file with proper port declarations and parameter overrides readable by any FPGA toolchain. Invoke Yosys synthesis in a serverless Docker function when the user clicks Synthesize — passing the generated Verilog through yosys -p 'synth_ice40 -json' — and parse the JSON statistics file to extract LUT count, flip-flop count, BRAM usage, and estimated maximum clock frequency based on the critical path delay, displaying them as a resource utilization dashboard next to the canvas. Implement a timing analyzer that highlights the critical path by tracing from the Yosys stat JSON back to the originating IP blocks, drawing a colored overlay on the canvas edges along the longest delay path so users can visually identify which connection is limiting their clock speed. Add an educational sidebar that opens when the user clicks any IP block or wire, explaining what the selected component does, its key parameters, a link to the relevant spec sheet, and a short quiz question about bus arbitration or memory-mapped I/O so students build conceptual understanding alongside their circuit design.", stack: ["Yosys", "Next.js"], difficulty: "Advanced", vibes: ["Hardware", "Edu"] },
  { title: "AgenticAtlas", tagline: "Visual debugger for multi-agent LLM workflows.", description: "Watch tool calls, retries, and inter-agent messages on a live timeline.", prompt: "Build an OpenTelemetry-compatible instrumentation SDK in TypeScript and Python that wraps LLM client calls (Anthropic, OpenAI, Vertex) with automatic span creation, capturing model name, prompt tokens, completion tokens, latency, and the full message list as span attributes, and propagates trace context across async agent handoffs so the entire multi-agent call tree is captured under a single root trace ID. Ingest trace data via an OTLP HTTP collector endpoint into ClickHouse using its columnar storage for efficient range queries over high-cardinality span data, enabling the dashboard to render thousands of agent runs per second without aggregation approximations. Render the live timeline as a swimlane diagram in React using a custom canvas-based renderer: each agent gets a horizontal lane, LLM calls are drawn as colored bars sized by duration, tool calls branch downward as sub-bars from their parent LLM call with color-coded success/failure status, and inter-agent handoff arrows are drawn diagonally between lanes with message content shown on hover. Implement a replay mode where the user scrubs a timeline slider to step forward through a trace at any speed, seeing the current agent state, active tool calls, and pending messages at each timestamp, enabling post-mortem debugging of complex multi-hop reasoning chains that are impossible to understand from raw logs. Add anomaly detection that computes per-agent baseline latency and token usage from historical traces stored in ClickHouse, flagging spans that deviate more than 2 sigma as orange warnings in the timeline so engineers can spot runaway retry loops or unexpectedly expensive reasoning steps without manually scanning thousands of spans.", stack: ["OpenTelemetry", "React"], difficulty: "Advanced", vibes: ["Agents", "DevTools"] },
  { title: "PrivateBench", tagline: "Run benchmarks on private data with differential privacy.", description: "Compare model performance without exposing the eval set.", prompt: "Build a Python service where data owners register encrypted evaluation datasets by uploading AES-256-GCM encrypted JSONL files along with a dataset manifest (task type, num_examples, label schema) stored in Postgres — the plaintext never touches the server, and a dataset key is held only by the data owner who will provide it transiently at benchmark time via a short-lived signed URL. Implement a DP-protected scoring flow where models submit predictions via an API, the server decrypts the eval set in-memory for the duration of a single request, computes exact metrics (accuracy, F1, BLEU), adds calibrated Laplace noise scaled to the sensitivity divided by epsilon (configurable per dataset owner), and immediately discards the decrypted data before returning the noised score. Support multiple privacy accounting modes using the Google DP library: basic composition for simple budgets, Rényi DP for tighter analysis of repeated queries, and a budget ledger per (model_id, dataset_id) pair that rejects scoring requests once the configured epsilon is exhausted, protecting the eval set from information-theoretically extracting the labels through many repeated queries. Build a leaderboard frontend where submitted model scores appear with their DP epsilon and noise level annotated, so benchmark consumers know how much to trust each number — models queried with tight epsilon (e.g., ε=1) have wide error bars, while large-epsilon queries have tighter but less private scores. Add a data-owner analytics dashboard showing the remaining privacy budget for each model that has queried their dataset, query timestamps, and a budget depletion projection so owners can revoke access before their privacy budget is fully consumed by a single aggressive submitter.", stack: ["TFP", "Next.js"], difficulty: "Advanced", vibes: ["Privacy", "Eval"] },
  { title: "EmbedExplain", tagline: "UMAP visualizations with interactive explanations.", description: "Hover a cluster; the LLM explains what it represents based on samples.", prompt: "Build a Python FastAPI backend that accepts a JSONL or CSV file of texts or images, embeds them using the OpenAI text-embedding-3-small or CLIP model (user's choice), reduces to 2D using UMAP with configurable n_neighbors and min_dist parameters, and runs HDBSCAN clustering on the reduced embeddings to assign cluster labels, returning all projected coordinates, cluster IDs, and metadata fields to the frontend in a single response payload. Build a React frontend with a D3 force-simulation scatter plot that renders each point as a dot colored by cluster assignment using a neon color palette, supports rectangular lasso selection to define a custom region of interest, and renders a smooth convex-hull polygon around each detected HDBSCAN cluster so cluster boundaries are visually clear even in dense areas. Implement on-hover cluster explanation: when the user hovers a cluster hull, the frontend sends a sample of up to 20 random texts from that cluster to the LLM API and streams back a 2-sentence theme description (e.g., 'This cluster contains customer complaints about shipping delays...') displayed in a floating tooltip next to the cluster, giving instant interpretability without manually reading samples. Add a global/local zoom control where double-clicking a cluster zooms the viewport to fill it and re-runs UMAP locally on just that cluster's points with higher n_neighbors for finer sub-cluster resolution, enabling multi-level exploration from broad topic areas down to nuanced semantic subcategories in a single session. Support comparison mode where the user uploads two JSONL files (e.g., before/after fine-tuning) and the tool renders both embedding clouds in the same plot with semi-transparent overlapping dots and a difference heatmap overlay showing where the distributions diverged most, quantified as per-cluster membership shift percentages in a side panel.", stack: ["UMAP", "D3", "Claude"], difficulty: "Intermediate", vibes: ["MLOps", "EDA"] },
  { title: "CRDTConductor", tagline: "Visual sandbox for testing CRDT merges.", description: "Simulate replicas, partitions, and merges to debug your data model.", prompt: "Build a Next.js web app where users define a shared data structure — a collaborative text document, a todo list, a counter, or a key-value store — using Yjs or Automerge as the CRDT backend, and visually spawn up to 6 simulated replica nodes on a canvas as labeled boxes with a shared state inspector showing the current document contents of each replica in real time. Implement a network partition simulator where users draw partition lines across the canvas to sever communication between node groups, then make independent edits on both sides of the partition (typing into replicas, completing tasks, incrementing counters), with each replica's state diverging visually in the inspector panels as the disconnected edits accumulate. Animate the merge step: when the user removes a partition by erasing the line, the app runs the CRDT merge algorithm and animates the state flowing between reconnected replicas as a pulsing colored edge, showing each replica's document transitioning from its diverged state to the merged consensus state with a diff highlight showing which operations from each side won. Add an operations log panel that records every mutation as a structured entry (replica_id, timestamp, operation_type, payload) in causal order, visualizing the partial order as a DAG timeline so users can trace exactly which operation was applied first and understand why the CRDT converged to its specific merged state rather than a different one. Include a built-in invariant checker that runs after every merge and asserts CRDT convergence: all connected replicas must have identical states after full propagation; if they differ, the checker highlights the divergent replica in red and shows a JSON diff so users can immediately identify bugs in custom CRDT extensions they write as JavaScript plugins.", stack: ["Yjs", "Next.js"], difficulty: "Advanced", vibes: ["Distributed", "Edu"] },
  { title: "DepthDive", tagline: "Convert a 2D photo to a parallax 3D walkable scene.", description: "Use depth-anything to add parallax + free camera in browser.", prompt: "Build a Next.js web app where users upload a JPEG or PNG photo that is sent to a Replicate serverless function running Depth Anything V2, returning a 16-bit grayscale depth map at the same resolution as the input, with the depth values normalized to [0, 1] and stored alongside the original image in S3. Load both the color image and depth map as WebGL textures in a Three.js scene, then implement a displacement-mapped plane mesh where each vertex is shifted along the z-axis by the depth map value scaled by a configurable parallax intensity slider, creating a bumped 3D surface that represents the scene's geometry without requiring a full mesh reconstruction. Add a smooth orbit camera with mouse drag and trackpad pinch-to-zoom, constrained to a semi-spherical arc around the scene center so the user can tilt and pan to see objects in the background separate from foreground objects, experiencing the parallax depth without any VR hardware. Implement an inpainting seam fixer that detects exposed background pixels near the depth edges (where geometry stretches reveal empty areas) and fills them using a simple neighborhood-sampling approach, blending sampled pixels from the nearest background region to hide the stretching artifacts that appear at steep depth discontinuities. Add a background blur shader that reads the depth map to apply variable Gaussian blur intensity inversely proportional to depth, simulating shallow depth-of-field by keeping the foreground subject sharp while softening distant background elements, with a bokeh intensity slider controlling the maximum blur radius at infinite depth.", stack: ["Three.js", "Replicate"], difficulty: "Advanced", vibes: ["3D", "AI"] },
  { title: "RouteRover", tagline: "Self-driving simulation with your custom maps.", description: "Drop a city map, train a tiny perception/planning agent end-to-end.", prompt: "Build a Phaser 3 top-down driving simulation where users upload a section of OpenStreetMap data via the Overpass API query builder, parse the OSM XML to extract road geometries and intersections, and render them as a drivable map canvas with lane markings, stop signs, and traffic lights placed at intersection nodes according to the OSM highway=traffic_signals tag. Implement a simulated sensor suite for the autonomous vehicle: four forward-facing raycasts for obstacle and lane detection, a minimap showing the global route, and a velocity sensor, all represented as a fixed-size observation vector that is fed into an ONNX.js inference session at 30Hz without blocking the render thread. Train a planning agent using a Python imitation-learning pipeline (behavioral cloning from recorded expert demonstrations) that outputs steering angle and throttle commands as a regression task, export the trained model as an ONNX file, and load it in the browser for real-time inference so the trained agent can navigate the user's custom map without any server round-trip. Add an episode recorder that captures observation-action pairs when the user manually drives the vehicle with arrow keys, accumulating a training dataset that improves with each recording session, and a Dataset Manager panel showing recorded episode count, total timesteps, and a Download Training Data button that exports the dataset as a JSONL file for offline training in Colab. Implement traffic agents — other vehicles that follow Bezier-curved routes with configurable density and speed — so the trained policy must handle dynamic obstacles rather than an empty road, and display a violation counter that increments whenever the agent runs a red light, crosses a lane divider, or collides with traffic, giving a quantitative safety metric alongside the episode completion rate.", stack: ["Phaser", "ONNX.js"], difficulty: "Advanced", vibes: ["AV", "RL"] },
  { title: "TinyServer", tagline: "Run a Postgres-compatible DB on Workers.", description: "Educational re-implementation of basic PG protocol on edge runtime.", prompt: "Build a Cloudflare Worker that implements the PostgreSQL wire protocol (version 3.0) over TCP using Workers' TCP socket API: handle the startup message, authentication (trust mode for educational use), and the simple query flow where the client sends a Query message and the server responds with RowDescription, DataRow, and CommandComplete messages formatted exactly per the PostgreSQL frontend/backend protocol specification. Implement an in-memory SQL execution engine backed by Cloudflare Durable Objects for per-session table state, supporting a subset of SQL including CREATE TABLE, INSERT INTO, SELECT with WHERE/ORDER BY/LIMIT, UPDATE, and DELETE, with column type enforcement for TEXT, INTEGER, REAL, and BOOLEAN so students work with a typed system rather than a schemaless toy. Parse SQL using a hand-written recursive-descent parser that produces an AST, execute the AST against the Durable Object state, and format results using the PG wire protocol binary encoding, enabling real psql and pg npm package connections to the Worker endpoint without any client-side modification. Add a protocol inspector endpoint that returns a human-readable log of the last 50 PG wire protocol messages exchanged with any client, formatted as a message-by-message breakdown showing message type, byte offset, and parsed field values so students can watch their psql commands become protocol frames in real time. Include a series of guided exercises embedded in the Worker's landing page: each exercise asks the student to issue a specific SQL command, validates that the Worker received and correctly executed it by checking the Durable Object state, and awards a checkmark badge, building up from CREATE TABLE through multi-table JOIN (stretch challenge) so the project doubles as interactive database curriculum.", stack: ["Workers", "Durable Objects"], difficulty: "Advanced", vibes: ["DB", "Edu"] },
  { title: "ChainVision", tagline: "On-chain anomaly detection for DeFi treasury.", description: "Alerts when wallet flows look like a known exploit pattern.", prompt: "Build a TypeScript monitoring service that subscribes to real-time Ethereum events via Alchemy or Infura WebSocket subscriptions, indexing all ERC-20 Transfer and ETH transfer events involving a configurable set of treasury wallet addresses into a PostgreSQL database with pgvector, storing each transaction as a node in a directed transaction graph. Represent each wallet's transaction neighborhood as a graph embedding by running a node2vec-style random walk over the transaction graph and encoding the resulting walk sequences with a lightweight embedding model, then computing cosine similarity between incoming wallet embeddings and a curated library of known exploit signatures (reentrancy patterns, flash-loan sandwiches, pump-and-dump wallet clusters) stored in pgvector. Trigger Slack webhook alerts when similarity to any exploit signature exceeds 0.85, formatting the alert as a rich message block showing the wallet address, matched signature name, similarity score, the top-3 most suspicious transactions with Etherscan links, and a recommended action (freeze treasury, require 2-of-3 multisig approval for the next 24h). Build a Next.js dashboard showing a live D3 force-directed graph of the last 100 treasury-adjacent transactions, color-coding wallets by risk score (green/yellow/red) and rendering exploit-pattern matches as pulsing red edges, with a time-scrubber slider to replay any 24-hour window from historical data. Add an on-chain report endpoint that generates a PDF treasury health report on demand, summarizing net inflows and outflows by token, top counterparties by volume, number of anomaly alerts in the period, and a risk-score trend chart, suitable for sharing with a DAO governance forum or multisig co-signers.", stack: ["Ethers.js", "GraphQL"], difficulty: "Advanced", vibes: ["Web3", "Security"] },
  { title: "PyrolyticPlot", tagline: "Run ad-hoc Python in the browser via Pyodide.", description: "Drop CSV, ask in English, get matplotlib charts back.", prompt: "Build a Next.js app with a split-pane UI where users upload a CSV or paste JSON data on the left, and the right pane contains a chat interface backed by Claude claude-haiku-4-5 that generates Python data-analysis code using pandas and matplotlib based on natural-language requests like 'show me a histogram of the age column by gender with viridis colormap.' Load Pyodide in a Web Worker and execute the LLM-generated code in an isolated micropython environment with a restricted __builtins__ that blocks os, subprocess, socket, and importlib to prevent sandbox escapes, executing only whitelisted data-science imports. Capture matplotlib figure output by overriding the plt.show() function in the Pyodide environment to call a JavaScript callback that receives the figure as a base64 PNG, then display it in the chat pane as an inline image with a download button and an expand-to-full-screen overlay, building a seamless notebook-like experience without Jupyter. Maintain a shared DataFrame state across chat turns by serializing the current df to an Arrow IPC buffer after each code execution, passing it back to the worker's namespace before the next execution so users can run iterative transformations ('now normalize the numeric columns') that build on prior steps without re-uploading the data. Add a code transparency toggle that shows the generated Python code for every chart as a collapsible code block with syntax highlighting, letting users learn from the generated code, copy it to a Jupyter notebook for further exploration, or spot when the LLM generated something logically wrong before it executes.", stack: ["Pyodide", "Claude"], difficulty: "Advanced", vibes: ["Data", "AI"] },
  { title: "RealtimeREST", tagline: "Auto-realtime any REST API.", description: "Plug a REST endpoint, get a websocket of changes via change-data-capture.", prompt: "Build a Node.js proxy server where users register a REST API endpoint URL along with a polling interval and an optional JSON path selector — the proxy polls the endpoint on a schedule using node-cron, parses the response, extracts the selected data subset if a path is specified, and diffs it against the previous response using a deep-structural diff algorithm (json-diff) to identify added, changed, and removed fields. Push delta events to connected WebSocket clients as structured JSON messages containing the operation type (add/change/remove), the JSON path of the changed field, the old value, the new value, and a sequence number so clients can detect dropped messages and request a full resync. Implement a replay buffer in Redis using a sorted set keyed by sequence number per registered endpoint, keeping the last 1000 delta events, so WebSocket clients that reconnect after a drop can request all missed events since their last sequence number rather than receiving only a full snapshot. Build a JavaScript client library (npm package) that wraps the WebSocket connection, handles reconnection with exponential backoff, optionally applies missed deltas from the replay buffer to a local state object using Immer, and exposes a React hook (useRealtimeEndpoint) that returns the current live state so frontend developers can turn any REST API into a live-updating data source with three lines of code. Add a dashboard page listing all registered endpoints with their polling interval, last polled time, current delta rate (events/minute), and a real-time event stream preview showing the last 10 raw delta events as a monospace scrolling log, so operators can verify the proxy is correctly tracking changes before shipping to production.", stack: ["Next.js", "Redis"], difficulty: "Advanced", vibes: ["Infra"] },
  { title: "WhisperWatch", tagline: "Local-only desktop dictation that beats Siri.", description: "Hotkey to dictate; Whisper.cpp on-device, no audio leaves your machine.", prompt: "Build a Tauri desktop app with a Rust backend that registers a global keyboard shortcut (default: Ctrl+Shift+Space on Windows/Linux, Cmd+Shift+Space on macOS) using the tauri-plugin-global-shortcut crate, capturing the shortcut even when the app window is not focused, and recording microphone audio to a WAV buffer in memory using the cpal crate for cross-platform audio device access. Bundle whisper.cpp compiled as a Rust static library using a build.rs script that downloads the appropriate GGML model file (whisper-base.en or whisper-small) into the Tauri resource directory at first launch, runs transcription entirely on CPU in a background thread using the whisper-rs crate bindings so transcription never requires network access and works completely offline. Transcribe the recorded audio upon hotkey release, and paste the resulting text into whatever text field has OS focus using the clipboard + OS-level keyboard event injection: on macOS via the Accessibility API (AXUIElement), on Windows via the SendInput Win32 API call, and on Linux via xdotool, creating a seamless paste experience without requiring the user to manually Ctrl+V. Show a compact floating indicator window near the system tray that appears during recording (animated waveform visualization drawn using egui) and displays a transcription confidence score and word count after each paste, letting the user know the dictation was captured successfully before the window auto-dismisses after 2 seconds. Provide a settings panel accessible from the system tray menu where users can select the model size (base/small/medium with download progress bars), configure the hotkey, toggle auto-punctuation using a secondary GPT-2-tiny pass, and view a session history log of the last 50 dictations stored locally in SQLite for reviewing or re-pasting earlier transcriptions.", stack: ["Tauri", "Whisper.cpp"], difficulty: "Advanced", vibes: ["Privacy", "Voice"] },
  { title: "ProofPanel", tagline: "Interactive theorem-proving puzzles for devs.", description: "Lean / Coq mini-puzzles delivered weekly with hints.", prompt: "Build a Next.js web app that compiles and runs Lean 4 proofs in the browser using Lean4Web (a WASM-compiled Lean kernel), presenting weekly theorem-proving puzzles categorized by topic — propositional logic, natural number arithmetic, list operations, basic algebra — with each puzzle defined as a Lean 4 theorem statement with a sorry placeholder that the user must replace with a valid proof term. Implement a step-by-step hint system that reveals up to 5 progressive hints per puzzle stored in a JSONL file, where each hint adds one more line or tactic to a skeleton proof, so users who are stuck can see the next logical step without getting the full solution handed to them, and earning a solution without any hints awards a gold badge vs. silver for hint-assisted solutions. Track user progress in a Postgres database authenticated via GitHub OAuth, recording which puzzles each user has solved, how many hints they used, and their solving time, surfacing a weekly leaderboard of fastest solves and a personal streak counter that motivates daily practice. Add a proof checker that validates the submitted proof not just for syntactic compilation but also for absence of sorry, axiom, and native_decide tactics that bypass proof obligations, ensuring users write genuine proofs rather than exploiting escape hatches, with a lint error message explaining why each banned tactic is disallowed. Implement a community proof gallery where solved puzzles display anonymized proof submissions from other users after the user has solved the puzzle themselves, ranked by elegance (shorter proof term length), encouraging users to study idiomatic Lean 4 tactic style by reading accepted community solutions.", stack: ["Lean 4 WASM"], difficulty: "Advanced", vibes: ["Math", "Edu"] },
  { title: "ZeroKnowU", tagline: "Hands-on intro to ZK proofs.", description: "Build your first Circom circuit in a browser IDE, with worked exercises.", prompt: "Build a Next.js web app with a Monaco editor pane pre-loaded with Circom circuit templates where users write Circom 2.0 circuits, and a browser-side compilation pipeline that invokes the circom WASM compiler to generate a R1CS constraint system and a WASM witness calculator, catching and displaying syntax/constraint errors inline in the editor with highlighted line numbers and plain-English explanations of what each constraint violation means. Implement witness generation in the browser by loading the circom-generated WASM witness calculator, letting users fill in private and public input values in a form panel, computing the witness vector client-side, and displaying the intermediate signal values in a scrollable signal inspector so students can verify that their circuit computes what they intended before generating a proof. Generate a Groth16 proof using snarkjs.js in a Web Worker — loading a pre-computed trusted setup (.zkey file) stored on S3 for each exercise circuit — and display the proof object as formatted JSON alongside a verification call that checks the proof against the verification key and the public inputs, showing a green Proof Valid or red Proof Invalid badge within a few seconds. Structure the learning path as a sequence of 10 curated exercises stored in a YAML file: Hash Preimage, Merkle Membership, Range Proof, Sudoku Constraint, Age Gate without Revealing Birthday, and more — each with a problem statement, a partial circuit with TODOs, worked expected output, and a conceptual explainer linking ZK theory to the specific circuit technique used. Add a gas estimator that calls the snarkjs generateSolidityVerifier function, deploys the Solidity verifier to an in-browser Anvil testnet fork using ethers.js, submits the proof on-chain, and displays the gas cost alongside a note explaining that cheaper proof systems (Plonk, STARK) would further reduce this cost, grounding the abstraction in concrete Ethereum economics.", stack: ["Circom", "snarkjs"], difficulty: "Advanced", vibes: ["Crypto", "Edu"] },
  { title: "SignalSpa", tagline: "DSP playground for audio engineers.", description: "Compose biquads, FFTs, and convolutions visually; preview live.", prompt: "Build a React node-editor using ReactFlow where each node represents a DSP processing stage — source nodes (sine/square/sawtooth oscillator, pink noise, microphone input, WAV file loader), transform nodes (biquad filter, FFT, IFFT, convolution reverb, dynamic range compressor, ring modulator), and sink nodes (speakers, spectrum analyzer, waveform oscilloscope) — and drawing edges between nodes routes the audio signal through the connected processing chain. Implement the audio processing graph using the Web Audio API's AudioWorklet processor for custom DSP nodes and native AudioNode types for standard processors, compiling the user's visual graph into an AudioNode network in real time whenever the topology changes, so the output audio updates within a single audio buffer (128 samples) of any node parameter change. Render real-time DSP visualizations for sink nodes using canvas: the spectrum analyzer draws a live FFT magnitude plot using analyzer.getFloatFrequencyData() with user-configurable dB range, and the oscilloscope draws the time-domain waveform with trigger level control for stable periodic waveform display, both running at 60fps without impacting audio thread performance. Parameterize each node with per-node control panels that appear on click: a biquad node exposes filter type (lowpass/highpass/bandpass/notch), cutoff frequency, and Q factor as sliders with real-time AudioParam automation so the frequency response plot (a Bode magnitude curve rendered in SVG) updates continuously as the user drags the slider. Add a patch save system that serializes the entire node graph topology and all node parameters to a compact JSON object stored in the URL hash, enabling shareable patch URLs so audio engineers can post their DSP chains to forums or Twitter and recipients can open and hear the same sound chain instantly in any modern browser without installing anything.", stack: ["AudioWorklet", "ReactFlow"], difficulty: "Advanced", vibes: ["DSP", "Audio"] },
  { title: "TimeSeriesTwin", tagline: "Generate digital twins of metrics for chaos testing.", description: "Match a real metric distribution and replay scaled into your staging.", prompt: "Build a Python FastAPI service that accepts a production metrics export (Prometheus remote_write, InfluxDB line protocol, or CSV) and learns a generative model of each metric using Facebook Prophet for trend/seasonality decomposition plus a VAR (Vector AutoRegression) model for cross-metric correlations, storing the fitted parameters per metric in Postgres so the learned distribution can be persisted and versioned. Implement a sampling engine that generates new time-series segments from the fitted model at any configurable target date range and scaling factor — for example, synthesize a 2× Black Friday traffic spike on a Thursday afternoon to stress-test the staging environment before the real event, preserving realistic correlations between CPU usage, request rate, and error rate rather than scaling each metric independently. Expose a Prometheus remote_write compatible /write endpoint that replays the generated metrics into a staging Prometheus instance at configurable speed (1x, 2x, 10x), using a token-bucket rate limiter to match the exact timestamps and inter-arrival distribution of the original production data so chaos tests experience realistic bursty patterns rather than uniform synthetic load. Build a quality report that runs after generation, computing DTW (Dynamic Time Warping) distance between a sample of the original and generated series, autocorrelation function overlap, and spectral coherence, displaying all three as gauge charts on a Next.js dashboard so engineers can decide whether the twin is statistically faithful enough for their test scenario. Add a scenario library where teams save named scenarios (Black Friday, midnight batch job, weekly dip) as re-runnable configs with the fitted model version and scaling parameters frozen, building a library of reproducible chaos scenarios that new team members can launch with a single API call without needing to understand the underlying generative model.", stack: ["NumPy", "Next.js"], difficulty: "Advanced", vibes: ["Reliability"] },
  { title: "MicroLLM", tagline: "Train a 10MB language model on your domain.", description: "Sub-100MB transformer training pipeline for your jargon.", prompt: "Build a Next.js web app backed by a Modal serverless GPU function where users upload a plain-text corpus (technical docs, Discord logs, code comments, emails) and configure a nano-transformer architecture — 6 layers, 6 attention heads, 384 embedding dimensions — that fits comfortably within 100M parameters and trains in under 30 minutes on a single A10G GPU, making domain adaptation accessible without a GPU cluster. Implement a BPE tokenizer trained fresh on the user's corpus using the Hugging Face tokenizers Rust library, building a vocabulary of configurable size (512–4096 tokens) that captures domain jargon as single tokens (e.g., 'photolithography' or 'drawdown') rather than splitting into many subwords, dramatically reducing sequence lengths for specialized corpora and improving downstream perplexity on in-domain text. Stream training progress to the frontend via Server-Sent Events: each epoch pushes train loss, validation loss, tokens/sec throughput, and a sample continuation from the current checkpoint (given a fixed prompt) so users can watch the model improve in real time and abort early if the loss has plateaued. Export the trained model checkpoint as a GGUF file (4-bit quantized using llama.cpp convert-hf-to-gguf.py) that weighs 8–25MB depending on architecture, downloadable directly from the app, runnable locally with llama.cpp in a single terminal command, and loadable into a chat playground page on the app itself served via a serverless llama.cpp inference function. Display a domain perplexity benchmark card after training that evaluates the mini-LLM against GPT-2-small and distilGPT-2 on 1000 held-out sentences from the user's corpus, showing that even a tiny model trained on domain text beats general models on in-domain perplexity, giving users a concrete reason to care about custom language models over generic ones.", stack: ["transformers", "GGUF"], difficulty: "Advanced", vibes: ["LLM", "Tiny"] },
  { title: "P2PResearch", tagline: "Decentralized peer review for arXiv preprints.", description: "Sign reviews with your ORCID; build reputation graph over time.", prompt: "Build a Next.js app with ORCID OAuth login where authenticated researchers can submit peer reviews for any arXiv preprint (identified by arXiv ID), with each review structured as a JSON document containing the arXiv ID, ORCID of the reviewer, review text, ratings on reproducibility/novelty/clarity/significance, a confidence score, and the IPFS CID of a previous review if this is a public rebuttal or revision note. Sign each review object client-side using the researcher's browser-generated Ed25519 key pair (private key stored in browser localStorage, public key registered against their ORCID profile in a Postgres mapping table), and upload the signed JSON to IPFS via Web3.Storage, making the review permanently addressable and tamper-evident without relying on a centralized server to store content. Build a reputation graph in Postgres using a DAG data model where nodes are ORCID identifiers and edges represent citing-reviews (author → reviewer who cited their work), endorsements (reviewer → reviewer explicitly endorsed), and expertise claims (reviewer → arXiv subject category), running a lightweight PageRank variant on the graph weekly to compute per-field reputation scores that surface expert reviewers in each subfield. Implement a paper discovery page that embeds arXiv abstracts using the OpenAI embedding API, stores them in pgvector, and ranks search results by a composite score of semantic relevance to the user's query and review coverage (number of accepted reviews), surfacing well-reviewed papers prominently and flagging papers with zero reviews as needing community attention. Add a disagreement tracker that detects when two reviews of the same paper give opposite recommendations (accept vs. reject), groups them as a controversy thread linked bidirectionally, and displays the disagreement prominently on the paper's review page with a voting mechanism where logged-in researchers cast public votes on which review they find more compelling, creating a lightweight adversarial peer-review layer.", stack: ["IPFS", "Next.js"], difficulty: "Advanced", vibes: ["Science", "Web3"] },
  { title: "OnDeviceVision", tagline: "Run object detection in <10ms on a phone.", description: "Calibrate a YOLO-tiny via WebGPU for camera-based tasks.", prompt: "Build a Progressive Web App that loads YOLOv8-nano weights as an ONNX model and runs inference via the ONNX Runtime Web library with the WebGPU execution provider, binding the model to the device's GPU using the WebGPU API's GPUDevice and running inference on every video frame from getUserMedia without any server round-trips, achieving sub-10ms latency on modern phones with GPU support. Implement a real-time detection overlay by rendering bounding boxes and class labels directly on an HTML5 canvas layered over the camera video element, using requestAnimationFrame to synchronize canvas draws with browser compositing, and displaying an FPS counter computed as a rolling 30-frame average in the corner of the canvas to give users live performance feedback. Add a class filter UI with toggle chips for each of the 80 COCO classes so users running specific tasks (e.g., detecting only 'person' or 'bottle') can suppress irrelevant detections and reduce visual noise; also add a confidence threshold slider from 0.2 to 0.9 that updates the post-processing NMS threshold in real time without reloading the model. Implement a custom-class fine-tuning flow: the user taps a Capture button to save 50 bounding-box-annotated frames for a new object class, which are uploaded to a serverless Replicate function running YOLOv8 fine-tuning with transfer learning from the nano backbone, returning an updated ONNX weights file that the app caches in IndexedDB and loads on next launch, replacing the base COCO model with the personalized one. Add a benchmark mode that cycles through a test set of 100 reference images stored in the app bundle, runs inference on each, computes mAP@50 for the active model, and displays a comparison card showing the base YOLOv8-nano mAP vs. the fine-tuned model mAP side-by-side so users can quantify the improvement from their custom training data.", stack: ["WebGPU", "YOLO"], difficulty: "Advanced", vibes: ["Vision", "Edge"] },
  { title: "DataRoom", tagline: "Run SQL across multiple cloud DBs without ETL.", description: "Federated query engine that pushes down to Postgres, BQ, Snowflake.", prompt: "Build a Rust or Java query engine using Apache Calcite's SQL parser and planner that ingests a schema registry YAML defining tables across multiple cloud data sources — Postgres, BigQuery, and Snowflake — and exposes a single JDBC/SQL endpoint where users write cross-source JOINs as if all data were local, with the planner splitting the query into source-specific subqueries using each source's native SQL dialect. Implement predicate pushdown by walking Calcite's RelNode plan tree and converting filter conditions that reference only a single source's columns into native WHERE clauses pushed down in the subquery, minimizing the data transferred over the network to the query engine by filtering at the source rather than after fetching full tables. Execute subqueries in parallel using a thread pool: the query engine sends BigQuery jobs via the BigQuery REST API, Postgres queries via the JDBC driver, and Snowflake queries via the Snowflake JDBC driver simultaneously, then joins the result streams in memory using a sort-merge join algorithm with spill-to-disk when the join exceeds a configurable memory limit. Expose the federated query engine as a REST API endpoint that accepts a SQL string and returns paginated JSONL rows, and build a Next.js query editor using CodeMirror with SQL autocompletion that pulls the unified schema from the registry, letting analysts write cross-source queries in a web IDE and download results as CSV or Parquet without leaving the browser. Add a query plan visualizer that renders Calcite's final RelNode tree as an interactive D3 tree diagram annotated with estimated row counts (from collected table statistics), cost estimates, and pushed-down predicate badges on each source scan node, giving analysts visibility into how the query was optimized and where the engine expects most data to flow.", stack: ["Calcite-style", "Java/Rust"], difficulty: "Advanced", vibes: ["Data", "Infra"] },
  { title: "SpaceWalker", tagline: "Browse the latest cosmology preprints in 3D.", description: "Topic constellations from arXiv hep-ph using force-graph layout.", prompt: "Build a Next.js data pipeline that fetches the last 30 days of arXiv preprints across astrophysics (astro-ph), high-energy physics (hep-ph), and cosmology (gr-qc) categories using the arXiv API, embeds each abstract with text-embedding-3-small, stores the 1536-dimension vectors in pgvector with metadata (arXiv ID, title, authors, date, subject category), and runs this pipeline nightly via a Vercel cron job to keep the corpus fresh. Cluster the embedding space using UMAP dimension reduction to 3D coordinates followed by HDBSCAN clustering with a minimum cluster size of 5, assigning each cluster a topic label generated by prompting Claude with 10 random abstracts from the cluster and asking for a 3-word topic summary, storing the cluster label alongside each paper's 3D coordinates. Render the paper constellation in Three.js as a 3D force-directed graph: each paper is a glowing point particle, cluster boundaries are semi-transparent convex hulls, and particles are connected by edges to their 5 nearest embedding neighbors with edge opacity proportional to cosine similarity, creating visible strands between related papers that look like cosmic filaments. Implement orbit controls for free 3D navigation and a click-to-expand behavior: clicking any point pauses the rotation, zooms the camera toward the selected paper, and expands a floating info card showing the title, authors, abstract summary, publication date, and a Read on arXiv button, while dimming all papers outside the selected paper's 2-hop neighbor cluster. Add a personalized reading queue backed by a Postgres table where users authenticate via GitHub OAuth, bookmark papers by clicking a star icon on the info card, and access a side panel queue showing all bookmarked papers sorted by date with a weekly digest email generated by an LLM summarizing the themes across their bookmarked papers from the past 7 days.", stack: ["Three.js", "Embeddings"], difficulty: "Advanced", vibes: ["Science", "Visualization"] },
  { title: "CypherCity", tagline: "Generative dungeon mapping with provable randomness.", description: "VRF-seeded procedural maps for tabletop RPG fairness.", prompt: "Build a Next.js dungeon-generation service backed by Chainlink VRF (or a self-hosted Verifiable Random Function using ECVRF from the draft-irtf-cfrg-vrf spec) where each dungeon session is started by generating a VRF output — a pseudorandom value paired with a cryptographic proof — stored in Postgres alongside the session ID, giving the dungeon master a publicly auditable seed that players can independently verify was generated fairly without the DM's input. Feed the VRF output into a multi-pass dungeon generator: first a Binary Space Partitioning room placement algorithm to lay out non-overlapping chambers, then a minimum-spanning-tree corridor connector using Prim's algorithm over the room-center graph to ensure every room is reachable, then a detail pass that seeds monsters, treasure, and traps from the remaining VRF-derived entropy with encounter tables pulled from a YAML config file. Render the dungeon as an interactive SVG map in the browser using D3, with rooms drawn as rounded rectangles, corridors as paths, and icons for each entity type (skull for monster, chest for treasure, X for trap) — the DM view shows all icons while a shareable player view URL omits unexplored rooms using a fog-of-war mask stored as a Postgres bitmask updated by the DM clicking room-reveal buttons. Generate a VRF audit log PDF on demand that lists the session ID, VRF input (request hash), VRF output (random bytes hex), proof bytes, and a verification command (using a provided Python script) that any player can run locally to confirm the seed was not manipulated, providing a printable fairness certificate for tournament or competitive play. Add a campaign history page where DMs can browse all past generated dungeons with their seeds, see which rooms were explored in each session, replay the dungeon in the browser as a step-by-step reveal animation synchronized with session notes they typed into a companion text field, building an auditable record of an entire campaign.", stack: ["Three.js", "VRF"], difficulty: "Advanced", vibes: ["TTRPG", "Crypto"] },
  { title: "TinyShard", tagline: "Sharded SQLite over libSQL for indie scale.", description: "Auto-sharding of SQLite by tenant id with consistent reads.", prompt: "Build a Node.js TypeScript library that wraps the libSQL client (@libsql/client) and implements a consistent hashing ring using xxhash of the tenant_id column value to route every query to one of N configurable shard databases hosted on Turso, ensuring that a given tenant's data always lands on the same shard and cross-tenant queries never mix data at the application layer. Implement a query router that parses incoming SQL strings using a lightweight SQL AST parser (node-sql-parser), extracts the tenant_id from WHERE clauses or INSERT column lists, computes the shard assignment, and rewrites the query if necessary to ensure it targets the correct shard's libSQL connection from a pre-initialized connection pool. Support read replicas per shard by maintaining a primary and one or more replica libSQL connection strings per shard in the config, routing SELECT queries round-robin across replicas for load distribution and routing INSERT/UPDATE/DELETE queries exclusively to the primary, with a configurable read-your-writes consistency mode that routes all queries in a request session to the primary after the first write to prevent stale reads. Expose a Drizzle ORM compatible adapter by implementing the Database interface expected by drizzle-orm's libsql driver, patching the query execution path to intercept calls and route them through the sharding layer transparently, so applications using Drizzle can adopt TinyShard by changing one import line without rewriting any query code. Include a shard rebalancing CLI command that detects uneven shard utilization (>20% row-count imbalance), generates a migration plan listing tenants to move between shards, and executes the migration shard-by-shard with a quiescence period per tenant (blocking writes for under 100ms using an optimistic lock) to achieve zero-downtime rebalancing without requiring a maintenance window.", stack: ["libSQL", "Node"], difficulty: "Advanced", vibes: ["DB", "SaaS Infra"] },
  { title: "SchemaScribe", tagline: "Generate ETL schemas from sample JSON.", description: "Drop messy JSON, get a normalized SQL schema + import script.", prompt: "Build a Next.js web app where users paste or upload sample JSON (a single object or an array of objects) and a TypeScript inference engine traverses the parsed AST to collect all unique key paths, infer column types (string, number, boolean, date, JSON) by sampling the first 100 values per key and applying type-frequency voting, and detect nullable fields by checking if any sampled value is null or missing. Apply a normalization pass that detects repeated nested object structures (e.g., an address sub-object appearing in every record) and proposes extracting them into a separate dimension table with a foreign key reference, presenting the normalization suggestion as a diff between the flattened and normalized schema with an accept/reject toggle per suggestion so users control the final shape. Generate a CREATE TABLE SQL script for the accepted schema using Postgres syntax with appropriate NOT NULL constraints for fields that were never null in the sample, TIMESTAMPTZ for date-inferred columns, and TEXT vs. VARCHAR(255) chosen based on the maximum observed value length plus a safety margin, formatted with pgFormatter for readable output. Emit a one-click import script as a Node.js file using the pg npm package that reads the original JSON from stdin, maps each record to the normalized table structure (including splitting nested objects to their dimension tables), and runs the inserts inside a single transaction with ON CONFLICT DO NOTHING for idempotent re-runs, complete with a connection string placeholder and a usage comment at the top. Add a Claude-powered schema annotation pass that prompts the model with the inferred column names and types to generate human-readable column descriptions (e.g., 'customer_since_date: The date the customer first created an account') in a SQL COMMENT ON COLUMN format, making the generated schema self-documenting for anyone who later needs to understand the data model.", stack: ["Next.js", "Claude"], difficulty: "Intermediate", vibes: ["Data", "DX"] },
  { title: "AgentArena", tagline: "Pit AI agents against each other in mini-games.", description: "Each agent connects via WebSocket; arena referees, leaderboards.", prompt: "Build a Node.js WebSocket arena server where competing agents authenticate with an API key and join a named game room, receiving game state payloads as JSON and responding with action payloads within a configurable timeout window (default 5 seconds) or being assigned a forfeit action, supporting at least three deterministic mini-games: a negotiation game (multi-round prisoner's dilemma variant), a trivia game (fastest correct buzzer wins the point), and a coding race (agents emit solution code strings evaluated by a sandboxed Node.js vm.Script for correctness and scored by passing test cases). Implement the referee as a state machine per game that validates each action against the game rules, advances game state, logs every action with its timestamp and agent ID to a Postgres event log, and broadcasts the updated state to all room participants after each round, ensuring games are fully deterministic so any run can be replayed from the event log. Build a Next.js spectator page that connects to the arena via a read-only WebSocket channel, renders the live game state as a game-specific visualization (a payoff matrix with highlighted choices for negotiation, a leaderboard ticker for trivia, a live code diff viewer for coding race), and shows a real-time commentary sidebar with LLM-generated play-by-play text generated from the action stream. Maintain a persistent Elo rating system in Postgres per agent per game type, updating ratings after each match using the standard 400-point K=32 formula, and publishing a public leaderboard page at /leaderboard showing current ratings, win/loss records, and a recent match feed with outcome summaries so the AI research community can compare submitted agents. Add a submission portal where teams upload a Docker image containing their agent (must expose a WebSocket server on port 8080), the arena spawns it as a short-lived container per match using the Docker SDK, proxies its WebSocket to the game room, and tears down the container after the match ends, keeping the competition environment clean and preventing agents from persisting cross-match state.", stack: ["Next.js", "WebSockets"], difficulty: "Advanced", vibes: ["Agents", "Game"] },
  { title: "RAGRanker", tagline: "Bench RAG pipelines with one config file.", description: "Define your retriever+rerank+model; get RAG-eval scores out.", prompt: "Build a Python CLI tool where users define their RAG pipeline in a YAML config file specifying retriever type (BM25, dense pgvector, hybrid with RRF score fusion), optional reranker (Cohere rerank API, cross-encoder/ms-marco), generator model (Claude, GPT-4, Llama via Together), chunk size, and top-k settings, and the tool instantiates the described pipeline against a registered eval dataset in a single command: rag-bench --config pipeline.yaml --dataset squad_v2. Evaluate each query in the dataset through the full pipeline — retrieve, rerank if configured, generate — and compute RAGAS-style metrics for each sample: faithfulness (does the answer contradict the retrieved context?), answer relevancy (does the answer address the question?), context precision (are the top chunks actually relevant?), and context recall (were all ground-truth relevant chunks retrieved?), using LLM-as-judge calls for faithfulness and relevancy scoring to avoid requiring a separate NLI model. Persist all run results to a SQLite database keyed by pipeline config hash and dataset ID, so re-running the same config after a code change shows a diff of changed metrics rather than re-evaluating everything, and a --compare flag renders a side-by-side table of any two stored runs with delta columns color-coded green/red by improvement direction. Build an optional Next.js dashboard launched by rag-bench dashboard that reads the SQLite results database and renders an interactive parallel-coordinates plot where each axis is a metric and each line is a pipeline configuration, making it visually obvious which config trade-offs (e.g., BM25 retrieval trades context recall for speed) dominate across the metric space. Add a sensitivity analysis mode that runs ablations by systematically varying one hyperparameter at a time (chunk_size: [256, 512, 1024], top_k: [3, 5, 10]) while holding others fixed, generating a parameter-sensitivity heatmap that shows which knobs have the highest leverage on each metric so teams know where to focus tuning effort.", stack: ["RAGAS", "Next.js"], difficulty: "Advanced", vibes: ["RAG", "Eval"] },
  { title: "EdgeStream", tagline: "Real-time video processing on the edge.", description: "Apply AI filters to webrtc streams server-side at edge POPs.", prompt: "Build a mediasoup-based selective forwarding unit (SFU) deployed on Fly.io machines in multiple regions where WebRTC publishers send their video stream to the nearest POP and mediasoup's server-side consumer track provides raw video frames to a WASM-based processing pipeline compiled from Rust using wasm-pack, applying per-room AI filters (background blur, face detection overlay, style transfer) to the frame stream before forwarding to subscriber peers. Implement the WASM video processing module in Rust using the image crate for frame manipulation, integrating a compiled ONNX model (background segmentation using MediaPipe's SelfieSegmentation model or face landmark detection) via the tract ONNX runtime compiled to WASM, processing each 720p frame in under 15ms on a single CPU core to maintain 30fps throughput within the mediasoup frame pipeline. Build a React control panel UI that connects to the room via WebSocket to the mediasoup server, lets the room host toggle per-publisher filters from a sidebar (No Filter, Background Blur, Cartoon Style, Face Landmarks, Pixel Art), and sends filter change commands to the edge server which hot-swaps the WASM pipeline for that publisher's processing chain without dropping the WebRTC connection. Add a per-room analytics panel visible to the host showing real-time processing latency per publisher (time from frame capture to forward), frame drop rate when the processing pipeline falls behind, and bandwidth consumed by the room in Mbps, fetched from a mediasoup room statistics endpoint every 2 seconds and rendered as Recharts sparklines. Implement adaptive quality degradation: if the WASM processing pipeline latency exceeds 20ms for more than 3 consecutive frames, the edge server automatically switches that publisher to a lighter filter (or No Filter), notifying the control panel with a yellow badge, and attempting to restore the original filter after 30 seconds, balancing visual richness against latency SLA.", stack: ["mediasoup", "WASM"], difficulty: "Advanced", vibes: ["Video", "Edge"] },
  { title: "PolicyProbe", tagline: "Auto-test LLM safety policies with evolving attacks.", description: "Generates jailbreak attempts and reports policy holes.", prompt: "Build a Python red-team harness that maintains a population of candidate adversarial prompts as a Postgres-backed dataset, each tagged with a category (direct jailbreak, roleplay framing, indirect instruction, multi-turn escalation, encoded payload) and an effectiveness score from 0 to 1, and evolves the population using a genetic algorithm where selection, crossover, and mutation operators are themselves LLM calls to Claude that splice and mutate high-scoring prompts. Run each candidate prompt against the target model's API with a configurable safety policy (system prompt or safety classifier), evaluate the response using an automated harm classifier (fine-tuned on the OpenAI moderation dataset or a custom rubric), and score it as 0 (safe refusal), 0.5 (borderline), or 1.0 (policy violation), storing the full (prompt, response, score) triple in Postgres for audit. Evolve the population over 50 generations using tournament selection (keep top 20% of each generation), single-point crossover that splices the first half of one prompt with the second half of another at a sentence boundary detected via spaCy, and a mutation operator that asks Claude to paraphrase or encode a portion of the prompt in a different framing (hypothetical, persona, base64 obfuscation) — rerunning evaluation on each new candidate. Build a Next.js reporting dashboard that shows per-generation violin plots of effectiveness scores, a category breakdown of which attack types have the highest average effectiveness, and a searchable table of the top-50 highest-scoring prompts with their responses and harm scores, accessible only to authenticated operators via a time-limited JWT to prevent the red-team corpus from becoming a jailbreak cookbook. Add a differential mode that runs the same evolved population against two different policy configurations (e.g., system prompt v1 vs. v2) in parallel and produces a side-by-side comparison of effectiveness rates per category, quantifying which policy changes closed specific attack vectors and which introduced regressions, giving safety teams a metric-driven way to evaluate system prompt changes.", stack: ["Claude", "Next.js"], difficulty: "Advanced", vibes: ["Safety", "Eval"] },
  { title: "CapacityCharts", tagline: "Predict your cloud costs from PR diffs.", description: "Looks at PR resource changes and forecasts new infra spend.", prompt: "Build a GitHub Actions composite action that triggers on pull_request events, runs terraform plan -json on the changed Terraform files to capture the full planned resource diff as a machine-readable JSON output, and parses the resource additions, changes, and deletions from the resource_changes array to build a structured inventory of what's being created or destroyed. Map each Terraform resource type to a cost formula using Infracost's published cost components or a custom YAML pricing config (e.g., aws_instance.m5.xlarge → $0.192/hr × 730hr/month = $140.16/month), computing the monthly cost delta by summing cost increases from new resources and subtracting cost savings from removed resources, with separate line items for compute, storage, data transfer, and managed services. Extrapolate a 30-day cost projection using a simple linear model seeded with the current month's actual spend pulled from the AWS Cost Explorer API or a GCP Billing export BigQuery table, adding the delta and accounting for partial-month prorating based on the PR merge date relative to billing period boundaries. Post the forecast as a formatted GitHub PR comment using the GitHub Checks API with a collapsible cost breakdown table showing resource name, type, old cost, new cost, and delta per row, colored green for savings and red for increases, with a summary line showing the total monthly delta and a confidence note when pricing data is estimated vs. exact. Add an optional cost gate that fails the GitHub Check (blocking merge) if the monthly delta exceeds a configurable threshold stored in a .costlimit.yml file at the repo root, supporting different limits per Terraform workspace or environment tag so production changes face a stricter budget gate than staging changes.", stack: ["Terraform", "Infracost"], difficulty: "Advanced", vibes: ["FinOps"] },
  { title: "VoxelVoice", tagline: "Voxel-based 3D world built collaboratively via voice commands.", description: "Say 'add a tower at the lake' and the world updates.", prompt: "Build a Next.js multiplayer voxel world using Three.js with a sparse octree voxel data structure stored in Liveblocks' storage, rendering a 128×64×128 voxel grid as a single merged geometry built with greedy meshing to minimize draw calls, with voxel mutations broadcast to all connected clients via Liveblocks' real-time sync so every player sees changes within 50ms of a voice command completing. Implement the voice command pipeline: the user presses and holds Space to record audio via MediaRecorder, the audio blob is sent to Whisper API (or whisper.cpp via a serverless Rust function) for transcription, and the transcribed text is sent to Claude with a tool-use schema defining voxel mutation tools — place_block(x, y, z, material), remove_block(x, y, z), fill_region(x1,y1,z1, x2,y2,z2, material), clone_region, and generate_structure(description, anchor_point). Parse the LLM's tool_use response to extract the exact mutation operations, execute them against the Liveblocks voxel storage with optimistic local updates for instant feedback, and show a chat-style log panel that records each voice command, the Whisper transcription, and the Claude-parsed operation in human-readable form (e.g., 'You said: add a tower → placed 48 stone blocks at (64,32,64)') so users understand what the LLM interpreted. Implement a palette system with 16 material types (grass, stone, wood, glass, sand, water, lava, snow, etc.) each stored as a voxel type byte, rendered with distinct colors and ambient occlusion shading, and allow the LLM to reference materials by descriptive name so commands like 'build a glass dome' correctly select the glass material without the user needing to know material IDs. Add player presence avatars using Liveblocks' presence API: each player's position and looking direction is broadcast every 100ms, rendered as a floating name-badge arrow in the voxel world so collaborators can see where their teammates are standing while issuing voice commands, and add a world save/load feature that snapshots the Liveblocks storage to a Postgres JSON column so sessions can be restored after everyone leaves.", stack: ["Three.js", "Whisper"], difficulty: "Advanced", vibes: ["Voice", "3D", "Multiplayer"] },
  { title: "Trace2Test", tagline: "Generate Playwright tests from real user sessions.", description: "Replay session, get a deterministic test for the most-used flow.", prompt: "Build a rrweb-based session recording SDK that instruments any web app with a single script tag, capturing DOM mutations, mouse clicks, keyboard input (with PII masking for password fields), scroll events, and network requests as a compressed event stream, buffered in the browser and flushed to an API endpoint every 30 seconds or on page unload, stored in a Postgres table with session_id, user_id (anonymized), start_time, and a JSONB events column. Implement a session clustering pipeline that runs nightly via a cron job: represent each session as a sequence of CSS selector strings derived from the rrweb click events, compute pairwise Levenshtein edit distances between selector sequences, and run hierarchical agglomerative clustering to group sessions by similar navigation paths, selecting the medoid session from each cluster as the representative example for that user flow. Generate Playwright test code from the medoid session's event stream using a code generator that maps rrweb events to Playwright API calls: click events → page.click(selector), input events → page.fill(selector, value), scroll events → page.evaluate(() => window.scrollBy(x, y)), and navigation events → await page.goto(url), wrapping the sequence in a test('user flow N', async ({ page }) => { }) block and adding expect(page).toHaveURL() assertions at each navigation. Post-process the generated Playwright test through a Claude API call that rewrites the selector chains to prefer stable user-facing attributes (aria-label, data-testid, visible text) over fragile CSS class selectors detected in the rrweb recording, improves variable names, removes redundant scroll steps, and adds a short comment above each logical section grouping related actions (e.g., // User logs in, // User adds item to cart). Build a Next.js test management dashboard where engineers see all identified user flows ranked by frequency (sessions count per cluster), download the generated Playwright test file for any flow, mark a flow as Approved or Needs Review, and trigger a re-generation run after the underlying app has changed, building a continuously updated regression test suite derived directly from real user behavior.", stack: ["rrweb", "Playwright"], difficulty: "Advanced", vibes: ["QA", "AI"] },
  { title: "CryptoCat", tagline: "Educational tool: animals act out cryptography.", description: "ECC, RSA, and Diffie-Hellman explained as cat negotiations.", prompt: "Build a Next.js interactive educational site where whimsical animal characters (Alice the Otter, Bob the Bear, Eve the Fox) act as the principals in classic cryptographic protocol walkthroughs, each protocol presented as an animated scene where speech bubbles show the actual mathematical values exchanged while simplified emoji icons illustrate the conceptual meaning, bridging the gap between abstract math and intuitive understanding. Implement a step-by-step Diffie-Hellman key exchange scene: the user can edit the prime p and generator g as input fields, the site computes Alice's private key a, her public key g^a mod p, Bob's private key b, his public key g^b mod p, and the shared secret g^(ab) mod p, animating each value being 'passed' as a scroll between the animal characters with a caption explaining why Eve the Fox watching the exchange can't compute the secret from the public keys alone. Add an RSA encryption walkthrough where the user chooses two small primes and the site computes n = p×q, Euler's totient, public exponent e, private exponent d using the extended Euclidean algorithm, then lets the user type a short message, encrypts it as a number using modular exponentiation, and decrypts it back — all with each step animated as Alice sealing a padlock and Bob unlocking it. Build an ECC (Elliptic Curve) module that renders a real elliptic curve (y² = x³ + ax + b mod p for small p) as an interactive SVG scatter plot of its points over F_p, lets users click two points to see the group addition operation animated geometrically (reflection across the curve and the horizontal line), and steps through scalar multiplication to illustrate how ECDH key agreement works on the curve. Include a quiz layer that pauses after each animated scene and asks the user two multiple-choice questions about what they just observed — 'Why can't Eve compute the shared secret?' — with immediate feedback and a Feynman-style explanation for wrong answers, storing quiz progress in localStorage to track which concepts the user has mastered and suggesting the next topic to study.", stack: ["Next.js", "Framer Motion"], difficulty: "Intermediate", vibes: ["Crypto", "Edu"] },
  { title: "TranspileTwins", tagline: "Compare LLM-translated code across languages.", description: "Rust to Python translation? See semantic equivalence checks.", prompt: "Build a Next.js code translation playground with a Monaco editor on the left for source code and a Monaco pane on the right for the translated output, where the user selects source and target language from a dropdown (supporting Rust, Python, TypeScript, Go, Java, C++), pastes their code, and the LLM translation is streamed token-by-token into the right pane using Claude's streaming API with a system prompt that instructs idiomatic translation rather than literal line-for-line conversion. Run property-based equivalence tests after translation: extract all function signatures from both the source and translated code using tree-sitter language-specific parsers, generate 20 random input combinations for each function using a type-aware fuzzer (bounded integers, non-empty strings, short lists), execute both the original and translated function on the same inputs in a sandboxed subprocess (Pyodide for Python, Deno for TypeScript, a compiled WASM binary for Rust/C++), and diff the outputs. Highlight divergences in a diff panel below the editors: each input combination that produced different outputs is shown as a failing test case row with the input value, the source function's output, and the translated function's output side-by-side, with a severity badge (Type Error, Value Mismatch, Exception vs. Value, Both Error) helping the developer understand whether the bug is a type coercion issue or a semantic logic difference. Add a second-pass refinement button that feeds the divergent test cases back to the LLM along with both code versions and asks it to fix the translation to match the source behavior on the failing inputs, streaming the revised translation into the right pane and re-running the equivalence tests automatically until all 20 cases pass or the user manually aborts after 3 refinement rounds. Include a translation history stored in localStorage that logs each (source_lang, target_lang, source_code, translated_code, pass_rate) tuple so developers can revisit prior translations, track how well Claude handles different language pairs for their specific codebase patterns, and share a permalink URL containing the base64-encoded source code and language selection for team review.", stack: ["WASM", "Claude"], difficulty: "Advanced", vibes: ["DevTools", "AI"] },
  { title: "InferenceLab", tagline: "Compare inference latency across providers.", description: "Pick a model, measure time-to-first-token across hosting providers.", prompt: "Build a Next.js dashboard backed by a Vercel Edge Function scheduler that benchmarks Llama 3 8B and Mistral 7B inference across major hosting providers (Together AI, Groq, Fireworks, Anyscale, Replicate, AWS Bedrock, Azure ML) every 15 minutes using a fixed 512-token prompt and requesting a 128-token completion, measuring time-to-first-token (TTFT), total completion latency, and tokens-per-second throughput for each provider from 5 geographic regions using Fly.io distributed workers. Store all benchmark results in a ClickHouse table with columns for provider, model, region, ttft_ms, total_ms, tokens_per_second, and cost_per_1k_tokens computed from each provider's published pricing, enabling fast OLAP aggregates over millions of data points without performance degradation as the historical dataset grows. Render the main dashboard as a multi-panel view: a real-time latency comparison bar chart sorted by current median TTFT, an interactive time-series chart showing TTFT trends over the last 7 days per provider with zoom and pan controls, a world-map heatmap showing per-region latency using Mapbox GL, and a cost-efficiency scatter plot with TTFT on the x-axis and cost on the y-axis with each provider as a labeled point so users can immediately see the speed-cost Pareto frontier. Implement an API endpoint /v1/recommend that accepts a user's requirements (max_ttft_ms, max_cost_per_1k_tokens, required_model, preferred_region) and returns a ranked list of providers meeting all constraints, computed from the last 15-minute benchmark window, so applications can dynamically route to the optimal provider rather than hard-coding a choice. Add a custom benchmark runner where users submit their own prompt and desired token count, the service runs it against all configured providers simultaneously from the user's nearest Fly.io region, returns the real results within 30 seconds, and adds the data point to the ClickHouse historical record with a user_submitted flag so the community dataset grows richer over time with real-world diverse prompts.", stack: ["Next.js", "ClickHouse"], difficulty: "Intermediate", vibes: ["LLM", "Infra"] },
];

function buildProjects(): Project[] {
  const sections: Array<[string, Project["category"], Omit<Project, "id" | "category">[]]> = [
    ["saas", "saas", SAAS],
    ["creative", "creative", CREATIVE],
    ["utility", "utility", UTILITY],
    ["social", "social", SOCIAL],
    ["deeptech", "deeptech", DEEPTECH],
  ];

  const out: Project[] = [];
  for (const [prefix, category, list] of sections) {
    list.forEach((p, i) => {
      out.push({
        id: `${prefix}-${String(i + 1).padStart(3, "0")}`,
        category,
        ...p,
      });
    });
  }
  return out;
}

export const PROJECTS: Project[] = buildProjects();
